/* MS-06: isolated Music Studio projects with safe Standard MIDI File round-trip. */
(function(root){
  'use strict';

  const HOME_ROUTE='music-studio';
  const PROJECTS_ROUTE=`${HOME_ROUTE}/recent-projects`;
  const PROJECTS_ALIAS_ROUTE=`${HOME_ROUTE}/projects`;
  const NEW_ROUTE=`${HOME_ROUTE}/new-project`;
  const EDIT_ROUTE=`${HOME_ROUTE}/project`;
  const MIDI_EDITOR_ROUTE=`${HOME_ROUTE}/midi-editor`;
  const SETTINGS_ROUTE=`${HOME_ROUTE}/settings`;
  const BACKUP_ROUTE=`${HOME_ROUTE}/backup`;
  const LOGIC_PRO_ROUTE=`${HOME_ROUTE}/logic-pro`;
  const FIRST_SONG_GUIDE_ROUTE=`${HOME_ROUTE}/first-song-guide`;
  const FORMAT='music-studio-project';
  const SCHEMA_VERSION='1.0';
  const APP_VERSION='1.4.0';
  const DB_NAME='music-studio-projects';
  const STORE_NAME='projects';
  const SETTINGS_STORE_NAME='settings';
  const AUTO_BACKUP_STORE_NAME='autoBackups';
  const MIDI_HISTORY_STORE_NAME='midiExportHistory';
  const MIDI_IMPORT_HISTORY_STORE_NAME='midiImportHistory';
  const SETTINGS_ID='current';
  const SETTINGS_FORMAT='music-studio-settings';
  const SETTINGS_VERSION=1;
  const BACKUP_FORMAT='music-studio-backup';
  const BACKUP_VERSION=1;
  const PIANO_PITCH_COUNT=128;
  const PIANO_ROW_HEIGHT=18;
  const PIANO_HEADER_HEIGHT=46;
  const LAST_PROJECT_KEY='musicStudio_lastProjectId_v1';
  const STATUS_LABELS={available:'使用可能',working:'作業中',planned:'未実装'};
  const PROJECT_STATUS_LABELS={idea:'アイデア',draft:'制作中',review:'確認中',complete:'完成',paused:'保留'};
  const FEATURES=[
    {id:'new-project',icon:'＋',title:'新しい音楽プロジェクト',description:'曲名や制作目的を決めて、新しい制作を始める入口です。',status:'available'},
    {id:'recent-projects',icon:'◷',title:'最近使ったプロジェクト',description:'最近編集したMusic Studioプロジェクトへ戻る一覧です。',status:'available'},
    {id:'logic-pro',icon:'LP',title:'Logic Pro X連携',description:'標準MIDIと音声参照を使った安全な手動受け渡しを案内します。',status:'working'},
    {id:'midi-composer',icon:'♬',title:'MIDI Composer',description:'Melody／Drums／Bass共通のMIDIノート編集画面です。',status:'available'},
    {id:'lyrics-notes',icon:'あ',title:'歌詞・音符割付',description:'歌詞の読みと音符の対応を確認・調整します。',status:'planned'},
    {id:'ai-import',icon:'AI',title:'AI作曲データ取り込み',description:'AI作曲サービスの出力を確認して複製取り込みします。',status:'planned'},
    {id:'instrument-midi',icon:'Pf',title:'楽器別MIDI',description:'パートと楽器ごとにMIDI素材を整理します。',status:'planned'},
    {id:'sounds-plugins',icon:'Fx',title:'音色・プラグイン管理',description:'使用する音色、プラグイン、プリセット情報を管理します。',status:'planned'},
    {id:'mixing',icon:'Mx',title:'ミックス支援',description:'音量、定位、エフェクトなどのミックスメモを整理します。',status:'planned'},
    {id:'mastering',icon:'Ms',title:'マスタリング支援',description:'完成音源の確認項目と書き出しメモを管理します。',status:'planned'},
    {id:'files',icon:'▤',title:'ファイル管理',description:'MIDI、音声、歌詞などの参照ファイルを整理します。',status:'planned'},
    {id:'backup',icon:'⇩',title:'バックアップ',description:'設定とプロジェクトを安全に書き出し・追加復元します。',status:'available'},
    {id:'settings',icon:'⚙',title:'Music Studio設定',description:'保存、自動保存、MIDI、Logic Pro X、表示と製品情報を管理します。',status:'available'},
    {id:'dream-architect',icon:'DA',title:'Dream Architect Studioへ戻る',description:'制作アプリをまとめるDream Architect Studioへ戻ります。',status:'available',action:'dream'},
    {id:'send-nova',icon:'N',title:'Nova Studioへ送る',description:'楽曲情報を自動登録せず、確認してNova Studioへ渡す入口です。',status:'working'}
  ];
  const state={projects:[],loaded:false,notice:'',noticeKind:'info',search:'',dirty:false,currentDraft:null,settings:null,settingsDraft:null,saveStatus:'saved',saveSequence:0,savedSequence:0,editGeneration:0,autosaveTimer:null,intervalTimer:null,backupPreview:null,autoBackups:[],midiExportHistory:[],midiImportHistory:[],midiExportPreview:null,midiImportPreview:null,midiExporting:false,midiImporting:false,lastMidiExport:null,midiEditor:null,partialEditSession:null,midiAddNoteSequence:{session:null,part:'',noteIds:[]},midiEditorSaveTimer:null,midiEditorViewSaveTimer:null,midiEditorSaveInFlight:false,midiEditorSaveRequested:false,midiEditorSavePromise:null,midiInput:{initialized:false,supported:null,access:null,inputs:[],boundInputs:[],inputSignature:'',selectedId:'',targetPart:'melody',messageGate:null,recorder:null,recording:false,recordingPart:null,recordingTrackId:null,starting:false,stopping:false,countingIn:false,countInBeat:0,countInToken:0,countInTimer:null,countInResolve:null,countInAdapter:null,recordRequestedAt:0,countInStartedAt:0,recordingScheduledAt:0,recordingActivatedAt:0,recordingFrame:null,recordingStartedAt:0,recordingStartTick:0,recordingPlayback:null,liveNotes:[],liveNoteSequence:0,status:''},melodyAudio:{synth:null,playing:false,starting:false,playRequest:0,playbackTimer:null,playbackFrame:null,metronomeToken:0,metronomeTimer:null,playbackState:{mutedByTrackId:{},soloByTrackId:{}},transport:null,status:''}};

  state.partialEditProvider={provider:'openai',model:'',intent:'simpler',text:'',status:'idle',errorCode:'',runId:0};

  function defaultSettings(timestamp=now()){return{
    id:SETTINGS_ID,format:SETTINGS_FORMAT,version:SETTINGS_VERSION,createdAt:timestamp,updatedAt:timestamp,appVersion:APP_VERSION,
    general:{language:'ja',dateFormat:'ja-JP',confirmDialogs:true,notifications:true,confirmDangerousActions:true},
    projectDefaults:{bpm:120,timeSignature:'4/4',key:null,status:'idea',purpose:'',notes:''},
    autosave:{enabled:true,intervalSeconds:60,idleDelaySeconds:3,manualSave:true},
    midi:{fileType:1,ppq:480,autoChannel:true,useTrackNames:true,includeTempo:true,includeTimeSignature:true,omitEmptyTracks:true,quantize:'1/16',velocity:100,channel:1},
    logicPro:{method:'file-export',preferMidi:true,audioReferences:'external',autoLaunch:false,directControl:false,showInstructions:true,intelSafe:true},
    fileNaming:{template:'{projectName}_{type}_{date}',maxLength:120,fallback:'music-studio'},
    backup:{manualEnabled:true,automaticEnabled:false,intervalHours:24,maxCopies:10,showScope:true,confirmRestore:true,safetyCopyBeforeRestore:true},
    display:{theme:'music-studio',fontSize:'standard',cardDensity:'standard',reducedMotion:'system'},
    accessibility:{focusVisible:true,keyboard:true,textStatus:true},
    privacy:{externalTransfer:false,usageData:false,analytics:false,cloudSync:false,localOnly:true},
    experimental:{enabled:false,features:{}}
  }}
  function clampNumber(value,min,max,fallback,integer=false){const number=Number(value);if(!Number.isFinite(number))return fallback;const safe=Math.min(max,Math.max(min,number));return integer?Math.round(safe):safe}
  function mergeKnown(base,input){if(!input||typeof input!=='object'||Array.isArray(input))return clone(base);const result=clone(base);for(const key of Object.keys(base)){if(input[key]===undefined)continue;if(base[key]&&typeof base[key]==='object'&&!Array.isArray(base[key]))result[key]=mergeKnown(base[key],input[key]);else result[key]=input[key]}return result}
  function normalizeSettings(input={},timestamp=now()){
    const base=defaultSettings(input.createdAt||timestamp),value=mergeKnown(base,input);
    value.id=SETTINGS_ID;value.format=SETTINGS_FORMAT;value.version=SETTINGS_VERSION;value.appVersion=APP_VERSION;
    value.createdAt=typeof input.createdAt==='string'&&!Number.isNaN(Date.parse(input.createdAt))?input.createdAt:timestamp;value.updatedAt=typeof input.updatedAt==='string'&&!Number.isNaN(Date.parse(input.updatedAt))?input.updatedAt:timestamp;
    value.projectDefaults.bpm=clampNumber(value.projectDefaults.bpm,20,400,120);
    if(!/^(?:[1-9]|1[0-6])\/(?:1|2|4|8|16|32)$/.test(String(value.projectDefaults.timeSignature)))value.projectDefaults.timeSignature='4/4';
    const keys=[null,'C','C#','Db','D','D#','Eb','E','F','F#','Gb','G','G#','Ab','A','A#','Bb','B','Cm','C#m','Dbm','Dm','D#m','Ebm','Em','Fm','F#m','Gbm','Gm','G#m','Abm','Am','A#m','Bbm','Bm'];if(!keys.includes(value.projectDefaults.key))value.projectDefaults.key=null;
    if(!Object.keys(PROJECT_STATUS_LABELS).includes(value.projectDefaults.status))value.projectDefaults.status='idea';
    value.projectDefaults.purpose=String(value.projectDefaults.purpose||'').slice(0,240);value.projectDefaults.notes=String(value.projectDefaults.notes||'').slice(0,10000);
    value.autosave.intervalSeconds=clampNumber(value.autosave.intervalSeconds,15,3600,60,true);value.autosave.idleDelaySeconds=clampNumber(value.autosave.idleDelaySeconds,1,30,3,true);
    value.midi.fileType=[0,1].includes(Number(value.midi.fileType))?Number(value.midi.fileType):1;value.midi.ppq=clampNumber(value.midi.ppq,24,9600,480,true);value.midi.velocity=clampNumber(value.midi.velocity,1,127,100,true);value.midi.channel=clampNumber(value.midi.channel,1,16,1,true);
    if(!['1/4','1/8','1/16','1/32'].includes(value.midi.quantize))value.midi.quantize='1/16';
    value.backup.intervalHours=clampNumber(value.backup.intervalHours,1,720,24,true);value.backup.maxCopies=clampNumber(value.backup.maxCopies,1,100,10,true);
    value.fileNaming.maxLength=clampNumber(value.fileNaming.maxLength,20,200,120,true);value.fileNaming.fallback=safeFileName(value.fileNaming.fallback||'music-studio');
    value.fileNaming.template=String(value.fileNaming.template||base.fileNaming.template).slice(0,200);if(!/\{(?:projectName|songTitle|date|time|version|type)\}/.test(value.fileNaming.template))value.fileNaming.template=base.fileNaming.template;
    for(const group of ['general','autosave','midi','logicPro','backup','accessibility','privacy'])for(const key of Object.keys(value[group]))if(typeof base[group][key]==='boolean')value[group][key]=value[group][key]===true;
    value.logicPro.autoLaunch=false;value.logicPro.directControl=false;value.privacy.externalTransfer=false;value.privacy.usageData=false;value.privacy.analytics=false;value.privacy.cloudSync=false;value.privacy.localOnly=true;value.experimental.enabled=false;value.experimental.features={};return value
  }
  function validateSettings(value){const errors=[];if(!value||typeof value!=='object'||Array.isArray(value))return{valid:false,errors:['JSONのルートがオブジェクトではありません。']};if(value.format!==SETTINGS_FORMAT)errors.push(`形式は ${SETTINGS_FORMAT} である必要があります。`);if(value.version!==SETTINGS_VERSION)errors.push(`Version ${value.version??'未設定'} には対応していません。`);return{valid:errors.length===0,errors}}

  function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}
  function uuid(){return root.crypto?.randomUUID?.()||`ms-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,12)}`}
  function now(){return new Date().toISOString()}
  function clone(value){return JSON.parse(JSON.stringify(value))}
  function safeStorage(){try{return root.localStorage||null}catch(_){return null}}
  function setLastProject(id){try{const storage=safeStorage();if(id)storage?.setItem(LAST_PROJECT_KEY,id);else storage?.removeItem(LAST_PROJECT_KEY)}catch(error){console.warn('Music Studio: last project could not be saved',error)}}
  function getLastProject(){try{return safeStorage()?.getItem(LAST_PROJECT_KEY)||''}catch(_){return ''}}
  function makeProject(input={},timestamp=now()){
    const defaults=state.settings?.projectDefaults||defaultSettings(timestamp).projectDefaults;
    const bpm=Number(input.bpm??input.musicalSettings?.bpm??defaults.bpm);
    const existingSignature=input.musicalSettings?.timeSignature;const signature=String(input.timeSignature||(existingSignature?`${existingSignature.numerator}/${existingSignature.denominator}`:defaults.timeSignature)).split('/').map(Number);
    const defaultSignature=String(defaults.timeSignature).split('/').map(Number);
    const project={
      format:FORMAT,schemaVersion:SCHEMA_VERSION,appVersion:APP_VERSION,projectId:input.projectId||uuid(),revision:Number(input.revision)||1,
      projectName:String(input.projectName||'').trim(),songTitle:String(input.songTitle||'').trim(),purpose:String(input.purpose??defaults.purpose??'').trim(),status:input.status||defaults.status,
      musicalSettings:{bpm,timeSignature:{numerator:signature[0]||defaultSignature[0]||4,denominator:signature[1]||defaultSignature[1]||4},key:input.key===''?null:(input.key??input.musicalSettings?.key??defaults.key),bars:input.musicalSettings?.bars??null},
      productionNotes:String(input.productionNotes??input.notes??defaults.notes??'').trim(),sections:[],chordProgressions:[],melodies:[],instruments:[],midiAssets:[],audioAssets:[],
      lyrics:{versions:[],activeVersionId:null},syllableAssignments:[],soundSelections:[],plugins:[],mixNotes:[],masteringNotes:[],productionHistory:[],
      fileReferences:Array.isArray(input.fileReferences)?clone(input.fileReferences):[],integrations:{dreamArchitect:[],novaStudio:[],logicPro:[]},
      legacy:{sourceFormat:null,sourceVersion:null,extensions:{}},createdAt:input.createdAt||timestamp,updatedAt:input.updatedAt||timestamp
    };
    const preserve=['sections','chordProgressions','melodies','instruments','midiAssets','audioAssets','lyrics','syllableAssignments','soundSelections','plugins','mixNotes','masteringNotes','productionHistory','integrations','legacy','midiData','importSource'];
    preserve.forEach(key=>{if(input[key]!=null)project[key]=clone(input[key])});
    return project;
  }
  function validateProject(value){
    const errors=[];
    if(!value||typeof value!=='object'||Array.isArray(value))return{valid:false,errors:['JSONのルートがオブジェクトではありません。']};
    if(value.format!==FORMAT)errors.push(`形式は ${FORMAT} である必要があります。`);
    if(value.schemaVersion!==SCHEMA_VERSION)errors.push(`schemaVersion ${value.schemaVersion||'未設定'} には対応していません。`);
    if(typeof value.projectName!=='string'||!value.projectName.trim())errors.push('プロジェクト名は必須です。');
    if(typeof value.projectId!=='string'||!value.projectId.trim())errors.push('projectIdは必須です。');
    if(!Number.isInteger(value.revision)||value.revision<1)errors.push('revisionは1以上の整数である必要があります。');
    const bpm=Number(value.musicalSettings?.bpm);
    if(!Number.isFinite(bpm)||bpm<20||bpm>400)errors.push('BPMは20〜400で入力してください。');
    const ts=value.musicalSettings?.timeSignature;
    if(!Number.isInteger(ts?.numerator)||ts.numerator<1||![1,2,4,8,16,32].includes(ts?.denominator))errors.push('拍子が正しい形式ではありません。');
    for(const field of ['createdAt','updatedAt'])if(typeof value[field]!=='string'||Number.isNaN(Date.parse(value[field])))errors.push(`${field}はISO 8601日時である必要があります。`);
    return{valid:errors.length===0,errors};
  }

  function memoryRepository(initial=[]){
    const records=new Map(initial.map(item=>[item.projectId,clone(item)]));
    const automaticBackups=new Map();let settings=null;
    const midiHistory=new Map(),midiImportHistory=new Map();return{kind:'memory',async list(){return [...records.values()].map(clone)},async get(id){return records.has(id)?clone(records.get(id)):null},async put(item){records.set(item.projectId,clone(item));return clone(item)},async delete(id){records.delete(id)},async has(id){return records.has(id)},async getSettings(){return settings?clone(settings):null},async putSettings(value){settings=clone(value);return clone(settings)},async listAutoBackups(){return [...automaticBackups.values()].map(clone)},async putAutoBackup(value){automaticBackups.set(value.backupId,clone(value));return clone(value)},async deleteAutoBackup(id){automaticBackups.delete(id)},async listMidiHistory(){return [...midiHistory.values()].map(clone)},async putMidiHistory(value){midiHistory.set(value.id,clone(value));return clone(value)},async listMidiImportHistory(){return [...midiImportHistory.values()].map(clone)},async putMidiImportHistory(value){midiImportHistory.set(value.id,clone(value));return clone(value)}};
  }
  function indexedDbRepository(){
    if(!root.indexedDB)return memoryRepository();
    const database=new Promise((resolve,reject)=>{const request=root.indexedDB.open(DB_NAME,5);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains(STORE_NAME)){const store=request.result.createObjectStore(STORE_NAME,{keyPath:'projectId'});store.createIndex('updatedAt','updatedAt')}if(!request.result.objectStoreNames.contains(SETTINGS_STORE_NAME))request.result.createObjectStore(SETTINGS_STORE_NAME,{keyPath:'id'});if(!request.result.objectStoreNames.contains(AUTO_BACKUP_STORE_NAME)){const store=request.result.createObjectStore(AUTO_BACKUP_STORE_NAME,{keyPath:'backupId'});store.createIndex('createdAt','createdAt')}if(!request.result.objectStoreNames.contains(MIDI_HISTORY_STORE_NAME)){const store=request.result.createObjectStore(MIDI_HISTORY_STORE_NAME,{keyPath:'id'});store.createIndex('createdAt','createdAt')}if(!request.result.objectStoreNames.contains(MIDI_IMPORT_HISTORY_STORE_NAME)){const store=request.result.createObjectStore(MIDI_IMPORT_HISTORY_STORE_NAME,{keyPath:'id'});store.createIndex('createdAt','createdAt')}};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);request.onblocked=()=>reject(Error('データベース更新がほかのタブでブロックされています。'))});
    const request=(storeName,mode,operation)=>database.then(db=>new Promise((resolve,reject)=>{const transaction=db.transaction(storeName,mode);const result=operation(transaction.objectStore(storeName));result.onsuccess=()=>resolve(result.result);result.onerror=()=>reject(result.error);transaction.onabort=()=>reject(transaction.error)}));
    return{kind:'indexeddb',async list(){return request(STORE_NAME,'readonly',store=>store.getAll())},async get(id){return request(STORE_NAME,'readonly',store=>store.get(id))},async put(item){await request(STORE_NAME,'readwrite',store=>store.put(clone(item)));return clone(item)},async delete(id){await request(STORE_NAME,'readwrite',store=>store.delete(id))},async has(id){return Boolean(await request(STORE_NAME,'readonly',store=>store.getKey(id)))},async getSettings(){return request(SETTINGS_STORE_NAME,'readonly',store=>store.get(SETTINGS_ID))},async putSettings(value){await request(SETTINGS_STORE_NAME,'readwrite',store=>store.put(clone(value)));return clone(value)},async listAutoBackups(){return request(AUTO_BACKUP_STORE_NAME,'readonly',store=>store.getAll())},async putAutoBackup(value){await request(AUTO_BACKUP_STORE_NAME,'readwrite',store=>store.put(clone(value)));return clone(value)},async deleteAutoBackup(id){await request(AUTO_BACKUP_STORE_NAME,'readwrite',store=>store.delete(id))},async listMidiHistory(){return request(MIDI_HISTORY_STORE_NAME,'readonly',store=>store.getAll())},async putMidiHistory(value){await request(MIDI_HISTORY_STORE_NAME,'readwrite',store=>store.put(clone(value)));return clone(value)},async listMidiImportHistory(){return request(MIDI_IMPORT_HISTORY_STORE_NAME,'readonly',store=>store.getAll())},async putMidiImportHistory(value){await request(MIDI_IMPORT_HISTORY_STORE_NAME,'readwrite',store=>store.put(clone(value)));return clone(value)}};
  }
  let repository=indexedDbRepository();
  function setRepository(next){repository=next}
  async function loadSettings(){let stored=null;try{stored=await repository.getSettings?.()}catch(error){console.warn('Music Studio settings could not be read',error)}const normalized=normalizeSettings(stored||{});state.settings=normalized;state.settingsDraft=clone(normalized);if(!stored)try{await repository.putSettings?.(normalized)}catch(error){console.warn('Music Studio default settings could not be saved',error)}return normalized}
  async function refresh(){state.projects=(await repository.list()).sort((a,b)=>(b.updatedAt||'').localeCompare(a.updatedAt||''));state.loaded=true;requestPaint()}
  let noticeTimer=null;
  function clearNotice(){
    if(noticeTimer&&root.clearTimeout)root.clearTimeout(noticeTimer);
    noticeTimer=null;
    if(!state.notice)return;
    state.notice='';
    requestPaint();
  }
  function openHelp(id){const dialog=root.document?.getElementById?.(id);if(!dialog)return false;if(typeof dialog.showModal==='function')dialog.showModal();else dialog.setAttribute('open','');return true}
  function closeHelp(id){const dialog=root.document?.getElementById?.(id);if(!dialog)return false;if(typeof dialog.close==='function')dialog.close();else dialog.removeAttribute('open');return true}
  function prefersInlineFirstSongGuide(){const navigator=root.navigator||{},ua=String(navigator.userAgent||'');return/iPhone|iPod|iPad/i.test(ua)||navigator.platform==='MacIntel'&&Number(navigator.maxTouchPoints)>1}
  function openFirstSongWindow(){if(prefersInlineFirstSongGuide())return openHelp('firstSongGuide');const url=`${root.location?.pathname||'./music-studio.html'}${root.location?.search||''}#${FIRST_SONG_GUIDE_ROUTE}`,guide=root.open?.(url,'musicStudioFirstSongGuide','popup,width=720,height=900,resizable=yes,scrollbars=yes');if(!guide){openHelp('firstSongGuide');return false}guide.focus?.();return true}
  function focusMusicStudio(){if(root.opener&&!root.opener.closed){root.opener.focus?.();return true}routeTo(HOME_ROUTE);return false}
  function notice(message,kind='info'){
    if(noticeTimer&&root.clearTimeout)root.clearTimeout(noticeTimer);
    state.notice=message;state.noticeKind=kind;requestPaint();
    noticeTimer=root.setTimeout?.(()=>{noticeTimer=null;if(state.notice===message){state.notice='';requestPaint()}},2600)||null;
  }
  function routeTo(route){if(root.document?.body?.dataset.musicStudioStandalone==='true')root.location.hash=route;else if(typeof root.setView==='function')root.setView(route);else root.location.hash=route}
  function currentRoute(){return (root.location?.hash||`#${HOME_ROUTE}`).slice(1)||HOME_ROUTE}
  function requestPaint(){if(root.document?.body?.dataset.musicStudioStandalone==='true')paintStandalone();else{root.render?.();restoreEditorPitchScroll()}}
  function statusBadge(status){return `<span class="music-status is-${status}">${STATUS_LABELS[status]}</span>`}
  function routeFor(item){return `${HOME_ROUTE}/${item.id}`}
  function navAction(item,standalone){if(item.action==='dream')return standalone?"location.href='./index.html#dream-architect'":"setView('dream-architect')";if(item.id==='midi-composer')return'MusicStudio.openMidiEditor()';return standalone?`location.hash='${routeFor(item)}'`:`setView('${routeFor(item)}')`}
  const TERM_HELP={BPM:'曲の速さ。数値が大きいほど速くなります。',拍子:'1小節に何拍あるかを表します。4/4は1小節に4拍です。',Key:'曲の中心になる音と調（メジャー／マイナー）です。',MIDI:'音そのものではなく、音程・長さ・強さなどの演奏データです。',Pitch:'音の高さです。MIDIでは0〜127の番号で表します。',Velocity:'ノートを鳴らす強さです。値が大きいほど強い演奏になります。',Snap:'ノートの位置や長さをグリッドに合わせます。',Grid:'ノートを置く時間位置の目盛りです。',Quantize:'演奏タイミングを指定したグリッドへ近づける補正です。',Swing:'交互のタイミングをずらして跳ねるリズム感を作ります。',PPQ:'1拍を何分割して記録するかを示すMIDIの時間解像度です。',Loop:'指定範囲を繰り返し再生します。',FitRange:'使用している音域が画面内に収まるように表示します。',Import:'外部ファイルの内容をMusic Studioへ取り込みます。',Export:'Music Studioの内容を外部ファイルとして書き出します。'};
  function helpButton(id,label='この画面のヘルプ'){return`<button type="button" class="music-help-button" aria-label="${escapeHtml(label)}" onclick="MusicStudio.openHelp('${escapeHtml(id)}')"><span aria-hidden="true">?</span><span class="music-help-button-label">ヘルプ</span></button>`}
  function firstSongHelpButton(label='Melody制作のヘルプ'){return`<button type="button" class="music-help-button music-editor-guide-button" aria-label="${escapeHtml(label)}" onclick="MusicStudio.openFirstSongWindow()"><span aria-hidden="true">?</span><span class="music-help-button-label">ヘルプ</span></button>`}
  function helpDialog(id,title,body){return`<dialog class="music-guide-dialog" id="${escapeHtml(id)}" aria-labelledby="${escapeHtml(id)}Title"><div class="music-guide-dialog-head"><h2 id="${escapeHtml(id)}Title">${escapeHtml(title)}</h2><button type="button" class="music-guide-close" aria-label="ヘルプを閉じる" onclick="MusicStudio.closeHelp('${escapeHtml(id)}')">× 閉じる</button></div>${body}</dialog>`}
  function steps(items){return`<ol class="music-guide-steps">${items.map(item=>`<li>${item}</li>`).join('')}</ol>`}
  function saveGuide(){return`<section class="music-save-guide" aria-labelledby="saveGuideTitle"><h3 id="saveGuideTitle">保存について</h3><div class="music-save-compare"><article><b>自動保存</b><p>Music Studio内部へ保存。同じ端末・同じブラウザで再度開くための保存です。クラウド同期ではありません。</p></article><article><b>JSON</b><p>Project全体の外部バックアップ。後からMusic Studioへ読み込んで復元できます。</p></article><article><b>MIDI</b><p>音程・長さなどの演奏データをLogic ProなどのDAWへ渡すファイルです。</p></article></div></section>`}
  function termsGuide(){return`<details class="music-terms-guide"><summary>用語を確認</summary><dl>${Object.entries(TERM_HELP).map(([term,description])=>`<div><dt>${escapeHtml(term==='FitRange'?'Fit Range':term)}</dt><dd>${escapeHtml(description)}</dd></div>`).join('')}</dl></details>`}
  function homeHelpDialog(){return helpDialog('musicHomeHelp','Music Studio Home ヘルプ',`${steps(['「新しい音楽プロジェクト」で曲の基本情報を作る','Projectを開き、Melody → Drums → Bassの順に制作する','Savedを確認し、必要な時にJSONバックアップを作る','Logic Proへ渡す時はMIDIを書き出す'])}${saveGuide()}${termsGuide()}`)}
  function firstSongGuideContent(){return`${steps(['新しいProjectを作る','プロジェクト名・曲名・BPM・拍子・Keyを設定する','Melody制作を開く','MIDIキーボードを使う場合は、上部の「MIDI入力」を押して使用するMIDIキーボードを選択する（SafariなどWeb MIDIを利用できない環境ではMac Chromeを使用）','「Record（録音）」を押してMIDIキーボードを弾き、「Stop（停止）」を押して録音を確定する。Piano Rollに録音したノートが表示されたことを確認する','Piano Rollでノートの位置・長さ・Velocityを修正する','Playで確認する','録音・編集後、上部の Saved（保存済み）を確認する（この端末・このブラウザのMusic Studio保存領域への自動保存。クラウド同期ではありません）','JSONを書き出してProject全体を外部バックアップする','Export Melody MIDIでMelodyの演奏データを書き出す','Logic ProでMIDIファイルを読み込む','Logic ProからStandard MIDI Fileを書き出し、Logic Pro IntegrationのImport MIDIでMusic Studioへ新規または複製として戻す'])}${saveGuide()}${termsGuide()}`}
  function firstSongDialog(){return helpDialog('firstSongGuide','最初の1曲を作る',firstSongGuideContent())}
  function firstSongGuideView(){return`<main class="music-studio-shell music-guide-window" aria-labelledby="firstSongWindowTitle"><header class="music-page-header"><p class="music-kicker">BEGINNER GUIDE</p><h1 id="firstSongWindowTitle">最初の1曲を作る</h1><p>Music Studio本体を操作しながら、このガイドを読み続けられます。</p><button type="button" class="music-primary" aria-label="Music Studio本体へ戻る" onclick="MusicStudio.focusMusicStudio()">← Music Studio本体へ戻る</button></header><section class="music-guide-window-content">${firstSongGuideContent()}</section></main>`}
  function card(item,standalone){const label=item.status==='available'?'開く':item.status==='working'?'入口を見る':'準備中ページを見る';return `<article class="music-feature-card is-${item.status}" data-music-feature-id="${escapeHtml(item.id)}"><div class="music-card-top"><span class="music-feature-icon" aria-hidden="true">${escapeHtml(item.icon)}</span>${statusBadge(item.status)}</div><h2>${escapeHtml(item.title)}</h2><p>${escapeHtml(item.description)}</p><button class="${item.status==='available'?'music-primary':'music-secondary'}" onclick="${navAction(item,standalone)}">${label}</button></article>`}
  function recentProjects(limit=3){const last=getLastProject();return [...state.projects].sort((a,b)=>a.projectId===last?-1:b.projectId===last?1:(b.updatedAt||'').localeCompare(a.updatedAt||'')).slice(0,limit)}
  function homeView(options={}){const standalone=Boolean(options.standalone),homeFeatures=FEATURES.filter(item=>item.id!=='dream-architect');const counts=Object.keys(STATUS_LABELS).map(status=>`<div><b>${homeFeatures.filter(item=>item.status===status).length}</b><span>${STATUS_LABELS[status]}</span></div>`).join('');const recent=recentProjects().map(project=>`<button class="music-recent-item" onclick="MusicStudio.openProject('${escapeHtml(project.projectId)}')"><b>${escapeHtml(project.projectName)}</b><span>${escapeHtml(project.songTitle||'曲名未設定')} · ${formatDate(project.updatedAt)}</span></button>`).join('')||'<p class="music-empty">プロジェクトはまだありません。「新しい音楽プロジェクト」から始められます。</p>';return `<main class="music-studio-shell" aria-label="Music Studio操作とプロジェクト"><nav class="music-quick-nav" aria-label="Music Studio主要ナビゲーション"><button class="music-primary" onclick="${navAction(FEATURES[0],standalone)}">＋ 新しい音楽プロジェクト</button><button class="music-secondary" onclick="${navAction(FEATURES[1],standalone)}">プロジェクト一覧</button></nav><section class="music-recent" aria-labelledby="musicRecentTitle"><div class="music-section-heading"><div><p class="music-kicker">Recent</p><h2 id="musicRecentTitle">最近使ったプロジェクト</h2></div></div>${recent}</section><section class="music-status-summary" aria-label="機能の状態">${counts}</section><section aria-labelledby="musicFeaturesTitle"><div class="music-section-heading"><div><p class="music-kicker">Workspace</p><h2 id="musicFeaturesTitle">制作メニュー</h2></div></div><div class="music-feature-grid">${homeFeatures.map(item=>card(item,standalone)).join('')}</div></section><footer class="music-footer"><p><b>独立保存：</b>Music Studio専用領域を使用し、既存アプリの保存データは変更しません。</p></footer></main>`}
  function flowNavigation(){return ''}
  function logicFlowNavigation(project){return project?flowNavigation('プロジェクト詳細',`MusicStudio.openProject('${escapeHtml(project.projectId)}')`,'Music Studio設定','MusicStudio.goSettings()'):flowNavigation('Music Studioホーム','MusicStudio.goHome()','Music Studio設定','MusicStudio.goSettings()')}
  function backButton(){const editorId=midiEditorId(currentRoute());return editorId?flowNavigation('プロジェクト詳細',`MusicStudio.openProject('${escapeHtml(editorId)}')`,'Music Studioホーム','MusicStudio.goHome()'):currentRoute()===LOGIC_PRO_ROUTE?logicFlowNavigation(selectedMidiProject()):flowNavigation('Music Studioホーム','MusicStudio.goHome()','プロジェクト一覧','MusicStudio.goProjects()')}
  function noticeView(){return state.notice?`<div class="music-notice is-${state.noticeKind}" role="status"><span>${escapeHtml(state.notice)}</span><button type="button" aria-label="通知を閉じる" onclick="MusicStudio.clearNotice()">×</button></div>`:''}
  function formFields(project={}){const ts=project.musicalSettings?.timeSignature;const signature=ts?`${ts.numerator}/${ts.denominator}`:'4/4';const key=project.musicalSettings?.key??'';const signatures=['2/4','3/4','4/4','5/4','6/8','7/8','12/8'];const keys=['C','C#','Db','D','D#','Eb','E','F','F#','Gb','G','G#','Ab','A','A#','Bb','B','Cm','C#m','Dbm','Dm','D#m','Ebm','Em','Fm','F#m','Gbm','Gm','G#m','Abm','Am','A#m','Bbm','Bm','C♯/D♭','D♯/E♭','F♯/G♭','G♯/A♭','A♯/B♭'];return `<div class="music-form-grid"><label for="msProjectName">プロジェクト名 <span aria-hidden="true">*</span><input id="msProjectName" name="projectName" required maxlength="120" value="${escapeHtml(project.projectName||'')}"></label><label for="msSongTitle">曲名<input id="msSongTitle" name="songTitle" maxlength="120" value="${escapeHtml(project.songTitle||'')}"></label><label class="music-span-2" for="msPurpose">制作目的または使用作品<input id="msPurpose" name="purpose" maxlength="240" value="${escapeHtml(project.purpose||'')}"></label><label for="msBpm">BPM<input id="msBpm" name="bpm" type="number" min="20" max="400" inputmode="decimal" step="0.1" required value="${escapeHtml(project.musicalSettings?.bpm??120)}"></label><label for="msTimeSignature">拍子<select id="msTimeSignature" name="timeSignature">${signatures.map(value=>`<option ${signature===value?'selected':''}>${value}</option>`).join('')}</select></label><label for="msKey">キー<select id="msKey" name="key"><option value="">未設定</option>${keys.map(value=>`<option ${key===value?'selected':''}>${value}</option>`).join('')}</select></label><label for="msStatus">制作状態<select id="msStatus" name="status">${Object.entries(PROJECT_STATUS_LABELS).map(([value,label])=>`<option value="${value}" ${(project.status||'idea')===value?'selected':''}>${label}</option>`).join('')}</select></label><label class="music-span-2" for="msProductionNotes">制作メモ<textarea id="msProductionNotes" name="productionNotes" rows="6" maxlength="10000">${escapeHtml(project.productionNotes||'')}</textarea></label></div>`}
  function createView(){const d=state.settings?.projectDefaults||defaultSettings().projectDefaults;const project=makeProject({projectName:'',bpm:d.bpm,timeSignature:d.timeSignature,key:d.key,status:d.status,purpose:d.purpose,productionNotes:d.notes});return `<main class="music-studio-shell music-project-page" aria-labelledby="musicCreateTitle">${flowNavigation('プロジェクト一覧','MusicStudio.goProjects()','プロジェクト一覧','MusicStudio.goProjects()')}${noticeView()}<section class="music-project-panel"><p class="music-kicker">New project</p><h1 id="musicCreateTitle">新しい音楽プロジェクト</h1><p class="music-lead">入力内容はMusic Studio専用領域へ保存されます。</p><form id="musicProjectForm" oninput="MusicStudio.markDirty()" onsubmit="MusicStudio.submitCreate(event)">${formFields(project)}<div class="music-form-actions"><button class="music-primary" type="submit">作成して保存</button><button class="music-secondary" type="button" onclick="MusicStudio.cancelEdit()">キャンセル</button><span class="music-save-state" data-dirty="${state.dirty}" aria-live="polite">${state.dirty?'未保存の変更があります':'未変更'}</span></div></form></section></main>`}
  function projectRow(project){return `<article class="music-project-row"><div><h2>${escapeHtml(project.projectName)}</h2><p>${escapeHtml(project.songTitle||'曲名未設定')} · ${escapeHtml(PROJECT_STATUS_LABELS[project.status]||project.status)} · BPM ${escapeHtml(project.musicalSettings?.bpm)} · ${escapeHtml(project.musicalSettings?.key||'キー未設定')}</p><small>最終更新：${formatDate(project.updatedAt)}</small></div><button class="music-primary" onclick="MusicStudio.openProject('${escapeHtml(project.projectId)}')">開く</button></article>`}
  function listView(){const query=state.search.trim().toLocaleLowerCase('ja');const items=state.projects.filter(project=>!query||[project.projectName,project.songTitle,project.purpose].some(value=>String(value||'').toLocaleLowerCase('ja').includes(query))).sort((a,b)=>(b.updatedAt||'').localeCompare(a.updatedAt||''));return `<main class="music-studio-shell music-project-page" aria-labelledby="musicProjectsTitle">${flowNavigation('Music Studioホーム','MusicStudio.goHome()','新しい音楽プロジェクト','MusicStudio.goNew()')}${noticeView()}<section class="music-project-panel"><div class="music-section-heading"><div><p class="music-kicker">Projects</p><h1 id="musicProjectsTitle">音楽プロジェクト</h1></div><button class="music-primary" onclick="MusicStudio.goNew()">＋ 新規作成</button></div><label class="music-search" for="musicProjectSearch">プロジェクトを検索<input id="musicProjectSearch" type="search" value="${escapeHtml(state.search)}" placeholder="プロジェクト名・曲名・使用作品" oninput="MusicStudio.searchProjects(this.value)"></label><div class="music-project-list">${state.loaded?(items.map(projectRow).join('')||`<div class="music-empty"><p>${query?'検索条件に一致するプロジェクトはありません。':'プロジェクトはまだありません。'}</p><button class="music-primary" onclick="MusicStudio.goNew()">最初のプロジェクトを作る</button></div>`):'<p class="music-empty">プロジェクトを読み込んでいます…</p>'}</div><div class="music-import-box"><label for="musicProjectImport">JSONから読み込む<input id="musicProjectImport" type="file" accept="application/json,.json" onchange="MusicStudio.importFile(this.files[0]);this.value=''" hidden></label><button class="music-secondary" onclick="document.querySelector('#musicProjectImport').click()">JSONを読み込む</button><p>既存データを消さず、Version 1形式を新規または複製として追加します。</p></div></section></main>`}
  function editView(id){const project=state.projects.find(item=>item.projectId===id);if(!project)return `<main class="music-studio-shell music-project-page">${flowNavigation('プロジェクト一覧','MusicStudio.goProjects()','Music Studioホーム','MusicStudio.goHome()')}<div class="music-empty"><h1>プロジェクトが見つかりません</h1><p>削除済みか、URLが正しくない可能性があります。</p><button class="music-primary" onclick="MusicStudio.goProjects()">一覧へ戻る</button></div></main>`;return `<main class="music-studio-shell music-project-page" aria-labelledby="musicEditTitle">${flowNavigation('プロジェクト一覧','MusicStudio.goProjects()','Melody制作',`MusicStudio.openPart('${escapeHtml(project.projectId)}','melody')`)}${noticeView()}<section class="music-project-panel"><div class="music-section-heading"><div><p class="music-kicker">Project</p><h1 id="musicEditTitle">${escapeHtml(project.projectName)}</h1><p>作成：${formatDate(project.createdAt)} / 更新：${formatDate(project.updatedAt)} / Revision ${project.revision}</p></div></div><form id="musicProjectForm" oninput="MusicStudio.markDirty()" onsubmit="MusicStudio.submitEdit(event,'${escapeHtml(project.projectId)}')">${formFields(project)}<div class="music-form-actions"><button class="music-primary" type="submit">変更を保存</button><button class="music-secondary" type="button" onclick="MusicStudio.cancelEdit('${escapeHtml(project.projectId)}')">キャンセル</button><span class="music-save-state" data-dirty="${state.dirty}">${state.dirty?'未保存の変更があります':'保存済み'}</span></div></form><div class="music-project-tools"><button class="music-primary" onclick="MusicStudio.openPart('${escapeHtml(project.projectId)}','melody')">Melody制作を開く</button><button class="music-secondary" onclick="MusicStudio.openPart('${escapeHtml(project.projectId)}','drums')">Drums制作を開く</button><button class="music-secondary" onclick="MusicStudio.openPart('${escapeHtml(project.projectId)}','bass')">Bass制作を開く</button><button class="music-secondary" onclick="MusicStudio.openMidiEditor('${escapeHtml(project.projectId)}')">共通MIDIエディター</button><button class="music-secondary" onclick="MusicStudio.openLogicPro('${escapeHtml(project.projectId)}')">MIDI読み込み・Logic Pro</button><button class="music-secondary" onclick="MusicStudio.duplicateProject('${escapeHtml(project.projectId)}')">複製</button><button class="music-secondary" onclick="MusicStudio.exportProject('${escapeHtml(project.projectId)}')">JSON書き出し</button><button class="music-danger" onclick="MusicStudio.askDelete('${escapeHtml(project.projectId)}')">このプロジェクトを削除</button></div></section></main>`}
  const settingSections=[['general','一般'],['projectDefaults','プロジェクト初期値'],['autosave','自動保存'],['midi','MIDI'],['logicPro','Logic Pro X'],['fileNaming','ファイル名'],['backup','バックアップ'],['display','表示'],['accessibility','アクセシビリティ'],['privacy','プライバシー'],['appInfo','アプリ情報'],['experimental','詳細設定']];
  function checked(value){return value?' checked':''}
  function option(value,current,label=value){return `<option value="${escapeHtml(value)}"${String(value)===String(current)?' selected':''}>${escapeHtml(label)}</option>`}
  function toggle(name,label,value,help='',disabled=false){return `<label class="music-toggle"><input type="checkbox" name="${name}"${checked(value)}${disabled?' disabled':''}><span><b>${escapeHtml(label)}</b>${help?`<small>${escapeHtml(help)}</small>`:''}</span></label>`}
  function settingSection(id,title,body){return `<section id="ms-setting-${id}" class="music-setting-section" aria-labelledby="ms-setting-${id}-title"><h2 id="ms-setting-${id}-title">${title}</h2>${body}</section>`}
  function settingsView(){const s=state.settingsDraft||state.settings||defaultSettings();const nav=settingSections.map(([id,label])=>`<a href="#ms-setting-${id}">${label}</a>`).join('');return `<main class="music-studio-shell music-settings-page" aria-labelledby="musicSettingsTitle">${flowNavigation('Music Studioホーム','MusicStudio.goHome()','バックアップ管理','MusicStudio.goBackup()')}${noticeView()}<header class="music-page-header"><p class="music-kicker">MS-03 / Settings foundation</p><h1 id="musicSettingsTitle">Music Studio設定</h1><p>端末内のMusic Studio専用領域だけに保存します。プロジェクトや他アプリのデータは初期化しません。</p><span class="music-save-state" data-status="${state.saveStatus}" aria-live="polite">${state.dirty?'未保存の変更があります':state.saveStatus==='saving'?'保存中':state.saveStatus==='error'?'保存に失敗しました':'保存しました'}</span></header><div class="music-settings-layout"><nav class="music-settings-nav" aria-label="設定分類">${nav}</nav><form id="musicSettingsForm" class="music-settings-form" oninput="MusicStudio.markSettingsDirty()" onsubmit="MusicStudio.saveSettingsForm(event)">
  ${settingSection('general','一般',`<div class="music-form-grid"><label>言語<select name="general.language">${option('ja',s.general.language,'日本語')}</select></label><label>日付表示<select name="general.dateFormat">${option('ja-JP',s.general.dateFormat,'日本向け')}</select></label></div>${toggle('general.confirmDialogs','確認ダイアログ',s.general.confirmDialogs)}${toggle('general.notifications','通知',s.general.notifications)}${toggle('general.confirmDangerousActions','危険操作の確認',s.general.confirmDangerousActions)}`)}
  ${settingSection('projectDefaults','プロジェクト初期値',`<div class="music-form-grid"><label>BPM<input type="number" name="projectDefaults.bpm" min="20" max="400" step="0.1" value="${s.projectDefaults.bpm}" required></label><label>拍子<select name="projectDefaults.timeSignature">${['4/4','3/4','6/8','5/4','7/8','2/4','12/8'].map(v=>option(v,s.projectDefaults.timeSignature)).join('')}</select></label><label>キー<select name="projectDefaults.key">${option('',s.projectDefaults.key??'','未設定')}${['C','C#','Db','D','D#','Eb','E','F','F#','Gb','G','G#','Ab','A','A#','Bb','B','Cm','Dm','Em','Fm','Gm','Am','Bm'].map(v=>option(v,s.projectDefaults.key)).join('')}</select></label><label>制作状態<select name="projectDefaults.status">${Object.entries(PROJECT_STATUS_LABELS).map(([v,l])=>option(v,s.projectDefaults.status,l)).join('')}</select></label><label class="music-span-2">制作目的<input name="projectDefaults.purpose" maxlength="240" value="${escapeHtml(s.projectDefaults.purpose)}"></label><label class="music-span-2">制作メモ<textarea name="projectDefaults.notes" maxlength="10000" rows="3">${escapeHtml(s.projectDefaults.notes)}</textarea></label></div><p class="music-help">変更後に作成する新規プロジェクトだけへ反映します。</p>`)}
  ${settingSection('autosave','自動保存',`${toggle('autosave.enabled','自動保存を有効にする',s.autosave.enabled)}<div class="music-form-grid"><label>定期保存間隔（秒）<input type="number" name="autosave.intervalSeconds" min="15" max="3600" value="${s.autosave.intervalSeconds}"></label><label>入力終了後の遅延（秒）<input type="number" name="autosave.idleDelaySeconds" min="1" max="30" value="${s.autosave.idleDelaySeconds}"></label></div><p class="music-help">手動保存は常に利用できます。保存中・保存済み・未保存・失敗を文字でも表示します。</p>`)}
  ${settingSection('midi','MIDI',`<div class="music-form-grid"><label>標準MIDI形式<select name="midi.fileType">${option(1,s.midi.fileType,'Type 1')}${option(0,s.midi.fileType,'Type 0')}</select></label><label>PPQ<input type="number" name="midi.ppq" min="24" max="9600" value="${s.midi.ppq}"></label><label>クオンタイズ<select name="midi.quantize">${['1/4','1/8','1/16','1/32'].map(v=>option(v,s.midi.quantize)).join('')}</select></label><label>ベロシティ<input type="number" name="midi.velocity" min="1" max="127" value="${s.midi.velocity}"></label><label>MIDIチャンネル<input type="number" name="midi.channel" min="1" max="16" value="${s.midi.channel}"></label></div>${toggle('midi.autoChannel','チャンネルを自動割当',s.midi.autoChannel)}${toggle('midi.useTrackNames','トラック名を利用',s.midi.useTrackNames)}${toggle('midi.includeTempo','BPM情報を含める',s.midi.includeTempo)}${toggle('midi.includeTimeSignature','拍子情報を含める',s.midi.includeTimeSignature)}${toggle('midi.omitEmptyTracks','空トラックを書き出さない',s.midi.omitEmptyTracks)}<p class="music-help">MS-05ではType 1のみ正式書き出しします。Type 0は既存設定互換のため表示を維持します。</p>`)}
  ${settingSection('logicPro','Logic Pro X',`<div class="music-form-grid"><label>連携方式<select name="logicPro.method">${option('file-export',s.logicPro.method,'ファイル書き出し')}</select></label><label>オーディオ参照<select name="logicPro.audioReferences">${option('external',s.logicPro.audioReferences,'外部ファイル')}</select></label></div>${toggle('logicPro.preferMidi','MIDI書き出しを優先',s.logicPro.preferMidi)}${toggle('logicPro.showInstructions','書き出し後に手順を表示',s.logicPro.showInstructions)}<p class="music-help">自動起動と直接操作は常に無効です。Intel Macでも安全なファイル受け渡しだけを前提にします。</p>`)}
  ${settingSection('fileNaming','ファイル名',`<div class="music-form-grid"><label class="music-span-2">テンプレート<input name="fileNaming.template" maxlength="200" value="${escapeHtml(s.fileNaming.template)}"><small>{projectName} {songTitle} {type} {date} {time} {version}</small></label><label>最大文字数<input type="number" name="fileNaming.maxLength" min="20" max="200" value="${s.fileNaming.maxLength}"></label><label>空欄時の名前<input name="fileNaming.fallback" maxlength="80" value="${escapeHtml(s.fileNaming.fallback)}"></label></div><p class="music-help">使用できない文字は安全なハイフンへ置換します。未対応トークンは保存時に拒否します。</p><output id="musicFileNamePreview" class="music-filename-preview">プレビュー：${escapeHtml(renderFileName(s.fileNaming.template,{projectName:'夜空のプロジェクト',songTitle:'星明かり',type:'midi'},{maxLength:s.fileNaming.maxLength,fallback:s.fileNaming.fallback,date:'2026-07-20T21:30:00.000Z'}).filename)}.json</output>`)}
  ${settingSection('backup','バックアップ設定',`${toggle('backup.automaticEnabled','自動バックアップ',s.backup.automaticEnabled,'初期状態は無効です')}<div class="music-form-grid"><label>間隔（時間）<input type="number" name="backup.intervalHours" min="1" max="720" value="${s.backup.intervalHours}"></label><label>最大保持件数<input type="number" name="backup.maxCopies" min="1" max="100" value="${s.backup.maxCopies}"></label></div><p class="music-help">ブラウザからの手動JSONバックアップを提供します。クラウド同期と一括削除はありません。</p><button class="music-secondary" type="button" onclick="MusicStudio.goBackup()">バックアップ管理を開く</button>`)}
  ${settingSection('display','表示',`<div class="music-form-grid"><label>テーマ<select name="display.theme">${option('music-studio',s.display.theme,'Music Studio')}</select></label><label>文字サイズ<select name="display.fontSize">${option('standard',s.display.fontSize,'標準')}${option('large',s.display.fontSize,'大きい')}</select></label><label>カード密度<select name="display.cardDensity">${option('standard',s.display.cardDensity,'標準')}${option('compact',s.display.cardDensity,'コンパクト')}</select></label><label>動きの軽減<select name="display.reducedMotion">${option('system',s.display.reducedMotion,'OS設定に従う')}${option('reduce',s.display.reducedMotion,'軽減する')}</select></label></div>`)}
  ${settingSection('accessibility','アクセシビリティ',`${toggle('accessibility.focusVisible','フォーカス表示',s.accessibility.focusVisible)}${toggle('accessibility.keyboard','キーボード操作',s.accessibility.keyboard)}${toggle('accessibility.textStatus','状態を文字でも表示',s.accessibility.textStatus)}`)}
  ${settingSection('privacy','プライバシー',`<p><b>保存場所：</b>端末内のMusic Studio専用IndexedDB</p>${toggle('privacy.externalTransfer','外部送信',false,'安全のため無効固定',true)}${toggle('privacy.analytics','解析送信',false,'安全のため無効固定',true)}${toggle('privacy.cloudSync','クラウド同期',false,'未実装',true)}`)}
  ${settingSection('appInfo','アプリ情報',`<dl class="music-info-list"><div><dt>アプリ名</dt><dd>Music Studio</dd></div><div><dt>所属</dt><dd>Dream Architect Studio</dd></div><div><dt>現在のバージョン</dt><dd>${APP_VERSION}</dd></div><div><dt>開発段階</dt><dd>Music Studio 0.4 実装完了</dd></div><div><dt>設定形式</dt><dd>${SETTINGS_FORMAT} Version ${SETTINGS_VERSION}</dd></div><div><dt>プロジェクト形式</dt><dd>${FORMAT} Version 1</dd></div><div><dt>バックアップ形式</dt><dd>${BACKUP_FORMAT} Version ${BACKUP_VERSION}</dd></div><div><dt>保存場所</dt><dd>端末内</dd></div><div><dt>外部送信</dt><dd>行いません</dd></div></dl><p>単体アプリ化を前提とし、端末内保存のみを行います。ライセンス情報とCHANGELOGは同梱文書を参照してください。</p><p><a href="./docs/music-studio/MS-00C_LICENSE_REGISTER.md">ライセンス情報</a> · <a href="./CHANGELOG.md">CHANGELOG</a></p>`)}
  ${settingSection('experimental','詳細設定',`<p>将来機能用です。販売版で不安定機能を誤って有効化しないため、すべて無効です。</p>${toggle('experimental.enabled','実験機能',false,'現在は変更できません',true)}`)}
  <div class="music-sticky-actions"><button class="music-primary" type="submit">変更を保存</button><button class="music-secondary" type="button" onclick="MusicStudio.discardSettings()">変更を破棄</button><button class="music-secondary" type="button" onclick="MusicStudio.resetSettings()">初期値に戻す</button><button class="music-secondary" type="button" onclick="MusicStudio.exportSettings()">設定を書き出す</button><input id="musicSettingsImport" type="file" accept="application/json,.json" hidden onchange="MusicStudio.importSettingsFile(this.files[0]);this.value=''"><button class="music-secondary" type="button" onclick="document.querySelector('#musicSettingsImport').click()">設定を読み込む</button></div></form></div></main>`}
  function backupView(){const preview=state.backupPreview;const automatic=state.autoBackups.map(item=>`<article class="music-auto-backup-row"><div><b>${formatDate(item.createdAt)}</b><small>プロジェクト ${item.snapshot?.projects?.length||0}件 · ${escapeHtml(item.sizeLabel||'容量不明')}</small></div><button class="music-secondary" onclick="MusicStudio.restoreAutoBackup('${escapeHtml(item.backupId)}')">この自動バックアップを復元</button></article>`).join('')||'<p class="music-empty">自動バックアップはまだありません。</p>';return `<main class="music-studio-shell music-settings-page" aria-labelledby="musicBackupTitle">${flowNavigation('Music Studio設定','MusicStudio.goSettings()','プロジェクト一覧','MusicStudio.goProjects()')}${noticeView()}<header class="music-page-header"><p class="music-kicker">Local JSON backup</p><h1 id="musicBackupTitle">Music Studioバックアップ</h1><p>対象：設定1件、プロジェクト${state.projects.length}件、参照ファイルのメタ情報。MIDI・音声・画像本体は含みません。</p></header><section class="music-setting-section"><h2>手動バックアップを書き出す</h2><p>既存データを変更せず、Music Studio全体をVersion 1 JSONへまとめます。手動ファイルは自動整理の対象外です。</p><button class="music-primary" onclick="MusicStudio.exportBackup()">全体バックアップを書き出す</button></section><section class="music-setting-section"><h2>バックアップを検証・復元</h2><input id="musicBackupImport" type="file" accept="application/json,.json" hidden onchange="MusicStudio.previewBackupFile(this.files[0]);this.value=''"><button class="music-secondary" onclick="document.querySelector('#musicBackupImport').click()">バックアップJSONを選択</button>${preview?`<div class="music-backup-preview" role="status"><h3>復元前の概要</h3><p>作成：${formatDate(preview.metadata.createdAt)} / プロジェクト ${preview.projects.length}件 / 参照 ${preview.references?.length||0}件</p><label><input id="restoreSettings" type="checkbox" checked> 設定を復元</label><label><input id="restoreProjects" type="checkbox" checked> プロジェクトを追加・統合</label><p>同じIDは新しいIDで追加します。不正な項目はスキップして件数を報告します。既存データは削除・上書きせず、復元前に現在状態を安全コピーとしてダウンロードします。</p><button class="music-primary" onclick="MusicStudio.restorePreviewedBackup()">内容を確認して復元</button><button class="music-secondary" onclick="MusicStudio.clearBackupPreview()">キャンセル</button></div>`:''}</section><section class="music-setting-section"><div class="music-section-heading"><div><h2>端末内の自動バックアップ</h2><p>Music Studioを開いた時だけ期限を確認します。バックグラウンド常駐は行いません。</p></div><button class="music-secondary" onclick="MusicStudio.checkAutoBackupNow()">期限を確認</button></div><div class="music-auto-backup-list">${automatic}</div></section></main>`}
  function selectedMidiProject(){const last=getLastProject();return state.projects.find(project=>project.projectId===last)||state.projects.find(project=>project.midiData)||state.projects[0]||null}
  const MIDI_EXPORT_SCOPES={melody:'Melody',drums:'Drums',bass:'Bass',all:'All'};
  function currentMidiExportProject(projectId=''){const targetId=projectId||state.midiEditor?.projectId||getLastProject(),project=state.projects.find(item=>item.projectId===targetId)||selectedMidiProject();return project&&state.midiEditor?.projectId===project.projectId?{...project,midiData:clone(state.midiEditor.midiData)}:project}
  function midiExportInput(project,scope='all'){if(!project?.midiData)return null;const normalized=root.MusicStudioEditor?.normalizeMidiData?root.MusicStudioEditor.normalizeMidiData(project.midiData,project):clone(project.midiData),wanted=scope==='all'?['melody','drums','bass']:[scope],tracks=(normalized.tracks||[]).map((track,index)=>{if(track.part)return track;const name=String(track.name||'').toLowerCase(),part=track.channel===10||/drum|ドラム/.test(name)?'drums':/bass|ベース/.test(name)?'bass':index===0?'melody':null;return part?{...track,part}:track}).filter(track=>wanted.includes(track.part));return{...clone(normalized),tracks:tracks.map(clone)}}
  function midiExportSummary(project,scope='all'){const midi=root.MusicStudioMidi,label=MIDI_EXPORT_SCOPES[scope]||MIDI_EXPORT_SCOPES.all;if(!project||!midi)return{project,scope,label,validation:{ok:false,errors:[!project?'プロジェクトが選択されていません。':'MIDI生成モジュールを読み込めません。'],warnings:[],trackCount:0,noteCount:0}};const input=midiExportInput(project,scope),validation=midi.validateMidiProjectData(input),filename=`${safeFileName(project.projectName,100,'Music-Studio')}_${label}.mid`;return{project,scope,label,input,validation,filename}}
  function exportPreviewHtml(summary){const p=summary.project,v=summary.validation;if(!p)return`<p class="music-empty">プロジェクトが選択されていません。先にプロジェクトを作成または開いてください。</p>`;const d=summary.input||{},ts=d.timeSignature||p.musicalSettings?.timeSignature||{};return`<dl class="music-info-list"><div><dt>対象</dt><dd>${escapeHtml(p.projectName)} / ${escapeHtml(summary.label)}</dd></div><div><dt>曲名</dt><dd>${escapeHtml(p.songTitle||'未設定')}</dd></div><div><dt>MIDI Type</dt><dd>Type 1（正式対応）</dd></div><div><dt>BPM / 拍子 / PPQ</dt><dd>${escapeHtml(d.tempo??'—')} / ${escapeHtml(`${ts.numerator||'—'}/${ts.denominator||'—'}`)} / ${escapeHtml(d.ppq??'—')}</dd></div><div><dt>トラック / ノート</dt><dd>${v.trackCount} / ${v.noteCount}</dd></div><div><dt>トラック名 / channel</dt><dd>${(v.data?.tracks||[]).filter(track=>track.notes.length).map(track=>`${escapeHtml(track.name)}（${track.channel}）`).join('<br>')||'—'}</dd></div><div><dt>予定ファイル名</dt><dd class="music-break">${escapeHtml(summary.filename)}</dd></div></dl>${v.warnings.length?`<div class="music-validation is-warning"><b>警告</b><ul>${v.warnings.map(item=>`<li>${escapeHtml(item)}</li>`).join('')}</ul></div>`:''}${v.errors.length?`<div class="music-validation is-error" role="alert"><b>エラー</b><ul>${v.errors.map(item=>`<li>${escapeHtml(item)}</li>`).join('')}</ul></div>`:''}<div class="music-export-actions">${Object.entries(MIDI_EXPORT_SCOPES).map(([scope,label])=>{const scoped=midiExportSummary(p,scope);return`<button class="${scope==='all'?'music-primary':'music-secondary'}" type="button" ${scoped.validation.ok&&!state.midiExporting?'':`disabled`} onclick="MusicStudio.performMidiExport('${scope}','${escapeHtml(p.projectId)}')">${state.midiExporting?'書き出し中…':`${label} MIDI`}</button>`}).join('')}<button class="music-secondary" type="button" onclick="MusicStudio.goSettings()">設定を開く</button><button class="music-secondary" type="button" onclick="MusicStudio.openProject('${escapeHtml(p.projectId)}')">プロジェクトを開く</button></div><p class="music-help">各ボタンは対象パートだけを含むMIDI、AllはMelody / Drums / Bassを別トラックで含むMIDIを書き出します。</p>`}
  function historyHtml(){return state.midiExportHistory.length?`<div class="music-history-list">${state.midiExportHistory.map(item=>`<details><summary>${escapeHtml(item.filename)} · ${formatDate(item.createdAt)}</summary><p>${escapeHtml(item.projectName)} / Type ${item.midiType} / ${item.trackCount}トラック / ${item.noteCount}ノート / ${byteLabel(item.fileSize)}</p><p>内部検査：${item.inspectionOk?'成功':'失敗'}。ファイル本体は保存していません。</p></details>`).join('')}</div>`:'<p class="music-empty">書き出し履歴はまだありません。履歴にはメタ情報だけを保存し、MIDI本体は保持しません。</p>'}
  function requestMidiExport(){state.midiExportPreview=midiExportSummary(selectedMidiProject());if(!state.midiExportPreview.validation.ok)notice(`MIDIを書き出せません：${state.midiExportPreview.validation.errors.join(' ')}`,'error');else requestPaint();return state.midiExportPreview}
  function cancelMidiExport(){state.midiExportPreview=null;state.midiExporting=false;notice('MIDI書き出しをキャンセルしました。入力データは変更していません。','info')}
  async function createMidiTestProject(){const midi=root.MusicStudioMidi;if(!midi)return notice('MIDI生成モジュールを読み込めません。','error');const timestamp=now(),project=makeProject({projectName:'MS-05 MIDI書き出しテスト',songTitle:'Cメジャー音階',midiData:midi.createTestMidiData()},timestamp);try{await repository.put(project);state.projects=[project,...state.projects];setLastProject(project.projectId);notice('既存プロジェクトを変更せず、テスト用MIDIプロジェクトを作成しました。','success')}catch(error){console.warn(error);notice('テスト用プロジェクトを保存できませんでした。既存データは変更していません。','error')}return project}
  async function loadMidiHistory(){try{state.midiExportHistory=((await repository.listMidiHistory?.())||[]).sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));return state.midiExportHistory}catch(error){console.warn('Music Studio MIDI history could not be read',error);state.midiExportHistory=[];return[]}}
  async function performMidiExport(scope='all',projectId=''){if(state.midiExporting)return{ok:false,message:'書き出し処理中です。'};const summary=midiExportSummary(currentMidiExportProject(projectId),scope);if(!summary.validation.ok){notice(`${summary.label} MIDIを書き出せません：${summary.validation.errors.join(' ')}`,'error');return{ok:false,validation:summary.validation}}state.midiExporting=true;requestPaint();let result;try{result=root.MusicStudioMidi.createMidiFile(summary.input);const blob=root.MusicStudioMidi.createMidiBlob(result.bytes);if(blob.size===0)throw Error('生成ファイルが0バイトです。');if(!root.document||!root.URL?.createObjectURL)throw Error('このブラウザではダウンロードURLを作成できません。');const url=root.URL.createObjectURL(blob);try{const link=root.document.createElement('a');link.href=url;link.download=summary.filename;root.document.body.appendChild(link);link.click();link.remove()}finally{root.setTimeout?.(()=>root.URL.revokeObjectURL(url),1000)}const history={id:uuid(),createdAt:now(),projectId:summary.project.projectId,projectName:summary.project.projectName,songTitle:summary.project.songTitle||'',scope,format:'smf',midiType:1,ppq:result.inspection.ppq,bpm:summary.input.tempo,timeSignature:`${summary.input.timeSignature.numerator}/${summary.input.timeSignature.denominator}`,trackCount:result.validation.trackCount,noteCount:result.validation.noteCount,filename:summary.filename,fileSize:result.bytes.length,success:true,warnings:result.validation.warnings,appVersion:APP_VERSION,inspectionOk:result.inspection.ok};try{await repository.putMidiHistory?.(history);await loadMidiHistory()}catch(error){console.warn('MIDI export history could not be saved',error);history.historyWarning='履歴を保存できませんでした。'}state.lastMidiExport={...history,inspection:result.inspection};notice(`${summary.filename}（${byteLabel(result.bytes.length)}）を書き出しました。内部検査は成功しました。${history.historyWarning||''}`,'success');return{ok:true,...result,blob,filename:summary.filename,history}}catch(error){console.error('Music Studio MIDI export failed',error);notice(`MIDIを書き出せませんでした：${error.message} 入力データは変更していません。`,'error');return{ok:false,error}}finally{state.midiExporting=false;requestPaint()}}
  function fileResult(id,message,kind='info'){const target=root.document?.querySelector(id);if(target){target.className=`music-file-result is-${kind}`;target.textContent=message}return{ok:kind==='success',message}}
  function inspectAudioReference(file){if(!file)return{ok:false,cancelled:true,message:'ファイル選択をキャンセルしました。'};if(!/\.(?:wav|wave|aif|aiff|caf)$/i.test(file.name||''))return fileResult('#logicAudioResult','対応していない形式です。WAV、AIFF、CAFを選択してください。','error');return fileResult('#logicAudioResult',`${file.name} / ${file.type||'形式情報なし'} / ${byteLabel(file.size)}。このタブ内で確認しただけで、本体・パスは保存していません。`,'success')}
  const LOGIC_EXPORT_STEPS=['Logic Proで編集を終え、元のLogicプロジェクトを先に保存します。','Standard MIDI Fileとして書き出します。複数トラックを維持する場合はType 1を推奨します。Type 0は単一トラック内にイベントがまとまります。','Music Studioで.midを選び、解析概要、トラック、テンポ／拍子、警告を確認します。','既存プロジェクトを守るため、新規プロジェクトまたは現在プロジェクトの複製へ保存します。','取り込み後にノート等を確認し、必要なら再びType 1 MIDIとして書き出します。','Drums MIDIはtrack名「Drums」、MIDI channel 10、Program Changeなしで書き出します。Logic ProがStudio Grandなどのピアノ音源を自動で割り当てた場合は、Drum Kit Designer等のドラム音源を手動で選択し、Kick 36／Snare 38／Closed Hi-Hat 42を確認します。','元のLogicプロジェクトとMIDIファイルは変更されません。音源、プラグイン、オーディオ、オートメーションはMIDIへ完全には含まれません。'];
  function importWarningsHtml(items){return items.length?`<div class="music-validation is-warning"><b>警告・未対応情報</b><ul>${items.map(item=>`<li>${escapeHtml(item.message||item)}</li>`).join('')}</ul></div>`:'<p class="music-help">重大な警告はありません。</p>'}
  function importPreviewHtml(){const preview=state.midiImportPreview;if(!preview)return'<p class="music-empty">MIDIを選択すると、保存前に解析結果を表示します。ファイル本体や絶対パスは保存しません。</p>';if(preview.error)return`<div class="music-validation is-error" role="alert"><b>取り込み不可</b><p>${escapeHtml(preview.error)}</p><button class="music-secondary" onclick="MusicStudio.cancelMidiImport()">閉じる</button></div>`;const s=preview.summary,tracks=s.tracks,assignments=root.MusicStudioMidiParser?.suggestTrackParts(preview.parsed.normalized.tracks)||{},partNames={melody:'Melody',drums:'Drums',bass:'Bass'};return`<div class="music-import-preview" role="region" aria-labelledby="midiImportPreviewTitle"><h3 id="midiImportPreviewTitle">解析結果：${escapeHtml(s.fileName)}</h3><dl class="music-info-list"><div><dt>形式 / 容量</dt><dd>SMF Type ${s.midiType} / ${byteLabel(s.fileSize)}</dd></div><div><dt>PPQ / トラック</dt><dd>${s.ppq} / 全${s.trackCount}・演奏${s.playableTrackCount}</dd></div><div><dt>イベント / ノート</dt><dd>${s.eventCount.toLocaleString()} / ${s.noteCount.toLocaleString()}</dd></div><div><dt>初期BPM / 変更</dt><dd>${Math.round(s.initialBpm*1000)/1000} / ${s.tempoChangeCount}回</dd></div><div><dt>初期拍子 / 変更</dt><dd>${s.initialTimeSignature} / ${s.timeSignatureChangeCount}回</dd></div><div><dt>総tick / 推定時間</dt><dd>${s.totalTick.toLocaleString()} / ${Math.floor(s.estimatedDurationSeconds/60)}分${Math.round(s.estimatedDurationSeconds%60)}秒</dd></div><div><dt>歌詞 / マーカー</dt><dd>${s.lyrics} / ${s.markers}</dd></div><div><dt>取り込み可否</dt><dd>${s.importable?'保存可能':'解析のみ（保存不可）'}</dd></div></dl><details open><summary>トラック割り当て</summary><p class="music-help">名前・channel・Programから安全に判定できた主要トラックだけを割り当てます。その他のトラックも保存し、勝手に既存パートへ上書きしません。</p><div class="music-track-table-wrap"><table class="music-track-table"><thead><tr><th scope="col">名前</th><th scope="col">割り当て</th><th scope="col">channel</th><th scope="col">note</th><th scope="col">音域</th><th scope="col">velocity</th><th scope="col">Program / CC</th></tr></thead><tbody>${tracks.map(t=>`<tr><th scope="row">${escapeHtml(t.name)}${t.drumCandidate?'（ドラム候補）':''}</th><td>${escapeHtml(partNames[assignments[t.id]]||'追加トラック')}</td><td>${escapeHtml(t.channels.join(', ')||'—')}</td><td>${t.noteCount}</td><td>${t.range?`${t.range.min}–${t.range.max}`:'—'}</td><td>${t.velocityRange?`${t.velocityRange.min}–${t.velocityRange.max}`:'—'}</td><td>${t.programChanges.map(p=>`${p.displayProgram}: ${escapeHtml(p.generalMidiName)}`).join('<br>')||'—'} / CC ${t.controlChanges.length}</td></tr>`).join('')}</tbody></table></div></details><details><summary>テンポ／拍子</summary><p>テンポイベント ${preview.parsed.normalized.tempoMap.length}件、拍子イベント ${preview.parsed.normalized.timeSignatureMap.length}件。途中変更は拡張情報へ保持します。</p></details><details><summary>その他イベント</summary><p>Program Change ${tracks.reduce((n,t)=>n+t.programChanges.length,0)}件、Control Change ${tracks.reduce((n,t)=>n+t.controlChanges.length,0)}件、Pitch Bend ${tracks.reduce((n,t)=>n+t.pitchBends.length,0)}件。SysEx等の専用編集に未対応の情報は保持候補と警告へ残します。</p></details><details ${s.warnings.length?'open':''}><summary>警告（${s.warnings.length}）</summary>${importWarningsHtml(s.warnings)}</details><div class="music-import-actions"><button class="music-primary" ${s.importable&&!state.midiImporting?'':`disabled aria-describedby="midiImportDisabledReason"`} onclick="MusicStudio.importMidiAsNew()">新しいプロジェクトとして取り込んで編集</button><button class="music-secondary" ${s.importable&&selectedMidiProject()&&!state.midiImporting?'':`disabled aria-describedby="midiImportDisabledReason"`} onclick="MusicStudio.importMidiAsDuplicate()">現在のプロジェクトを複製して取り込んで編集</button><button class="music-secondary" onclick="MusicStudio.cancelMidiImport()">解析結果だけ確認して閉じる</button></div>${s.importable?'':`<p id="midiImportDisabledReason" class="music-help">重大エラーまたはEnd of Track不足があるため保存できません。</p>`}</div>`}
  function importHistoryHtml(){return state.midiImportHistory.length?`<div class="music-history-list">${state.midiImportHistory.map(item=>`<details><summary>${escapeHtml(item.fileName)} · ${formatDate(item.createdAt)}</summary><p>Type ${item.midiType} / PPQ ${item.ppq} / ${item.trackCount}トラック / ${item.noteCount}ノート → ${escapeHtml(item.projectName)}（${item.mode==='duplicate'?'複製':'新規'}）</p><p>警告 ${item.warningCount}件。ファイル本体は保存していません。</p></details>`).join('')}</div>`:'<p class="music-empty">読み込み履歴はまだありません。履歴へMIDI本体は保存しません。</p>'}
  function logicProView(){const summary=midiExportSummary(selectedMidiProject());return`<main class="music-studio-shell music-logic-page" aria-labelledby="logicProTitle">${backButton()}${noticeView()}<header class="music-page-header"><p class="music-kicker">MS-06 / Safe SMF round-trip</p><h1 id="logicProTitle">Logic Pro X連携</h1><p>Standard MIDI File Type 0／1を端末内だけで解析し、新規または複製へ安全に保存できます。Type 1の再書き出しにも対応します。</p><span class="music-status is-available">Type 0／1読み込み・Type 1書き出し</span></header><section class="music-setting-section" aria-labelledby="midiImportTitle"><h2 id="midiImportTitle">Logic ProからMIDIを取り込む</h2><p>16 MB以下を解析します。拡張子やMIMEだけで正常扱いせず、MThd、format、PPQ、全チャンクとイベント境界を検査します。</p><label class="music-file-label" for="logicMidiImport">Standard MIDI Fileを選択</label><input id="logicMidiImport" type="file" accept="audio/midi,audio/x-midi,.mid,.midi" onchange="MusicStudio.inspectMidiFile(this.files[0]);this.value=''" ${state.midiImporting?'disabled':''}><div id="logicMidiResult" class="music-file-result" aria-live="polite">${state.midiImporting?'解析中です…':'ファイルを選択してください。'}</div>${importPreviewHtml()}</section><section class="music-setting-section" aria-labelledby="midiExportTitle"><h2 id="midiExportTitle">Type 1 MIDIを書き出す</h2>${exportPreviewHtml(summary)}${!summary.input?`<div class="music-test-data"><h3>往復テスト用データ</h3><p>既存プロジェクトを変更せず、Piano／Bass・10ノートの新規プロジェクトを作成します。</p><button class="music-secondary" onclick="MusicStudio.createMidiTestProject()">テスト用MIDIプロジェクトを作成</button></div>`:''}</section><section class="music-setting-section"><h2>Logic Proからの安全な手順</h2><ol class="music-steps">${LOGIC_EXPORT_STEPS.map(step=>`<li>${escapeHtml(step)}</li>`).join('')}</ol><p class="music-help">iPad／iPhoneはFilesから選択できます。大規模MIDIはMacまたはiPadを推奨しますが、端末判定で禁止しません。</p></section><section class="music-integration-grid"><article class="music-setting-section"><h2>読み込み履歴</h2>${importHistoryHtml()}</article><article class="music-setting-section"><h2>書き出し履歴</h2>${historyHtml()}</article></section><section class="music-setting-section"><h2>既知の制限</h2><p>Type 2、SMPTE division、Logicプロジェクト直接読込、音源・プラグイン・オーディオの復元、SysEx編集は未対応です。Program ChangeはGeneral MIDI候補であり、Logicの実音源を断定しません。</p></section></main>`}
  async function inspectMidiFile(file){const parser=root.MusicStudioMidiParser,preflight=parser?.preflightMidiFile(file);if(!file){state.midiImportPreview=null;return{ok:false,cancelled:true,message:'ファイル選択をキャンセルしました。'}}if(!parser)return fileResult('#logicMidiResult','MIDI解析モジュールを読み込めません。','error');if(!preflight.ok)return fileResult('#logicMidiResult',`${preflight.errors.join(' ')} 元ファイルと既存プロジェクトは変更していません。`,'error');if(state.midiImporting)return{ok:false,busy:true,message:'解析中です。'};state.midiImporting=true;state.midiImportPreview=null;requestPaint();try{const buffer=await file.arrayBuffer();const parsed=parser.parseMidiFile(buffer),summary=parser.summarizeMidiFile(parsed,file);state.midiImportPreview={file:{name:file.name,size:file.size,type:file.type||''},parsed,summary,preflight};fileResult('#logicMidiResult',`Type ${summary.midiType}、PPQ ${summary.ppq}、${summary.noteCount}ノートを解析しました。保存前に内容を確認してください。`,summary.importable?'success':'error');return{ok:parsed.validation.ok,parsed,summary}}catch(error){console.warn('Music Studio MIDI import rejected',error);state.midiImportPreview={error:`${error.message} 元ファイルと既存プロジェクトは変更していません。`,file:{name:file.name,size:file.size,type:file.type||''}};fileResult('#logicMidiResult',state.midiImportPreview.error,'error');return{ok:false,error}}finally{state.midiImporting=false;requestPaint()}}
  function cancelMidiImport(){state.midiImportPreview=null;state.midiImporting=false;notice('MIDI取り込みをキャンセルしました。元ファイルと既存プロジェクトは変更していません。','info')}
  async function loadMidiImportHistory(){try{state.midiImportHistory=((await repository.listMidiImportHistory?.())||[]).sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));return state.midiImportHistory}catch(error){console.warn('Music Studio MIDI import history could not be read',error);state.midiImportHistory=[];return[]}}
  async function saveMidiImport(mode){if(state.midiImporting)return{ok:false,busy:true};const preview=state.midiImportPreview,parser=root.MusicStudioMidiParser;if(!preview?.summary?.importable)return{ok:false,message:'保存可能な解析結果がありません。'};state.midiImporting=true;requestPaint();try{const timestamp=now(),midiData=parser.convertParsedMidiToProjectData(preview.parsed,{fileName:preview.file.name,fileSize:preview.file.size,mimeType:preview.file.type,importedAt:timestamp});let input;if(mode==='duplicate'){const source=selectedMidiProject();if(!source)throw Error('複製元プロジェクトがありません。');input={...clone(source),projectId:uuid(),projectName:`${source.projectName}（MIDI取り込み複製）`,revision:1,createdAt:timestamp,updatedAt:timestamp,midiData,importSource:midiData.importSource,integrations:{...clone(source.integrations||{}),logicPro:[...(source.integrations?.logicPro||[]),{kind:'midi-import',importedAt:timestamp,sourceFileName:preview.file.name,sourceProjectId:source.projectId}]}}}else{input={projectName:parser.projectNameFromFile(preview.file.name),songTitle:parser.projectNameFromFile(preview.file.name),bpm:midiData.tempo,timeSignature:`${midiData.timeSignature.numerator}/${midiData.timeSignature.denominator}`,key:null,midiData,importSource:midiData.importSource}}const project=makeProject(input,timestamp);await repository.put(project);state.projects=[project,...state.projects.filter(p=>p.projectId!==project.projectId)];setLastProject(project.projectId);const history={id:uuid(),createdAt:timestamp,fileName:preview.file.name,fileSize:preview.file.size,midiType:preview.summary.midiType,ppq:preview.summary.ppq,trackCount:preview.summary.trackCount,noteCount:preview.summary.noteCount,bpm:preview.summary.initialBpm,timeSignature:preview.summary.initialTimeSignature,projectId:project.projectId,projectName:project.projectName,mode,warningCount:preview.summary.warnings.length,success:true,appVersion:APP_VERSION};try{await repository.putMidiImportHistory?.(history);await loadMidiImportHistory()}catch(error){console.warn('MIDI import history could not be saved',error);history.historyWarning=' 読み込み履歴は保存できませんでした。'}state.midiImportPreview=null;state.midiEditor=null;notice(`${project.projectName}へ安全に取り込み、共通MIDIエディターを開きました。${history.historyWarning||''}`,'success');routeTo(`${MIDI_EDITOR_ROUTE}/${encodeURIComponent(project.projectId)}`);return{ok:true,project,history}}catch(error){console.error('Music Studio MIDI import save failed',error);notice(`取り込み先を保存できませんでした：${error.message} 既存プロジェクトは変更していません。`,'error');return{ok:false,error}}finally{state.midiImporting=false;requestPaint()}}
  function importMidiAsNew(){return saveMidiImport('new')}
  function importMidiAsDuplicate(){return saveMidiImport('duplicate')}
  function editorProject(id){return state.projects.find(project=>project.projectId===id)||null}
  function ensureEditor(id){const core=root.MusicStudioEditor,project=editorProject(id);if(!core||!project)return null;if(!state.midiEditor||state.midiEditor.projectId!==id){state.midiEditor=core.createSession(project);state.partialEditSession=null;resetAddNoteSequence(state.midiEditor)}return state.midiEditor}
  function midiEditorId(route){return route.startsWith(`${MIDI_EDITOR_ROUTE}/`)?decodeURIComponent(route.slice(MIDI_EDITOR_ROUTE.length+1)):''}
  function isMidiEditorRoute(route){return route===`${HOME_ROUTE}/midi-composer`||Boolean(midiEditorId(route))}
  function editorNote(session){return root.MusicStudioEditor.currentTrack(session)?.notes.find(note=>note.id===session.selectedNoteId)||null}
  function noteName(pitch){const names=['C','C♯','D','D♯','E','F','F♯','G','G♯','A','A♯','B'],value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return`${names[value%12]}${Math.floor(value/12)-1}`}
  const GM_DRUM_NAMES={35:'Acoustic Bass Drum',36:'Kick',37:'Side Stick',38:'Snare',39:'Hand Clap',40:'Electric Snare',41:'Low Floor Tom',42:'Closed Hi-Hat',43:'High Floor Tom',44:'Pedal Hi-Hat',45:'Low Tom',46:'Open Hi-Hat',47:'Low-Mid Tom',48:'Hi-Mid Tom',49:'Crash Cymbal 1',50:'High Tom',51:'Ride Cymbal 1',52:'Chinese Cymbal',53:'Ride Bell',54:'Tambourine',55:'Splash Cymbal',56:'Cowbell',57:'Crash Cymbal 2',58:'Vibraslap',59:'Ride Cymbal 2'};
  function drumNoteName(pitch){const value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return GM_DRUM_NAMES[value]||`Note ${value}`}
  function notePitchClass(pitch){const names=['C','C♯','D','D♯','E','F','F♯','G','G♯','A','A♯','B'],value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return names[value%12]}
  function noteOctave(pitch){const value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return Math.floor(value/12)-1}
  function solfegeName(pitch){const names=['ド','ド♯','レ','レ♯','ミ','ファ','ファ♯','ソ','ソ♯','ラ','ラ♯','シ'],value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return names[value%12]}
  function noteLabelMarkup(note,part='melody'){const english=noteName(note.pitch),solfege=solfegeName(note.pitch),drum=part==='drums'?drumNoteName(note.pitch):null;if(drum){const fallback=drum===`Note ${note.pitch}`;return`<span class="music-note-label" aria-hidden="true"><span class="music-note-label-short">${note.pitch}</span><span class="music-note-label-medium">${fallback?drum:`${drum} · ${note.pitch}`}</span><span class="music-note-label-full">${fallback?drum:`${drum} · Note ${note.pitch}`}　V${note.velocity}</span></span>`}if(part==='bass')return`<span class="music-note-label" aria-hidden="true"><span class="music-note-label-short">${english}</span><span class="music-note-label-medium">${english} · Note ${note.pitch}</span><span class="music-note-label-full">${english} · Note ${note.pitch}　V${note.velocity}</span></span>`;return`<span class="music-note-label" aria-hidden="true"><span class="music-note-label-short">${english}</span><span class="music-note-label-medium">${english} / ${solfege}</span><span class="music-note-label-full">${english} / ${solfege}　V${note.velocity}</span></span>`}
  const NOTE_LABEL_RESERVED_WIDTH=22,NOTE_LABEL_FIT_MARGIN=2,NOTE_LABEL_FALLBACK_WIDTHS={short:26,medium:64,full:104};
  function noteLabelMode(width,textWidths=NOTE_LABEL_FALLBACK_WIDTHS){const available=Math.max(0,(Number(width)||0)-NOTE_LABEL_RESERVED_WIDTH),fits=name=>available>=Math.max(0,Number(textWidths?.[name])||NOTE_LABEL_FALLBACK_WIDTHS[name])+NOTE_LABEL_FIT_MARGIN;return fits('full')?'full':fits('medium')?'medium':'short'}
  function noteLabelTextWidths(note){const label=note?.querySelector?.('.music-note-label'),measure=(name,fallback)=>{const span=label?.querySelector?.(`.music-note-label-${name}`),width=Number(span?.scrollWidth)||Number(span?.getBoundingClientRect?.().width)||0;return width>0?Math.ceil(width):fallback};return{short:measure('short',NOTE_LABEL_FALLBACK_WIDTHS.short),medium:measure('medium',NOTE_LABEL_FALLBACK_WIDTHS.medium),full:measure('full',NOTE_LABEL_FALLBACK_WIDTHS.full)}}
  function updateNoteLabelElement(note){if(typeof note?.classList?.toggle!=='function')return'short';const width=Number(note.getBoundingClientRect?.().width)||Number(note.offsetWidth)||0,mode=noteLabelMode(width,noteLabelTextWidths(note));for(const name of['short','medium','full'])note.classList.toggle(`note-label-is-${name}`,name===mode);if(note.dataset)note.dataset.noteLabelMode=mode;return mode}
  function updateNoteLabelLayout(scope=root.document){const notes=scope?.querySelectorAll?.('.music-midi-editor-page .music-piano-roll .music-midi-note')||scope?.querySelectorAll?.('.music-midi-note')||[];for(const note of notes)updateNoteLabelElement(note);return notes.length}
  let noteLabelResizeFrame=null;
  function scheduleNoteLabelLayout(){if(noteLabelResizeFrame!=null)root.cancelAnimationFrame?.(noteLabelResizeFrame);if(root.requestAnimationFrame)noteLabelResizeFrame=root.requestAnimationFrame(()=>{noteLabelResizeFrame=null;updateNoteLabelLayout()});else updateNoteLabelLayout()}
  function pianoPitchY(pitch){const value=Math.max(0,Math.min(127,Math.round(Number(pitch)||0)));return(127-value+.5)*PIANO_ROW_HEIGHT}
  function pianoYToPitch(y){return Math.max(0,Math.min(127,127-Math.floor(Math.max(0,Number(y)||0)/PIANO_ROW_HEIGHT)))}
  function editorSelectedNotes(session){return root.MusicStudioEditor?.selectedNotes?.(session)||[]}
  const SNAP_VALUES=['measure','1/2','1/4','1/8','1/16','1/32'],QUANTIZE_VALUES=['1/4','1/8','1/16','1/32'];
  function editorSnapValue(session=state.midiEditor){const value=session?.view?.snap;return SNAP_VALUES.includes(value)?value:'1/16'}
  function editorSnapEnabled(session=state.midiEditor){return session?.view?.snapEnabled!==false}
  function editorGridTicks(session=state.midiEditor){if(!session)return 1;const value=editorSnapValue(session),ppq=session.midiData.ppq,signature=session.midiData.timeSignature||{numerator:4,denominator:4};if(value==='measure')return Math.max(1,Math.round(ppq*4/signature.denominator*signature.numerator));const denominator=Number(value.split('/')[1])||16;return Math.max(1,Math.round(ppq*4/denominator))}
  function editorSnapTicks(session=state.midiEditor){return editorSnapEnabled(session)?editorGridTicks(session):1}
  function editorLoop(session=state.midiEditor){const editor=session?.midiData?.editor||{},start=Number(editor.loopStart),end=Number(editor.loopEnd),specified=Number.isFinite(start)&&Number.isFinite(end)&&start>=0&&end>start;return{enabled:specified&&editor.loopEnabled===true,specified,start:specified?start:null,end:specified?end:null}}
  function velocityClass(value){return Number(value)<48?'velocity-low':Number(value)<96?'velocity-medium':'velocity-high'}
  function editorTimelineTicks(session){const signature=session.midiData.timeSignature||{numerator:4,denominator:4},measureTicks=session.midiData.ppq*4/signature.denominator*signature.numerator,noteEnd=(session.midiData.tracks||[]).reduce((end,track)=>Math.max(end,...(track.notes||[]).map(note=>note.startTick+note.durationTicks)),0),saved=Number(session.midiData.editor?.measureCount)||4,measureCount=Math.max(4,Math.min(10000,Math.round(saved)),noteEnd?Math.ceil(noteEnd/measureTicks):4);return{measureTicks,measureCount,totalTicks:measureCount*measureTicks}}
  function editorConfiguredTimelineTicks(session){const signature=session.midiData.timeSignature||{numerator:4,denominator:4},measureTicks=session.midiData.ppq*4/signature.denominator*signature.numerator,measureCount=Math.max(4,Math.min(10000,Math.round(Number(session.midiData.editor?.measureCount)||4)));return measureCount*measureTicks}
  function editorVisibleBarRange(session=state.midiEditor,metrics={}){
    if(!session)return{first:1,last:1,total:1,label:'Bar 1–1 / 1'};
    const{measureTicks,measureCount,totalTicks}=editorTimelineTicks(session),scrollWidth=Math.max(1,Number(metrics.scrollWidth)||1),clientWidth=Math.max(1,Math.min(scrollWidth,Number(metrics.clientWidth)||scrollWidth)),maxScroll=Math.max(0,scrollWidth-clientWidth),scrollLeft=Math.max(0,Math.min(maxScroll,Number(metrics.scrollLeft)||0)),leftTick=scrollLeft/scrollWidth*totalTicks,rightTick=Math.min(totalTicks,(scrollLeft+clientWidth)/scrollWidth*totalTicks),first=Math.max(1,Math.min(measureCount,Math.floor(leftTick/measureTicks+1e-9)+1)),last=Math.max(first,Math.min(measureCount,Math.ceil(rightTick/measureTicks-1e-9)));
    return{first,last,total:measureCount,label:`Bar ${first}–${last} / ${measureCount}`};
  }
  function editorMeasureLabelStep(measureCount,zoom){const visibleMeasures=Math.max(1,Number(measureCount)||1)/Math.max(1,Number(zoom)||1),minimumStep=Math.max(1,Math.ceil(visibleMeasures/24));let step=1;while(step<minimumStep)step*=2;return step}
  function editorTimeline(session){
    const core=root.MusicStudioEditor,track=core.currentTrack(session),correctionPreview=session.part==='melody'?session.correctionPreview:null,transposePreview=session.part==='melody'?session.transposePreview:null,noteLengthPreview=session.part==='melody'?session.noteLengthPreview:null,displayNotes=correctionPreview?.viewMode!=='original'&&correctionPreview?.correctedNotes||transposePreview?.transposedNotes||noteLengthPreview?.changedNotes||track.notes,liveNotes=session.part===state.midiInput.recordingPart&&state.midiInput.recording&&!state.midiInput.stopping?state.midiInput.liveNotes:[],timelineNotes=[...displayNotes,...liveNotes],{measureCount,totalTicks}=editorTimelineTicks(session),view=session.view||(session.view={zoom:1,pitchMin:48,pitchMax:72}),selectedIds=new Set(core.selectedIds?.(session)||[]),selectedPitches=new Set(displayNotes.filter(note=>selectedIds.has(note.id)).map(note=>note.pitch));
    const pitches=timelineNotes.map(note=>note.pitch),autoMin=pitches.length?Math.max(0,Math.min(...pitches)-3):48,autoMax=pitches.length?Math.min(127,Math.max(...pitches)+3):72;
    view.pitchMin=Math.max(0,Math.min(view.pitchMin??autoMin,126));view.pitchMax=Math.max(view.pitchMin+1,Math.min(view.pitchMax??autoMax,127));
    const pitchSpan=PIANO_PITCH_COUNT,zoom=Math.max(1,Math.min(30,Number(view.zoom)||1)),snap=editorSnapValue(session),initialScroll=pitchScrollTop(session),initialScrollLeft=Math.max(0,Number(view.pitchScrollLeft)||0),beatsPerMeasure=Math.max(1,Number(session.midiData.timeSignature?.numerator)||4),measurePercent=100/measureCount,beatPercent=measurePercent/beatsPerMeasure,subdivisionPercent=beatPercent/4,microPercent=subdivisionPercent/2,playheadPercent=Math.min(100,session.playheadTick/totalTicks*100),initialBarRange=editorVisibleBarRange(session,{scrollLeft:0,clientWidth:1,scrollWidth:zoom}).label;
    const measureLabelStep=editorMeasureLabelStep(measureCount,zoom),measures=Array.from({length:measureCount},(_,index)=>{const measure=index+1,selected=session.selectedMeasures.includes(measure),locked=session.lockedMeasures.includes(measure),showLabel=index%measureLabelStep===0,label=showLabel?`Bar ${measure}${locked?' 🔒':''}`:'';return`<button class="music-measure ${showLabel?'has-label':'is-unlabeled'} ${selected?'is-selected':''} ${locked?'is-locked':''}" style="left:${index/measureCount*100}%;width:${100/measureCount}%" onclick="MusicStudio.editorToggleMeasure(${measure})" aria-label="Bar ${measure}${locked?'・固定中':''}" aria-pressed="${selected}" title="${locked?'固定中':'クリックして選択'}">${label}</button>`}).join('');
    const notes=displayNotes.map(note=>{const pitchY=pianoPitchY(note.pitch),left=note.startTick/totalTicks*100,width=Math.max(note.durationTicks/totalTicks*100,.45),selected=selectedIds.has(note.id),locked=core.isNoteLocked(note),drum=session.part==='drums'?drumNoteName(note.pitch):null,description=`${drum?`${drum} / `:''}${noteName(note.pitch)} / Velocity ${note.velocity} / Note ${note.pitch} / ${note.startTick} tick${locked?' / Locked（固定）':''}`,classes=`music-midi-note ${velocityClass(note.velocity)} ${selected?'is-selected':''} ${locked?'is-locked':''} ${correctionPreview&&correctionPreview.viewMode!=='original'?'is-correction-preview':''} ${transposePreview?'is-transpose-preview':''} ${noteLengthPreview?'is-note-length-preview':''}`;return`<button class="${classes}" data-note-id="${escapeHtml(note.id)}" data-pitch="${note.pitch}" style="--pitch-y:${pitchY}px;top:var(--pitch-y);left:${left}%;width:${width}%" onpointerdown="MusicStudio.editorStartNoteDrag(event,'${escapeHtml(note.id)}')" aria-pressed="${selected}" aria-label="${escapeHtml(description)}" title="${escapeHtml(description)}">${locked?'<span class="music-note-lock-icon" aria-hidden="true">🔒</span>':''}${noteLabelMarkup(note,session.part)}<span class="music-note-resize" onpointerdown="MusicStudio.editorStartNoteResize(event,'${escapeHtml(note.id)}')" aria-label="ノート長を変更"></span></button>`}).join('');
    const liveNoteMarkup=liveNotes.map(note=>liveRecordingNoteHtml(note,totalTicks)).join('');
    const selectedPitchRows=[...selectedPitches].sort((a,b)=>b-a).map(pitch=>`<span data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px"></span>`).join(''),selectedPitchLayer=`<div class="music-selected-pitch-layer" aria-hidden="true">${selectedPitchRows}</div>`;
    const guideSettings=session.correctionSettings||{key:'C',scale:'Major',guideEnabled:true},guidePitchClasses=session.part==='melody'&&guideSettings.guideEnabled!==false?core.correctionScalePitchClasses(guideSettings.key,guideSettings.scale):[],guideSet=new Set(guidePitchClasses),bassRangeGuide=session.part==='bass'?`<div class="music-bass-range-guide" style="--bass-guide-top:${pianoPitchY(55)}px;--bass-guide-height:${(55-28+1)*PIANO_ROW_HEIGHT}px" aria-hidden="true"><span>Bass guide E1–G3 · Note 28–55</span></div>`:'',scaleGuide=(guidePitchClasses.length?`<div class="music-scale-guide" data-key="${escapeHtml(guideSettings.key)}" data-scale="${escapeHtml(guideSettings.scale)}" aria-hidden="true">${Array.from({length:pitchSpan},(_,index)=>{const pitch=127-index;return guideSet.has(pitch%12)?`<span data-pitch="${pitch}" style="top:${index/pitchSpan*100}%;height:${100/pitchSpan}%"></span>`:''}).join('')}</div>`:'')+bassRangeGuide+selectedPitchLayer;
    const keyPitches=Array.from({length:pitchSpan},(_,index)=>127-index),blackPitchClasses=new Set([1,3,6,8,10]);
    const keyVisuals=keyPitches.map(pitch=>{const isBlack=blackPitchClasses.has(pitch%12);return`<span class="music-piano-key ${isBlack?'is-black':'is-white'}${selectedPitches.has(pitch)?' is-selected-pitch':''}" data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px" aria-hidden="true"></span>`}).join('');
    const pitchHits=keyPitches.map(pitch=>{const drum=session.part==='drums'?drumNoteName(pitch):null;return`<button type="button" class="music-pitch-hit" data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px" onpointerdown="event.preventDefault();MusicStudio.editorPreviewPitchFromKey(event)" onclick="if(event.detail===0)MusicStudio.editorPreviewPitchFromKey(event)" aria-label="${drum?`${drum}、Note ${pitch}`:`${solfegeName(pitch)}／${noteName(pitch)}`}を試聴"></button>`}).join('');
    const pitchLabels=keyPitches.map(pitch=>{const drum=session.part==='drums'?drumNoteName(pitch):null;if(drum)return`<span class="music-pitch-name music-drum-pitch-name" data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px" aria-hidden="true"><span class="music-drum-name">${drum}</span><span class="music-drum-number">${pitch}</span></span>`;return`<span class="music-pitch-name ${blackPitchClasses.has(pitch%12)?'is-black':'is-white'}" data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px" aria-hidden="true"><span class="music-pitch-solfege">${solfegeName(pitch)}</span><span class="music-pitch-separator">／</span><span class="music-pitch-note-name"><span class="music-pitch-class">${notePitchClass(pitch)}</span><span class="music-pitch-octave">${noteOctave(pitch)}</span></span></span>`}).join('');
    const snapOptions=[['measure','1小節'],['1/2','1/2'],['1/4','1/4'],['1/8','1/8'],['1/16','1/16'],['1/32','1/32']].map(([value,label])=>`<option value="${value}" ${snap===value?'selected':''}>${label}</option>`).join('');
    const shortcutHelp=`<details class="music-shortcut-guide"><summary>？ ショートカット</summary><p>Mac用。入力欄では動作しません。iPadは画面ボタンだけでもすべて操作できます。</p><dl><div><dt>再生 / 停止</dt><dd>Space</dd></div><div><dt>先頭へ戻る</dt><dd>Return</dd></div><div><dt>Undo / Redo</dt><dd>⌘Z / ⌘⇧Z</dd></div><div><dt>Copy / Paste</dt><dd>⌘C / ⌘V</dd></div><div><dt>すべて選択 / 削除</dt><dd>⌘A / Delete</dd></div><div><dt>左右移動 / 半音上下</dt><dd>← → / ↑ ↓</dd></div><div><dt>1オクターブ上下</dt><dd>⇧↑ / ⇧↓</dd></div><div><dt>短く / 長く</dt><dd>⌥← / ⌥→</dd></div><div><dt>Zoom</dt><dd>⌘+ / ⌘− / ⌘0</dd></div><div><dt>ノート追加 / 選択解除</dt><dd>N / Esc</dd></div><div><dt>Snap 1/4〜1/32</dt><dd>1 / 2 / 3 / 4</dd></div></dl></details>`;
    const loop=editorLoop(session),loopState=loop.enabled?'is-enabled':'is-disabled',loopLeft=loop.specified?loop.start/totalTicks*100:0,loopWidth=loop.specified?(loop.end-loop.start)/totalTicks*100:0,loopLabel=`Loop ${loop.enabled?'ON':'OFF'}（ループ）`,loopSelection=`<span class="music-loop-length">曲の長さ：${measureCount}小節 · ${loop.specified?'ドラッグで移動・端で調整':'空きレーンをドラッグして作成'}</span>${loop.specified?`<div class="music-loop-selection ${loopState}" style="left:${loopLeft}%;width:${loopWidth}%" data-loop-part="move" data-loop-enabled="${loop.enabled}" onpointerdown="MusicStudio.editorStartLoopRange(event,'move')" aria-label="Loop ${loop.enabled?'ON':'OFF'}：タップで切替、ドラッグで範囲を移動"><button type="button" class="music-loop-handle is-start" data-loop-part="start" onpointerdown="MusicStudio.editorStartLoopRange(event,'start')" aria-label="ループ開始位置を調整"></button><span class="music-loop-selection-label"><span class="music-loop-label-full">${loopLabel}</span><span class="music-loop-label-short">Loop</span></span><button type="button" class="music-loop-handle is-end" data-loop-part="end" onpointerdown="MusicStudio.editorStartLoopRange(event,'end')" aria-label="ループ終了位置を調整"></button></div>`:''}`;
    const rulerMarkup=`<div class="music-loop-ruler ${loopState}" data-loop-enabled="${loop.enabled}" data-total-ticks="${totalTicks}" onpointerdown="MusicStudio.editorStartLoopRange(event)" aria-label="Loop ${loop.enabled?'ON':'OFF'}：ループ範囲をドラッグして指定">${loopSelection}</div><div class="music-measure-row">${measures}<div class="music-time-ruler" data-total-ticks="${totalTicks}" onpointerdown="MusicStudio.editorStartPlayheadMove(event)" title="クリック／タップ／ドラッグで赤い再生ラインを移動" aria-label="時間ルーラー"><span class="music-playhead-handle" style="left:${playheadPercent}%" aria-hidden="true"></span></div></div>`,bassTrackStatus=session.part==='bass'?'<div class="music-bass-track-status" role="status">Bass Track · Channel 2 · GM Program 32 · Guide E1–G3（Note 28–55）</div>':'';
    return`<div class="music-midi-timeline" aria-label="${escapeHtml(track.name)}タイムライン">${bassTrackStatus}<div class="music-piano-viewport" tabindex="0" aria-label="Piano Roll編集領域" data-initial-scroll-top="${initialScroll}" data-initial-scroll-left="${initialScrollLeft}" onscroll="MusicStudio.editorRememberPitchScroll(this)"><div class="music-piano-frame" style="--music-piano-row-height:${PIANO_ROW_HEIGHT}px;--music-piano-roll-height:${PIANO_PITCH_COUNT*PIANO_ROW_HEIGHT}px"><div class="music-piano-corner" aria-hidden="true"></div><div class="music-piano-header-scroll" aria-label="Piano Roll時間軸"><div class="music-piano-header-content" style="width:${zoom*100}%">${rulerMarkup}</div></div><div class="music-pitch-labels"><div class="music-piano-key-layer">${keyVisuals}</div><div class="music-pitch-name-layer">${pitchLabels}</div><div class="music-pitch-hit-layer">${pitchHits}</div></div><div class="music-piano-scroll"><div class="music-piano-content" style="width:${zoom*100}%"><div class="music-piano-roll" data-total-ticks="${totalTicks}" data-pitch-min="0" data-pitch-max="127" onpointerdown="MusicStudio.editorStartMarqueeSelection(event)">${scaleGuide}<div class="music-time-grid${zoom>=4?' is-detailed':''}${zoom>=20?' is-ultra':''}" style="--measure-size:${measurePercent}%;--beat-size:${beatPercent}%;--subdivision-size:${subdivisionPercent}%;--micro-size:${microPercent}%"></div><div class="music-playhead" style="left:${playheadPercent}%" aria-label="現在の再生位置とノート追加位置"></div>${notes||'<p class="music-midi-empty">ノートはまだありません。赤い再生ラインを移動して「ノート追加」を使えます。</p>'}<div class="music-live-note-layer" aria-live="off">${liveNoteMarkup}</div></div></div></div></div></div><div class="music-piano-position"><output class="music-piano-bar-range" aria-live="polite">${initialBarRange}</output><div class="music-piano-scrollbar" tabindex="0" role="scrollbar" aria-label="Piano Roll横スクロール" aria-orientation="horizontal"><div class="music-piano-scrollbar-content" style="width:${zoom*100}%"></div></div></div></div>`;
  }
  function pitchScrollTop(session,viewportHeight=508){const view=session.view||(session.view={zoom:1,pitchMin:48,pitchMax:72});if(Number.isFinite(view.pitchScrollTop))return Math.max(0,view.pitchScrollTop);const headerHeight=root.matchMedia?.('(pointer: coarse)')?.matches?56:PIANO_HEADER_HEIGHT,focus=(Number(view.pitchMin??48)+Number(view.pitchMax??72))/2,totalHeight=headerHeight+PIANO_PITCH_COUNT*PIANO_ROW_HEIGHT;return Math.max(0,Math.min(totalHeight-viewportHeight,headerHeight+(127-focus+.5)*PIANO_ROW_HEIGHT-viewportHeight/2))}
  function fitCorrectionPopover(){
    const menu=root.document?.querySelector?.('.music-correction-menu'),popover=menu?.querySelector?.('.music-correction-popover');
    if(!menu||!popover||!menu.open)return;
    const summaryRect=menu.querySelector?.('summary')?.getBoundingClientRect?.(),viewportHeight=Number(root.innerHeight)||root.document?.documentElement?.clientHeight||900,viewportWidth=Number(root.innerWidth)||1280;
    if(viewportHeight<=0)return;
    if(viewportWidth<=900){
      const top=Math.max(8,(Number(summaryRect?.bottom)||0)+8);
      popover.style.top=`${Math.min(top,viewportHeight-248)}px`;popover.style.right='8px';popover.style.left='8px';popover.style.width='auto';
    }else{
      popover.style.top='calc(100% + 8px)';popover.style.right='0';popover.style.left='auto';popover.style.width='min(720px,calc(100vw - 24px))';
    }
    const top=Number(popover.getBoundingClientRect?.().top)||0;
    popover.style.maxHeight=`${Math.max(240,viewportHeight-top-12)}px`;
    popover.style.overflowY=popover.scrollHeight>popover.clientHeight?'auto':'hidden';
  }
  function restoreEditorPitchScroll(){
    const view=state.midiEditor?.view,viewport=root.document?.querySelector?.('.music-piano-viewport'),pianoScroll=root.document?.querySelector?.('.music-piano-scroll'),headerScroll=root.document?.querySelector?.('.music-piano-header-scroll'),scrollbar=root.document?.querySelector?.('.music-piano-scrollbar');
    if(viewport&&viewport.dataset.scrollReady!=='true'){viewport.scrollTop=Number(viewport.dataset.initialScrollTop)||0;viewport.scrollLeft=0;viewport.dataset.scrollReady='true'}
    if(pianoScroll){if(pianoScroll.dataset.scrollReady!=='true'){pianoScroll.scrollLeft=Math.max(0,Number(view?.pitchScrollLeft)||0);pianoScroll.dataset.scrollReady='true'}syncEditorPianoPosition(pianoScroll);pianoScroll.onscroll=()=>{syncEditorPianoPosition(pianoScroll);editorRememberPitchScroll(pianoScroll,'horizontal')};if(scrollbar)scrollbar.onscroll=()=>{if(Math.abs(pianoScroll.scrollLeft-scrollbar.scrollLeft)>.5){pianoScroll.scrollLeft=scrollbar.scrollLeft;syncEditorPianoPosition(pianoScroll);editorRememberPitchScroll(pianoScroll,'horizontal')}}}
    const menu=root.document?.querySelector?.('.music-correction-menu');
    if(menu&&view){
      if(view.correctionMenuOpen)menu.open=true;
      menu.ontoggle=()=>{view.correctionMenuOpen=menu.open;if(menu.open)root.requestAnimationFrame?.(fitCorrectionPopover)};
      if(menu.open){fitCorrectionPopover();const popover=menu.querySelector?.('.music-correction-popover');if(popover)popover.scrollTop=Number(view.correctionPopoverScrollTop)||0}
    }
    updateNoteLabelLayout();
  }
  function syncEditorPianoPosition(pianoScroll=root.document?.querySelector?.('.music-piano-scroll')){
    if(!pianoScroll)return null;
    const headerScroll=root.document?.querySelector?.('.music-piano-header-scroll'),scrollbar=root.document?.querySelector?.('.music-piano-scrollbar'),rangeOutput=root.document?.querySelector?.('.music-piano-bar-range'),left=Math.max(0,Number(pianoScroll.scrollLeft)||0);
    if(headerScroll)headerScroll.scrollLeft=left;if(scrollbar&&Math.abs(scrollbar.scrollLeft-left)>.5)scrollbar.scrollLeft=left;
    const range=editorVisibleBarRange(state.midiEditor,{scrollLeft:left,clientWidth:Number(pianoScroll.clientWidth)||1,scrollWidth:Number(pianoScroll.scrollWidth)||1});
    if(rangeOutput&&rangeOutput.textContent!==range.label)rangeOutput.textContent=range.label;
    if(scrollbar){scrollbar.setAttribute?.('aria-valuemin','0');scrollbar.setAttribute?.('aria-valuemax',String(Math.max(0,(Number(scrollbar.scrollWidth)||0)-(Number(scrollbar.clientWidth)||0))));scrollbar.setAttribute?.('aria-valuenow',String(Math.round(left)))}
    return range;
  }
  function scheduleEditorPianoPosition(){if(root.requestAnimationFrame)root.requestAnimationFrame(()=>syncEditorPianoPosition());else syncEditorPianoPosition()}
  function rangeTools(session){
    const allLocked=session.selectedMeasures.length>0&&session.selectedMeasures.every(measure=>session.lockedMeasures.includes(measure)),request=session.regenerationRequest;
    return`<div class="music-range-tools"><form onsubmit="MusicStudio.editorSelectMeasureRange(event)"><label>開始小節<input name="from" type="number" min="1" value="${session.selectedMeasures[0]||1}"></label><label>終了小節<input name="to" type="number" min="1" value="${session.selectedMeasures.at(-1)||1}"></label><button class="music-secondary" type="submit">範囲を選択</button></form><button class="music-secondary" onclick="MusicStudio.editorToggleLock()" ${session.selectedMeasures.length?'':'disabled'}>${allLocked?'固定解除':'選択範囲を固定'}</button><button class="music-secondary" onclick="MusicStudio.editorPrepareRegeneration()" ${session.selectedMeasures.length?'':'disabled'}>再生成対象を準備（AI未接続）</button></div><p class="music-lock-status">固定中：${session.lockedMeasures.join(', ')||'なし'} / 選択中：${session.selectedMeasures.join(', ')||'なし'}</p>${request?`<div class="music-regeneration-status" role="status"><b>${escapeHtml(root.MusicStudioEditor.PARTS[session.part].name)}再生成リクエスト準備済み</b><p>対象小節：${request.targetMeasures.join(', ')} / 固定除外：${request.lockedMeasures.join(', ')||'なし'}</p><p>AI生成はまだ接続されていないため、既存ノートは変更していません。</p></div>`:`<p class="music-help">再生成操作では対象情報だけを準備し、既存ノートを変更しません。</p>`}`;
  }
  function partWorkflow(session){
    if(session.part==='melody'){const white=[60,62,64,65,67,69,71,72],black=[61,63,66,68,70];return`<details class="music-part-workflow music-melody-workflow"><summary><b>MIDI入力・演奏補助</b><span>画面鍵盤／Velocity／範囲固定</span></summary><div class="music-melody-workflow-body"><div class="music-keyboard-settings"><label>長さ<select id="melodyInputDuration"><option value="${session.midiData.ppq}">1拍</option><option value="${Math.max(1,Math.round(session.midiData.ppq/2))}">1/2拍</option><option value="${session.midiData.ppq*2}">2拍</option></select></label><label>Velocity<input id="melodyInputVelocity" type="number" min="1" max="127" value="100"></label></div><div class="music-keyboard" role="group" aria-label="メロディ入力鍵盤">${white.map(pitch=>`<button class="music-key is-white" onclick="MusicStudio.editorPianoInput(${pitch})" aria-label="Note ${pitch}">${pitch}</button>`).join('')}${black.map(pitch=>`<button class="music-key is-black" onclick="MusicStudio.editorPianoInput(${pitch})" aria-label="Note ${pitch}">${pitch}</button>`).join('')}</div>${rangeTools(session)}</div></details>`}
    if(session.part==='drums'){const candidate=session.candidates.drums,pads=[36,38,42,46];return`<details class="music-part-workflow music-drums-workflow"><summary><b>Drum Pad・パターン候補</b><span>GM Note 36 / 38 / 42 / 46</span></summary><div class="music-drums-workflow-body"><div class="music-section-heading"><div><p class="music-kicker">Drums workflow</p><h2 id="partWorkflowTitle">ドラム制作</h2></div><p>GM MIDIノート番号を保ったまま、パターン候補と共通エディターを同じDrumsトラックへ接続します。</p></div><div class="music-generation-actions"><button class="music-primary" onclick="MusicStudio.editorGenerateCandidate('basic')">基本パターン候補</button><button class="music-secondary" onclick="MusicStudio.editorGenerateCandidate('alternate')">別パターン候補</button>${candidate?`<button class="music-secondary" onclick="MusicStudio.editorApplyCandidate()">「${escapeHtml(candidate.label)}」を採用（${candidate.notes.length}ノート）</button>`:''}</div><div class="music-drum-pads" role="group" aria-label="ドラム入力">${pads.map(pitch=>{const label=drumNoteName(pitch);return`<button class="music-secondary" onclick="MusicStudio.editorDrumInput(${pitch})" aria-label="${label}、GM Note ${pitch}を追加"><span>${label}</span><small>GM Note ${pitch}</small></button>`}).join('')}</div>${candidate?'<p class="music-help">候補を確認中です。「採用」を押すまで現在のDrumsノートは変更されません。</p>':'<p class="music-help">候補生成は端末内の簡易パターンです。高度なAI生成は未接続です。</p>'}${rangeTools(session)}</div></details>`}
    const candidate=session.candidates.bass,melodyCount=session.midiData.tracks.find(track=>track.part==='melody')?.notes.length||0,chordCount=session.projectContext?.chordProgressions?.length||0;
    return`<details class="music-part-workflow music-bass-workflow"><summary><b>Bass候補・参照情報</b><span>Channel 2 · GM Program 32</span></summary><div class="music-bass-workflow-body"><div class="music-section-heading"><div><p class="music-kicker">Bass workflow</p><h2 id="partWorkflowTitle">ベース制作</h2></div><p>Melodyとコード情報を参照できる候補生成土台を、同じBassトラックへ接続します。</p></div><dl class="music-context-summary"><div><dt>Melody参照</dt><dd>${melodyCount}ノート</dd></div><div><dt>コード参照</dt><dd>${chordCount}件</dd></div></dl><div class="music-generation-actions"><button class="music-primary" onclick="MusicStudio.editorGenerateCandidate('root')">ルート基礎案</button><button class="music-secondary" onclick="MusicStudio.editorGenerateCandidate('alternate')">オクターブ別案</button>${candidate?`<button class="music-secondary" onclick="MusicStudio.editorApplyCandidate()">「${escapeHtml(candidate.label)}」を採用（${candidate.notes.length}ノート）</button>`:''}</div>${candidate?'<p class="music-help">候補を確認中です。「採用」を押すまで現在のBassノートは変更されません。</p>':'<p class="music-help">現段階は参照情報を渡す簡易候補です。高度なAI生成は未接続です。</p>'}${rangeTools(session)}</div></details>`;
  }
  function midiTransferTools(project,session){const current=midiExportSummary(project,session.part),all=midiExportSummary(project,'all');return`<section class="music-midi-transfer" aria-labelledby="midiTransferTitle"><div class="music-section-heading"><div><p class="music-kicker">MIDI round-trip</p><h2 id="midiTransferTitle">読み込み・書き出し・Logic Pro</h2></div><p>MIDIを安全な新規／複製プロジェクトへ取り込み、編集後はパート別または3パート統合で書き出します。</p></div><div class="music-transfer-actions"><label class="music-file-action" for="editorMidiImport">MIDIを読み込む<input id="editorMidiImport" type="file" accept="audio/midi,audio/x-midi,.mid,.midi" onchange="MusicStudio.inspectMidiFile(this.files[0]);this.value=''" ${state.midiImporting?'disabled':''}></label><button class="music-secondary" onclick="MusicStudio.performMidiExport('${session.part}','${escapeHtml(project.projectId)}')" ${current.validation.ok&&!state.midiExporting?'':'disabled'}>${escapeHtml(current.label)} MIDIを書き出す</button><button class="music-primary" onclick="MusicStudio.performMidiExport('all','${escapeHtml(project.projectId)}')" ${all.validation.ok&&!state.midiExporting?'':'disabled'}>All MIDIを書き出す</button><button class="music-secondary" onclick="MusicStudio.openLogicPro('${escapeHtml(project.projectId)}')">Logic Pro連携を開く</button></div><p class="music-help">予定ファイル名：${escapeHtml(current.filename)} / ${escapeHtml(all.filename)}</p><div id="logicMidiResult" class="music-file-result" aria-live="polite">${state.midiImporting?'解析中です…':'読み込むMIDIを選択してください。'}</div>${importPreviewHtml()}</section>`}
  function melodyCorrectionBasePopover(session){
    const preview=session.correctionPreview,settings=session.correctionSettings||(session.correctionSettings={key:'C',scale:'Major',guideEnabled:true}),options={...(preview?.options||{quantize:'1/16',strength:100,swing:0,target:'all',measureFrom:session.selectedMeasures[0]||1,measureTo:session.selectedMeasures.at(-1)||session.selectedMeasures[0]||1}),key:settings.key,scale:settings.scale},keys=['C','C#','D','D#','E','F','F#','G','G#','A','A#','B'],scales=['Major','Minor','Pentatonic','Chromatic'],quantize=['OFF','1/4','1/8','1/16','1/32'],checked=value=>options.target===value?'checked':'',selected=(value,current)=>value===current?'selected':'',mode=preview?.viewMode==='original'?'Original':'Corrected';
    return`<details class="music-editor-menu music-correction-menu" ${preview?'open':''}><summary>Melody Correction（メロディ補正）</summary><div class="music-editor-popover music-correction-popover"><form id="melodyCorrectionForm" onsubmit="event.preventDefault()"><div class="music-correction-heading"><div><b>Correction</b><small>基本的なキー／スケール補正とタイミング補正</small></div><span>${preview?`Preview: ${mode}`:'Not previewing'}</span></div><div class="music-correction-actions"><button type="button" class="music-primary" onclick="MusicStudio.editorPreviewCorrection()">Correct</button><button type="button" class="music-secondary" onclick="MusicStudio.editorToggleCorrectionPreview()">Preview</button></div><div class="music-correction-grid"><label>Key<select name="key" onchange="MusicStudio.editorUpdateCorrectionGuide(this.form)">${keys.map(value=>`<option ${selected(value,options.key)}>${value}</option>`).join('')}</select></label><label>Scale<select name="scale" onchange="MusicStudio.editorUpdateCorrectionGuide(this.form)">${scales.map(value=>`<option ${selected(value,options.scale)}>${value}</option>`).join('')}</select></label><label class="music-scale-guide-toggle"><input name="guideEnabled" type="checkbox" ${settings.guideEnabled!==false?'checked':''} onchange="MusicStudio.editorUpdateCorrectionGuide(this.form)"> スケールガイド表示</label><label>Quantize<select name="quantize">${quantize.map(value=>`<option ${selected(value,options.quantize)}>${value}</option>`).join('')}</select></label><label>Strength <output>${options.strength}%</output><input name="strength" type="range" min="0" max="100" value="${options.strength}" oninput="this.previousElementSibling.value=this.value+'%'"></label><label>Swing <output>${options.swing}%</output><input name="swing" type="range" min="0" max="100" value="${options.swing}" oninput="this.previousElementSibling.value=this.value+'%'"></label></div><fieldset class="music-correction-target"><legend>補正対象</legend><label><input type="radio" name="target" value="all" ${checked('all')}> 全ノート</label><label><input type="radio" name="target" value="selected" ${checked('selected')}> 選択ノートのみ</label><label><input type="radio" name="target" value="measures" ${checked('measures')}> 小節範囲指定</label><div><label>開始<input name="measureFrom" type="number" min="1" value="${options.measureFrom}"></label><label>終了<input name="measureTo" type="number" min="1" value="${options.measureTo}"></label></div></fieldset>${preview?`<div class="music-correction-preview" role="status"><div><b>Original</b><span>${preview.originalNotes.length} notes</span></div><div><b>Corrected</b><span>${preview.correctedNotes.length} notes · 対象 ${preview.targetNoteIds?.length||0}</span></div><p>Preview中の表示だけを切り替えています。ApplyまではMelodyデータを変更しません。</p></div>`:''}<div class="music-correction-actions"><button type="button" class="music-primary" onclick="MusicStudio.editorApplyCorrection()" ${preview?'':'disabled'}>Apply <span class="music-button-note">（適用）</span></button><button type="button" class="music-secondary" onclick="MusicStudio.editorCancelCorrection()" ${preview?'':'disabled'}>Cancel</button></div><div class="music-correction-future" aria-label="今回は未実装"><span>AIメロディ生成：未実装</span><span>コード追従解析：未実装</span><span>キー一括移調：未実装</span><span>ノート長一括変更：未実装</span></div></form></div></details>`;
  }
  function melodyTransposeControls(session){
    const core=root.MusicStudioEditor,preview=session.transposePreview,settings=session.transposeSettings||(session.transposeSettings={fromKey:session.correctionSettings?.key||'C',toKey:session.correctionSettings?.key||'C',target:'all',measureFrom:1,measureTo:1,direction:'shortest'}),keys=core.correctionKeys(),selected=(value,current)=>value===current?'selected':'',checked=value=>value===settings.target?'checked':'',directionChecked=value=>value===settings.direction?'checked':'',semitones=core.transposeSemitones(settings.fromKey,settings.toKey,settings.direction),amount=`${semitones>=0?'+':''}${semitones}半音`;
    return`<section class="music-transpose-panel" id="melodyTransposePanel" aria-labelledby="melodyTransposeTitle" onchange="MusicStudio.editorUpdateTransposeSettings(this)"><div class="music-correction-heading"><div><b id="melodyTransposeTitle">キー一括移調</b><small>音程関係・リズム・長さ・Velocityを保って移動</small></div><output class="music-transpose-amount">${settings.fromKey} → ${settings.toKey}：${amount}</output></div><div class="music-transpose-grid"><label>移調元キー<select name="transposeFromKey">${keys.map(key=>`<option ${selected(key,settings.fromKey)}>${key}</option>`).join('')}</select></label><label>移調先キー<select name="transposeToKey">${keys.map(key=>`<option ${selected(key,settings.toKey)}>${key}</option>`).join('')}</select></label></div><fieldset class="music-correction-target"><legend>対象</legend><label><input type="radio" name="transposeTarget" value="all" ${checked('all')}> 全ノート</label><label><input type="radio" name="transposeTarget" value="selected" ${checked('selected')}> 選択ノートのみ</label><label><input type="radio" name="transposeTarget" value="measures" ${checked('measures')}> 小節範囲</label><div><label>開始小節<input name="transposeMeasureFrom" type="number" min="1" value="${settings.measureFrom}" ${settings.target==='measures'?'':'disabled'}></label><label>終了小節<input name="transposeMeasureTo" type="number" min="1" value="${settings.measureTo}" ${settings.target==='measures'?'':'disabled'}></label></div></fieldset><fieldset class="music-transpose-direction"><legend>移調方向</legend><label><input type="radio" name="transposeDirection" value="shortest" ${directionChecked('shortest')}> 最短</label><label><input type="radio" name="transposeDirection" value="up" ${directionChecked('up')}> 上方向</label><label><input type="radio" name="transposeDirection" value="down" ${directionChecked('down')}> 下方向</label></fieldset>${preview?`<div class="music-transpose-preview" role="status"><b>移調Preview</b><span>${preview.targetNoteIds.length}ノート · ${preview.semitones>=0?'+':''}${preview.semitones}半音</span><p>Preview表示のみです。適用するまでMIDIデータは変更しません。</p></div>`:''}<div class="music-correction-actions"><button type="button" class="music-secondary" onclick="MusicStudio.editorPreviewTranspose()">プレビュー</button><button type="button" class="music-primary" onclick="MusicStudio.editorApplyTranspose()" ${preview?'':'disabled'}>適用</button><button type="button" class="music-secondary" onclick="MusicStudio.editorCancelTranspose()" ${preview?'':'disabled'}>キャンセル</button></div></section>`;
  }
  function melodyNoteLengthControls(session){
    const preview=session.noteLengthPreview,settings=session.noteLengthSettings||(session.noteLengthSettings={target:'all',measureFrom:1,measureTo:1,length:'current'}),checked=value=>value===settings.target?'checked':'',selected=value=>value===settings.length?'selected':'',lengths=[['current','現在のまま'],['1/1','1/1'],['1/2','1/2'],['1/4','1/4'],['1/8','1/8'],['1/16','1/16'],['1/32','1/32'],['dotted-1/4','付点1/4'],['dotted-1/8','付点1/8'],['dotted-1/16','付点1/16'],['triplet-1/4','三連1/4'],['triplet-1/8','三連1/8'],['triplet-1/16','三連1/16']];
    return`<section class="music-note-length-panel" id="melodyNoteLengthPanel" aria-labelledby="melodyNoteLengthTitle" onchange="MusicStudio.editorUpdateNoteLengthSettings(this)"><div class="music-correction-heading"><div><b id="melodyNoteLengthTitle">ノート長一括変更</b><small>Pitch・開始位置・Velocityを保って長さだけ変更</small></div></div><label class="music-note-length-value">長さ<select name="noteLengthValue">${lengths.map(([value,label])=>`<option value="${value}" ${selected(value)}>${label}</option>`).join('')}</select></label><fieldset class="music-correction-target"><legend>対象</legend><label><input type="radio" name="noteLengthTarget" value="all" ${checked('all')}> 全ノート</label><label><input type="radio" name="noteLengthTarget" value="selected" ${checked('selected')}> 選択ノートのみ</label><label><input type="radio" name="noteLengthTarget" value="measures" ${checked('measures')}> 小節範囲</label><div><label>開始小節<input name="noteLengthMeasureFrom" type="number" min="1" value="${settings.measureFrom}" ${settings.target==='measures'?'':'disabled'}></label><label>終了小節<input name="noteLengthMeasureTo" type="number" min="1" value="${settings.measureTo}" ${settings.target==='measures'?'':'disabled'}></label></div></fieldset>${preview?`<div class="music-note-length-preview" role="status"><b>Note Length Preview</b><span>${preview.targetNoteIds.length}ノート · ${lengths.find(([value])=>value===preview.options.length)?.[1]||preview.options.length}</span><p>Preview表示のみです。重なりは自動調整せず、ApplyまでMIDIデータは変更しません。</p></div>`:''}<div class="music-correction-actions"><button type="button" class="music-secondary" onclick="MusicStudio.editorPreviewNoteLength()">Preview</button><button type="button" class="music-primary" onclick="MusicStudio.editorApplyNoteLength()" ${preview?'':'disabled'}>Apply</button><button type="button" class="music-secondary" onclick="MusicStudio.editorCancelNoteLength()" ${preview?'':'disabled'}>Cancel</button></div></section>`;
  }
  function melodyCorrectionPopover(session){
    const future='<div class="music-correction-future" aria-label="今回は未実装"><span>AIメロディ生成：未実装</span><span>コード追従解析：未実装</span><span>キー一括移調：未実装</span><span>ノート長一括変更：未実装</span></div>';
    const controls=`<div class="music-correction-batch-grid">${melodyTransposeControls(session)}${melodyNoteLengthControls(session)}</div><div class="music-correction-future" aria-label="今回は未実装"><span>AIメロディ生成：未実装</span><span>コード追従解析：未実装</span></div>`;
    let html=melodyCorrectionBasePopover(session).replace(future,controls).replace('<div class="music-editor-popover music-correction-popover">','<div class="music-editor-popover music-correction-popover" aria-label="Melody Correctionパネル"><button type="button" class="music-secondary music-correction-panel-close" onclick="MusicStudio.editorCloseCorrectionPanel()" aria-label="Melody Correctionを閉じる">× 閉じる</button>');
    if(session.transposePreview||session.noteLengthPreview)html=html.replace('class="music-editor-menu music-correction-menu" >','class="music-editor-menu music-correction-menu" open>');
    return localizedEditorButtons(html);
  }
  function localizedEditorButtons(html){
    const replacements=[
      ['>Correct</button>','>Correct（補正）</button>'],['>Preview</button>','>Preview（プレビュー）</button>'],['>Apply</button>','>Apply（適用）</button>'],['>Cancel</button>','>Cancel（キャンセル）</button>'],
      ['>プレビュー</button>','>Preview（プレビュー）</button>'],['>適用</button>','>Apply（適用）</button>'],['>キャンセル</button>','>Cancel（キャンセル）</button>'],
      ['>× 閉じる</button>','>× Close（閉じる）</button>'],['>選択：','>Select（選択）：'],['>ノート追加</button>','>Add Note（ノート追加）</button>'],
      ['>消しゴム</button>','>Eraser（消しゴム）</button>'],['>コピー</button>','>Copy（コピー）</button>'],['>貼り付け</button>','>Paste（貼り付け）</button>'],
      ['>複製</button>','>Duplicate（複製）</button>'],['>全選択</button>','>Select All（全選択）</button>'],['>長さを揃える</button>','>Match Length（長さを揃える）</button>'],
      ['>Velocityを揃える</button>','>Match Velocity（Velocityを揃える）</button>'],['>− ズーム</button>','>− Zoom（ズーム）</button>'],
      ['>＋ ズーム</button>','>＋ Zoom（ズーム）</button>'],['>音域を表示</button>','>Fit Range（音域を表示）</button>'],['>＋ 小節を追加</button>','>＋ Add Measure（小節を追加）</button>'],
      ['><span class="music-record-dot" aria-hidden="true">●</span> 録音</button>','><span class="music-record-dot" aria-hidden="true">●</span> Record（録音）</button>'],['>▶ 再生</button>','>▶ Play（再生）</button>'],['>■ 停止</button>','>■ Stop（停止）</button>'],
      ['>範囲を選択</button>','>Select Range（範囲を選択）</button>'],['>選択範囲を固定</button>','>Lock Range（選択範囲を固定）</button>'],
      ['>固定解除</button>','>Unlock Range（固定解除）</button>'],['>再生成対象を準備（AI未接続）</button>','>Prepare Regeneration（再生成準備・AI未接続）</button>']
    ];
    for(const [before,after] of replacements)html=html.replaceAll(before,after);
    return html;
  }
  function midiConnectionStatus(){
    const midi=state.midiInput,adapter=root.MusicStudioMidiInput,support=adapter?.supportInfo?.(root.navigator)||{webMidi:adapter?.isSupported?.(root.navigator)===true,safariLike:false};
    const unavailable=support.safariLike?`<div class="music-midi-browser-guidance" role="status"><b>SafariではMIDI入力を利用できません</b><small>Mac Chromeで開いてください</small></div>`:`<div class="music-midi-browser-guidance" role="status"><b>この環境ではWeb MIDI APIを利用できません</b><small>対応ブラウザをHTTPSまたはlocalhostで開いてください</small></div>`;
    const content=!support.webMidi?unavailable:(()=>{const inputOptions=midi.inputs.length?midi.inputs.map(input=>`<option value="${escapeHtml(input.id)}" ${midi.selectedId===input.id?'selected':''}>${escapeHtml(input.name||input.manufacturer||'MIDI Input')}</option>`).join(''):'<option value="" selected>利用可能なMIDI機器がありません</option>',targetOptions=[['melody','Melody'],['drums','Drums'],['bass','Bass']].map(([value,label])=>`<option value="${value}" ${midi.targetPart===value?'selected':''}>${label}</option>`).join(''),targetLocked=midi.starting||midi.countingIn||midi.recording||midi.stopping;return`<label class="music-midi-device-select">MIDI機器選択<select onchange="MusicStudio.editorSelectMidiInput(this.value)" aria-label="MIDI機器選択">${inputOptions}<option value="__rescan__">↻ 再検出</option></select></label><label class="music-midi-target-select">Input To<select onchange="MusicStudio.editorSelectMidiTarget(this.value)" aria-label="Input To" ${targetLocked?'disabled':''}>${targetOptions}</select></label><p class="music-midi-connection-state"><b>接続状態：</b>${escapeHtml(midi.status||`${midi.inputs.length}台のMIDI入力を検出しました。`)}</p>`})();
    return`<details class="music-editor-menu music-midi-input-menu"><summary aria-label="MIDI入力">MIDI入力</summary><div class="music-editor-popover music-midi-input-popover"><p class="music-inline-guide">MIDIキーボードを選択して演奏・録音します。</p>${content}</div></details>`;
  }
  function editorTopBar(project,session){
    const current=midiExportSummary(project,session.part),all=midiExportSummary(project,'all'),melody=session.part==='melody',correction=melody?melodyCorrectionPopover(session):'',shortcuts=`<div class="music-editor-popover music-shortcuts-popover" aria-label="Shortcuts（ショートカット／操作ガイド）"><header><b>？ Shortcuts</b><span>ショートカット／操作ガイド</span></header><section><h3>基本操作</h3><dl><div><dt>Space</dt><dd>再生／停止</dd></div><div><dt>Enter／Return</dt><dd>Go to Start（先頭へ戻る）</dd></div><div><dt>⌘Z</dt><dd>Undo（元に戻す）</dd></div><div><dt>⇧⌘Z</dt><dd>Redo（やり直す）</dd></div></dl></section><section><h3>編集</h3><dl><div><dt>⌘C</dt><dd>Copy（コピー）</dd></div><div><dt>⌘V</dt><dd>Paste（貼り付け）</dd></div><div><dt>⌘A</dt><dd>Select All（全選択）</dd></div><div><dt>N</dt><dd>Add Note（ノート追加）</dd></div></dl></section><section><h3>ノート操作</h3><dl><div><dt>ドラッグ</dt><dd>移動／右端で長さ変更</dd></div><div><dt>空白ドラッグ</dt><dd>範囲選択</dd></div><div><dt>Shift＋クリック</dt><dd>選択追加</dd></div><div><dt>Command＋クリック</dt><dd>選択切替</dd></div><div><dt>音名をタップ</dt><dd>その音を試聴</dd></div><div><dt>赤い再生ライン</dt><dd>再生・ノート追加位置</dd></div></dl><p>赤い再生ライン：再生・ノート追加位置。ルーラーや空き位置で移動。</p></section><div class="music-shortcuts-devices"><section><h3>Mac</h3><p>上記のキーボード操作を使用できます。入力欄では動作しません。</p></section><section><h3>iPad</h3><p>キーボード操作は使わず、画面ボタンだけでもすべて操作できます。</p></section></div></div>`;
    return`<div class="music-editor-topbar" aria-label="エディター上部メニュー"><details class="music-editor-menu"><summary title="Project（プロジェクト情報）" aria-label="Project（プロジェクト情報）">Project（プロジェクト情報）</summary><div class="music-editor-popover"><b>${escapeHtml(project.projectName)}</b><p>PPQ ${session.midiData.ppq} / ${signatureLabel(session.midiData.timeSignature)}</p><button class="music-secondary" onclick="MusicStudio.openProject('${escapeHtml(project.projectId)}')">Open Project（プロジェクトを開く）</button></div></details><details class="music-editor-menu"><summary title="Shortcuts（ショートカット）" aria-label="Shortcuts（ショートカット）">Shortcuts（ショートカット）</summary>${shortcuts}</details><details class="music-editor-menu"><summary title="Import / Export（読み込み／書き出し）" aria-label="Import / Export（読み込み／書き出し）">Import / Export（読み込み／書き出し）</summary><div class="music-editor-popover music-editor-transfer"><label class="music-file-action" for="editorMidiImport">Import MIDI（MIDIを読み込む）<input id="editorMidiImport" type="file" accept="audio/midi,audio/x-midi,.mid,.midi" onchange="MusicStudio.inspectMidiFile(this.files[0]);this.value=''" ${state.midiImporting?'disabled':''}></label><button class="music-secondary" onclick="MusicStudio.performMidiExport('${session.part}','${escapeHtml(project.projectId)}')" ${current.validation.ok&&!state.midiExporting?'':'disabled'}>Export ${escapeHtml(current.label)} MIDI（書き出す）</button><button class="music-primary" onclick="MusicStudio.performMidiExport('all','${escapeHtml(project.projectId)}')" ${all.validation.ok&&!state.midiExporting?'':'disabled'}>Export All MIDI（全て書き出す）</button><button class="music-secondary" onclick="MusicStudio.openLogicPro('${escapeHtml(project.projectId)}')">Logic Pro Integration（連携）</button><div id="logicMidiResult" class="music-file-result" aria-live="polite">${state.midiImporting?'解析中です…':'読み込むMIDIを選択してください。'}</div>${importPreviewHtml()}</div></details>${melody?correction:''}<button class="music-primary music-editor-save" onclick="MusicStudio.saveMidiEditor()" ${session.dirty?'':'disabled'}>${session.dirty?'Save（保存）':'Saved（保存済み）'}</button>${midiConnectionStatus()}</div>`;
  }
  function editorTransportSettings(session=state.midiEditor){const transport=session?.midiData?.editor?.transport||{};return{countInEnabled:transport.countInEnabled!==false,metronomeEnabled:transport.metronomeEnabled!==false}}
  function countInBeatDisplay(session){const beats=Math.max(1,Number(session?.midiData?.timeSignature?.numerator)||4),active=state.midiInput.countingIn,current=state.midiInput.countInBeat;return`<div class="music-count-in-display ${active?'is-counting':''}" role="status" aria-live="polite" aria-label="${active?`カウントイン ${current}拍目`:'カウントイン待機'}"><b>Count-in</b>${Array.from({length:beats},(_,index)=>`<span class="${active&&current===index+1?'is-current':''}">${index+1}</span>`).join('')}</div>`}
  function partialEditMessage(reason){return reason==='stale-track'?'Trackが変更されています。Partial Editを開始し直してください。':reason==='stale-range'?'Edit Rangeが変更されています。Partial Editを開始し直してください。':reason==='id-conflict'?'Note IDが競合しています。Previewを作り直してください。':reason==='already-applied'?'このPartial EditはすでにApply済みです。':reason==='cancelled'?'このPartial EditはCancel済みです。':reason==='stale-project'?'NoteまたはLockが変更されています。Previewを作り直してください。':'現在の状態ではPartial Editを実行できません。'}
  function resetPartialEditProviderRun(){const runtime=state.partialEditProvider;runtime.status='idle';runtime.errorCode='';runtime.runId++}
  function editorStartPartialEdit(){const core=root.MusicStudioEditor,session=state.midiEditor;if(!core||!session)return false;try{const request=core.createPartialEditRequest(session);resetPartialEditProviderRun();state.partialEditSession=core.createPartialEditSession(request);notice(`Partial Editを開始しました。${request.part} / Measure ${request.range.startMeasure}–${request.range.endMeasure}`,'success');repaintEditor();return state.partialEditSession}catch(error){notice(`Partial Editを開始できません：${error.message}`,'error');return false}}
  function editorPreviewPartialEditPitchUp(){const core=root.MusicStudioEditor,session=state.midiEditor,partial=state.partialEditSession;if(!core||!session||!partial)return notice('先にPartial Editを開始してください。','info');if(!['created','result-ready'].includes(partial.status))return notice(partialEditMessage(partial.status==='applied'?'already-applied':partial.status),'info');const selectedIds=new Set(core.selectedIds(session)),targetId=partial.request.targetNoteIds.find(noteId=>selectedIds.has(noteId)),source=partial.request.notes.find(note=>note.id===targetId);if(!source)return notice('Edit Range内のUnlocked Noteを1つ選択してください。','info');try{const result=core.createPartialEditResult(partial.request,{updates:[{...clone(source),pitch:source.pitch+1}]});const attachedResult=core.attachPartialEditResult(partial,result);if(!attachedResult.attached)return notice(partialEditMessage(attachedResult.reason),'error');const preview=core.createPartialEditPreview(partial.request,partial.result),attachedPreview=core.attachPartialEditPreview(partial,preview);if(!attachedPreview.attached)return notice(partialEditMessage(attachedPreview.reason),'error');notice('Pitch +1のPreviewを作成しました。内容を確認してApplyしてください。','success');repaintEditor();return partial.preview}catch(error){notice(`Previewを作成できません：${error.message}`,'error');return false}}
  function partialEditProviderErrorMessage(code){return code==='timeout'?'Providerへの接続がtimeoutしました。Projectは変更していません。':code==='authentication-failure'?'Providerの認証に失敗しました。runtime API Keyを確認してください。':code==='rate-limit'?'Providerの利用上限に達しました。時間をおいて再実行してください。':code==='provider-server-error'?'Provider側で一時的な問題が発生しました。Projectは変更していません。':code==='request-invalid'?'Providerがrequestを受け付けませんでした。':code==='malformed-response'||code==='invalid-output'?'Provider responseを安全に検証できませんでした。Projectは変更していません。':'Provider実行に失敗しました。Projectは変更していません。'}
  async function editorRunPartialEditProvider(){
    const core=root.MusicStudioEditor,session=state.midiEditor,partial=state.partialEditSession,form=root.document?.querySelector?.('#partialEditProviderForm'),runtime=state.partialEditProvider;
    if(!core||!session||!partial||!form)return false;
    if(runtime.status==='loading')return{ok:false,reason:'loading'};
    if(!['created','result-ready'].includes(partial.status)){notice(partialEditMessage(partial.status),'error');return false}
    const provider=String(form.elements?.provider?.value||''),model=String(form.elements?.model?.value||'').trim(),apiKey=String(form.elements?.apiKey?.value||''),intent=String(form.elements?.intent?.value||''),text=String(form.elements?.instruction?.value||'').trim();
    if(form.elements?.apiKey)form.elements.apiKey.value='';
    runtime.provider=provider;runtime.model=model;runtime.intent=intent;runtime.text=text;runtime.errorCode='';const runId=++runtime.runId;runtime.status='loading';repaintEditor();
    try{
      const aiInput=core.createPartialEditAIInput(partial.request,{context:{tempo:session.midiData.tempo,timeSignature:session.midiData.timeSignature}}),instruction=core.createPartialEditInstruction({intent,...(text?{text}:{})}),context=core.createPartialEditPromptContext(partial.request,aiInput,instruction),payload=core.createPartialEditProviderPayload(provider,context),config=core.createPartialEditProviderConfig(provider,{apiKey,model}),execution=await core.executePartialEditProviderRequest(provider,payload,{config,apiKey});
      if(runtime.runId!==runId||state.partialEditSession!==partial)return{ok:false,reason:'stale-response'};
      if(execution.status!=='success'){const code=execution.error?.category||execution.error?.code||'provider-error';runtime.status='error';runtime.errorCode=code;notice(partialEditProviderErrorMessage(code),'error');repaintEditor();return{ok:false,error:clone(execution.error)}}
      const output=core.extractPartialEditProviderOutput(execution),proposal=core.parsePartialEditAIOutput(partial.request,output),result=core.createPartialEditResult(partial.request,proposal),preview=core.createPartialEditPreview(partial.request,result),staleCheck=core.applyPartialEditPreview(core.clone(session),partial.request,result,preview);
      if(!staleCheck.applied){runtime.status='error';runtime.errorCode=staleCheck.reason;notice(partialEditMessage(staleCheck.reason),'error');repaintEditor();return{ok:false,reason:staleCheck.reason}}
      const attachedResult=core.attachPartialEditResult(partial,result);if(!attachedResult.attached)throw Error('Partial Edit result could not be attached.');const attachedPreview=core.attachPartialEditPreview(partial,preview);if(!attachedPreview.attached)throw Error('Partial Edit preview could not be attached.');runtime.status='success';notice('Provider responseからPreviewを作成しました。Apply前はProjectを変更していません。','success');repaintEditor();return{ok:true,provider,model,preview:core.clone(partial.preview)}
    }catch(error){if(runtime.runId!==runId||state.partialEditSession!==partial)return{ok:false,reason:'stale-response'};const code=error?.code==='invalid-output'?'invalid-output':'provider-error';runtime.status='error';runtime.errorCode=code;notice(partialEditProviderErrorMessage(code),'error');repaintEditor();return{ok:false,reason:code}}
  }
  function editorApplyPartialEdit(){const core=root.MusicStudioEditor,session=state.midiEditor,partial=state.partialEditSession;if(!core||!session||!partial)return false;const result=core.applyPartialEditSession(session,partial);if(!result.applied){notice(partialEditMessage(result.reason),'error');repaintEditor();return result}resetPartialEditProviderRun();scheduleMidiEditorSave();notice(`Partial EditをApplyしました。Updated ${result.summary.updated} / Added ${result.summary.added} / Deleted ${result.summary.deleted}`,'success');repaintEditor();return result}
  function editorCancelPartialEdit(){const core=root.MusicStudioEditor,partial=state.partialEditSession;if(!core||!partial)return false;resetPartialEditProviderRun();const result=core.cancelPartialEditSession(partial);if(!result.cancelled){notice(partialEditMessage(result.reason),'info');return result}notice('Partial EditをCancelしました。Projectは変更していません。','info');repaintEditor();return result}
  function partialEditControls(){
    const partial=state.partialEditSession,status=partial?.status||'idle',runtime=state.partialEditProvider,summary=partial?.preview?.summary||{updated:0,added:0,deleted:0,unchanged:0},active=['created','result-ready','preview-ready'].includes(status),canPreview=['created','result-ready'].includes(status)&&runtime.status!=='loading',canApply=status==='preview-ready'&&runtime.status!=='loading',range=partial?.request?.range,context=partial?`${partial.request.part} / Measure ${range.startMeasure}–${range.endMeasure}`:'現在のTrackとEdit Rangeを使用',providerStatus=runtime.status==='loading'?'Provider実行中…':runtime.status==='success'?'Previewを作成しました':runtime.status==='error'?partialEditProviderErrorMessage(runtime.errorCode):'runtime credentialは保存されません',intentOptions=[['simpler','Simpler'],['brighter','Brighter'],['darker','Darker'],['more-energy','More energy'],['less-energy','Less energy'],['smoother','Smoother']].map(([value,label])=>`<option value="${value}" ${runtime.intent===value?'selected':''}>${label}</option>`).join('');
    return`<section class="music-partial-edit" aria-labelledby="partialEditTitle"><div class="music-partial-edit-heading"><h2 id="partialEditTitle">Partial Edit</h2><span class="music-partial-edit-status" role="status">${escapeHtml(status)}</span></div><p>${escapeHtml(context)}</p><form id="partialEditProviderForm" class="music-partial-edit-provider" onsubmit="event.preventDefault();MusicStudio.editorRunPartialEditProvider()" autocomplete="off"><label><span>AI instruction</span><select name="intent" ${canPreview?'':'disabled'}>${intentOptions}</select></label><label class="music-partial-edit-instruction"><span>Supplemental instruction（任意）</span><textarea name="instruction" maxlength="200" ${canPreview?'':'disabled'}>${escapeHtml(runtime.text)}</textarea></label><label><span>Provider</span><select name="provider" ${canPreview?'':'disabled'}><option value="openai" ${runtime.provider==='openai'?'selected':''}>OpenAI</option><option value="gemini" ${runtime.provider==='gemini'?'selected':''}>Gemini</option></select></label><label><span>Model</span><input name="model" value="${escapeHtml(runtime.model)}" required ${canPreview?'':'disabled'}></label><label><span>Runtime API Key</span><input name="apiKey" type="password" value="" required autocomplete="new-password" ${canPreview?'':'disabled'}></label><button class="music-primary" type="submit" ${canPreview?'':'disabled'}>${runtime.status==='loading'?'Running…':'Run Provider'}</button></form><p class="music-partial-edit-provider-status is-${escapeHtml(runtime.status)}" role="status" aria-live="polite">${escapeHtml(providerStatus)}</p><div class="music-partial-edit-actions"><button class="music-primary" onclick="MusicStudio.editorStartPartialEdit()" ${active?'disabled':''}>Start Edit</button><button class="music-secondary" onclick="MusicStudio.editorPreviewPartialEditPitchUp()" ${canPreview?'':'disabled'}>Pitch +1 Preview（Local）</button><button class="music-primary" onclick="MusicStudio.editorApplyPartialEdit()" ${canApply?'':'disabled'}>Apply</button><button class="music-secondary" onclick="MusicStudio.editorCancelPartialEdit()" ${active?'':'disabled'}>Cancel / Preview破棄</button></div><dl class="music-partial-edit-summary" aria-label="Partial Edit summary"><div><dt>Updated</dt><dd>${summary.updated}</dd></div><div><dt>Added</dt><dd>${summary.added}</dd></div><div><dt>Deleted</dt><dd>${summary.deleted}</dd></div><div><dt>Unchanged</dt><dd>${summary.unchanged}</dd></div></dl><p class="music-assist-description">ProviderとLocal Previewは既存の安全境界を共有します。Apply前はProjectを変更しません。</p></section>`
  }
  let noteLengthPresets='';
  function editorBottomBarMarkup(session,track,selectionCount){
    const editableSelectionCount=root.MusicStudioEditor.editableNotes(editorSelectedNotes(session)).length;noteLengthPresets=['1/1','1/2','1/4','1/8','1/16','1/32'].map(value=>`<button class="music-secondary" onclick="MusicStudio.editorSetSelectedNoteLength('${value}')" ${editableSelectionCount?'':'disabled'}>${value}</button>`).join('');
    const loop=editorLoop(session),transport=editorTransportSettings(session),transportActive=state.midiInput.countingIn||state.midiInput.recording||state.melodyAudio.playing||state.melodyAudio.starting,midiReady=state.midiInput.supported!==false&&state.midiInput.inputs.length>0,melody=session.part==='melody',hasPlaybackNotes=editorActivePlaybackTracks(session).some(item=>item.notes.length),playback=`<div class="music-transport-main"><button class="music-secondary" onclick="MusicStudio.editorToggleMidiRecording()" ${midiReady?'aria-disabled="false"':'disabled aria-disabled="true"'} aria-pressed="${state.midiInput.countingIn||state.midiInput.recording}"><span class="music-record-dot" aria-hidden="true">●</span> Record（録音）</button><button class="music-primary" onclick="MusicStudio.editorToggleMelodyPlayback()" ${hasPlaybackNotes&&!state.midiInput.recording&&!state.midiInput.countingIn?'':'disabled'} aria-pressed="${state.melodyAudio.playing||state.melodyAudio.starting}">▶ Play（再生）</button><button class="music-secondary" onclick="MusicStudio.editorStopTransport()" ${transportActive?'':'disabled'}>■ Stop（停止）</button>${melody?`<button class="${loop.enabled?'music-primary':'music-secondary'} music-loop-toggle" onclick="MusicStudio.editorToggleLoop()" aria-pressed="${loop.enabled}">Loop ${loop.enabled?'ON（ループON）':'OFF（ループOFF）'}</button><button class="music-secondary" onclick="MusicStudio.editorClearLoop()" ${loop.specified?'':'disabled'}>Clear Loop（ループ解除）</button>`:''}</div><div class="music-transport-options"><button class="${transport.countInEnabled?'music-primary':'music-secondary'}" onclick="MusicStudio.editorToggleCountIn()" aria-pressed="${transport.countInEnabled}" ${transportActive?'disabled':''}>Count-in ${transport.countInEnabled?'ON':'OFF'}</button><span>1 Bar（1小節）</span><button class="${transport.metronomeEnabled?'music-primary':'music-secondary'}" onclick="MusicStudio.editorToggleMetronome()" aria-pressed="${transport.metronomeEnabled}" ${transportActive?'disabled':''}>Metronome ${transport.metronomeEnabled?'ON':'OFF'}</button></div>${countInBeatDisplay(session)}`;
    const zoom=Math.max(1,Math.min(30,Number(session.view?.zoom)||1)),snap=editorSnapValue(session),snapEnabled=editorSnapEnabled(session),snapOptions=[['measure','1小節'],['1/2','1/2'],['1/4','1/4'],['1/8','1/8'],['1/16','1/16'],['1/32','1/32']].map(([value,label])=>`<option value="${value}" ${snap===value?'selected':''}>${label}</option>`).join(''),quantizeEnabled=session.view?.quantizeEnabled===true,quantize=QUANTIZE_VALUES.includes(session.view?.quantize)?session.view.quantize:'1/16',quantizeOptions=QUANTIZE_VALUES.map(value=>`<option value="${value}" ${quantize===value?'selected':''}>${value}</option>`).join('');
    return`<div class="music-editor-bottom" aria-label="エディター下部ツール"><section><h2>編集ツール</h2><div><button class="music-secondary" onclick="MusicStudio.editorToggleMultiSelect(this)" aria-pressed="${Boolean(state.midiMultiSelect)}">Select（選択）：${state.midiMultiSelect?'複数':'単一'}</button><button class="music-primary" onclick="MusicStudio.editorAddNote()">Add Note（ノート追加）</button><button class="music-secondary" onclick="MusicStudio.editorDeleteNote()" ${selectionCount?'':'disabled'}>Eraser（消しゴム）</button><button class="music-secondary" onclick="MusicStudio.editorCopy()" ${selectionCount?'':'disabled'}>Copy（コピー）</button><button class="music-secondary" onclick="MusicStudio.editorPaste()" ${session.clipboard.length?'':'disabled'}>Paste（貼り付け）</button><button class="music-secondary" onclick="MusicStudio.editorDuplicate()" ${selectionCount?'':'disabled'}>Duplicate（複製）</button><button class="music-secondary" onclick="MusicStudio.editorSelectAllNotes()" ${track.notes.length?'':'disabled'}>Select All（全選択）</button><button class="music-secondary" onclick="MusicStudio.editorMatchDuration()" ${selectionCount>1?'':'disabled'}>Match Length（長さを揃える）</button><button class="music-secondary" onclick="MusicStudio.editorMatchVelocity()" ${selectionCount>1?'':'disabled'}>Match Velocity（Velocityを揃える）</button></div></section><section class="music-display-assist"><h2>表示・編集補助</h2><div class="music-assist-layout"><div class="music-assist-block music-assist-snap"><div class="music-assist-snap-controls"><button class="${snapEnabled?'music-primary':'music-secondary'}" onclick="MusicStudio.editorToggleSnap()" aria-pressed="${snapEnabled}">Snap ${snapEnabled?'ON（スナップON）':'OFF（スナップOFF）'}</button><label><span>グリッド</span><select onchange="MusicStudio.editorSetSnap(this.value)" ${snapEnabled?'':'disabled'}>${snapOptions}</select></label></div><p class="music-assist-description">ノート位置・長さをGridへ合わせます<span>｜4/4目安：1/4＝1拍、1/8＝1/2拍、1/16＝1/4拍、1/32＝1/8拍</span></p></div><div class="music-assist-block music-assist-quantize"><div class="music-assist-quantize-controls"><button class="${quantizeEnabled?'music-primary':'music-secondary'}" onclick="MusicStudio.editorToggleQuantize()" aria-pressed="${quantizeEnabled}">Quantize ${quantizeEnabled?'ON':'OFF'}</button><label><span>補正グリッド</span><select onchange="MusicStudio.editorSetQuantize(this.value)">${quantizeOptions}</select></label><button class="music-secondary" onclick="MusicStudio.editorApplyQuantize()" ${quantizeEnabled?'':'disabled'}>適用</button></div><p class="music-assist-description">選択ノートの開始位置を最寄りGridへ補正。<span>Snap＝手動編集、Quantize＝録音済み／既存ノートの後補正。</span></p></div><div class="music-assist-block music-assist-zoom"><button class="music-secondary" onclick="MusicStudio.editorZoom(1)" ${zoom>=30?'disabled':''}>＋ Zoom（ズーム）</button><b class="music-assist-zoom-value">時間軸 Zoom：${zoom}x</b><button class="music-secondary" onclick="MusicStudio.editorZoom(-1)" ${zoom<=1?'disabled':''}>− Zoom（ズーム）</button></div><div class="music-assist-range-grid"><div class="music-assist-card"><button class="music-secondary" onclick="MusicStudio.editorFitPitchRange()">↕ Fit Range（音域を表示）</button><p class="music-assist-description">使用音域を画面内に収めます。</p></div><div class="music-assist-card"><div class="music-measure-length-controls"><button class="music-secondary" onclick="MusicStudio.editorAddMeasures()">＋ Add Measure（小節を追加）</button><button class="music-secondary" onclick="MusicStudio.editorRemoveMeasures()" title="曲末尾の空の1小節を削除" aria-label="Remove Measure（1小節削除）">− Remove Measure（小節削除）</button></div><p class="music-assist-description">末尾へ4小節追加／空の末尾1小節を削除します。</p></div></div></div></section><section><h2>再生</h2><div class="music-transport-controls">${playback||'<p>Melodyパートで再生できます。</p>'}</div></section></div>`;
  }
  function editorBottomBar(session,track,selectionCount){const markup=editorBottomBarMarkup(session,track,selectionCount),selected=editorSelectedNotes(session),lockedCount=selected.filter(note=>root.MusicStudioEditor.isNoteLocked(note)).length,unlockedCount=selected.length-lockedCount,controls=`<button class="music-secondary" onclick="MusicStudio.editorLockSelectedNotes()" ${unlockedCount?'':'disabled'} title="選択ノートを編集から保護">🔒 Lock（固定）</button><button class="music-secondary" onclick="MusicStudio.editorUnlockSelectedNotes()" ${lockedCount?'':'disabled'} title="選択ノートの編集保護を解除">🔓 Unlock（固定解除）</button>`,lengthControls=`<span class="music-note-length-label">Note Length</span>${noteLengthPresets}`,range=root.MusicStudioEditor.normalizeEditRange(session.editRange,session.midiData.editor.measureCount),rangeNotes=root.MusicStudioEditor.notesInRange(session,range),editableRangeNotes=root.MusicStudioEditor.editableNotesInRange(session,range),rangeControls=`<section class="music-edit-range" aria-label="編集対象の小節"><h2>編集対象の小節</h2><div><label><span>開始小節</span><input type="number" min="1" max="${session.midiData.editor.measureCount}" step="1" value="${range.startMeasure}" aria-label="編集対象の開始小節" onchange="MusicStudio.editorSetEditRange(this.value,null)"></label><span aria-hidden="true">–</span><label><span>終了小節</span><input type="number" min="1" max="${session.midiData.editor.measureCount}" step="1" value="${range.endMeasure}" aria-label="編集対象の終了小節" onchange="MusicStudio.editorSetEditRange(null,this.value)"></label></div><p>Edit Range: Measure ${range.startMeasure}–${range.endMeasure}｜${session.part} ${rangeNotes.length}ノート（編集可能 ${editableRangeNotes.length}）</p></section>${partialEditControls()}`;return markup.replace('<button class="music-secondary" onclick="MusicStudio.editorCopy()"',`${controls}<button class="music-secondary" onclick="MusicStudio.editorCopy()"`).replace('<button class="music-secondary" onclick="MusicStudio.editorMatchVelocity()"',`${lengthControls}<button class="music-secondary" onclick="MusicStudio.editorMatchVelocity()"`).replace('<section><h2>再生</h2>',`${rangeControls}<section><h2>再生</h2>`)}
  function midiEditorView(id){
    const project=editorProject(id),core=root.MusicStudioEditor;if(!id)return `<main class="music-studio-shell music-project-page">${backButton()}<div class="music-empty"><h1>MIDIエディターを開けません</h1><p>プロジェクトIDが空です。</p></div></main>`;if(!project)return `<main class="music-studio-shell music-project-page">${backButton()}<div class="music-empty"><h1>MIDIエディターを開けません</h1><p>対象のMusic Studioプロジェクトが見つかりません。</p><button class="music-primary" onclick="MusicStudio.goProjects()">プロジェクトを選ぶ</button></div></main>`;if(!core)return `<main class="music-studio-shell music-project-page">${backButton()}<div class="music-empty"><h1>MIDIエディターを開けません</h1><p>編集モジュールを読み込めませんでした。ページを再読み込みしてください。</p></div></main>`;let session;try{session=ensureEditor(id)}catch(error){console.error('Music Studio editor session could not be created',error);return `<main class="music-studio-shell music-project-page">${backButton()}<div class="music-empty"><h1>MIDIエディターを開けません</h1><p>プロジェクトの編集セッション生成中にエラーが発生しました。</p></div></main>`}if(!session)return `<main class="music-studio-shell music-project-page">${backButton()}<div class="music-empty"><h1>MIDIエディターを開けません</h1><p>プロジェクトの編集セッションを開始できませんでした。</p></div></main>`;
    const track=core.currentTrack(session),selectionCount=editorSelectedNotes(session).length;
    const partButtons=Object.values(core.PARTS).map(part=>`<button class="${session.part===part.id?'music-primary':'music-secondary'}" onclick="MusicStudio.editorSelectPart('${part.id}')" aria-pressed="${session.part===part.id}">${part.name}</button>`).join('');
    const soloByTrackId=state.melodyAudio.playbackState.soloByTrackId||{},trackControls=editorPlaybackTracks(session).map(item=>{const muted=item.muted===true,solo=soloByTrackId[item.id]===true,label=core.PARTS[item.part]?.name||item.name||item.id;return`<div class="music-track-runtime-control" data-track-id="${escapeHtml(item.id)}"><span>${escapeHtml(label)}</span><button class="${muted?'music-primary':'music-secondary'}" onclick="MusicStudio.editorSetTrackMuted('${escapeHtml(item.id)}',${!muted})" aria-pressed="${muted}" aria-label="${escapeHtml(label)} Mute">M</button><button class="${solo?'music-primary':'music-secondary'}" onclick="MusicStudio.editorSetTrackSolo('${escapeHtml(item.id)}',${!solo})" aria-pressed="${solo}" aria-label="${escapeHtml(label)} Solo">S</button></div>`}).join('');
    const undoIcon='<svg class="music-history-icon" viewBox="0 0 24 24" aria-hidden="true"><path d="M9 7 4.5 11.5 9 16"/><path d="M5 11.5h8.5a6 6 0 0 1 6 6"/></svg>',redoIcon='<svg class="music-history-icon" viewBox="0 0 24 24" aria-hidden="true"><path d="m15 7 4.5 4.5L15 16"/><path d="M19 11.5h-8.5a6 6 0 0 0-6 6"/></svg>';
    const historyControls=`<div class="music-history-controls" aria-label="編集履歴"><button class="music-secondary" onclick="MusicStudio.editorUndo()" ${session.undo.length?'':'disabled'} title="元に戻す" aria-label="元に戻す">${undoIcon}<span>戻る</span></button><button class="music-secondary" onclick="MusicStudio.editorRedo()" ${session.redo.length?'':'disabled'} title="やり直す" aria-label="やり直す">${redoIcon}<span>進む</span></button></div>`;
    return`<main class="music-studio-shell music-midi-editor-page" aria-labelledby="midiEditorTitle"><button class="music-secondary music-back" onclick="MusicStudio.openProject('${escapeHtml(id)}')">← Back（プロジェクトへ戻る）</button>${noticeView()}<header class="music-editor-heading"><h1 id="midiEditorTitle">${escapeHtml(core.PARTS[session.part].name)}制作</h1></header>${editorTopBar(project,session)}<nav class="music-part-tabs" aria-label="MIDIパート切替">${partButtons}${historyControls}</nav><div class="music-track-runtime-controls" aria-label="Track Mute / Solo">${trackControls}</div><div class="music-editor-layout">${editorTimeline(session)}</div>${localizedEditorButtons(editorBottomBar(session,track,selectionCount))}${localizedEditorButtons(partWorkflow(session))}</main>`;
  }
  function signatureLabel(signature){return`${signature.numerator}/${signature.denominator}`}
  function repaintEditor(){
    const view=state.midiEditor?.view,viewport=root.document?.querySelector?.('.music-piano-viewport'),pianoScroll=root.document?.querySelector?.('.music-piano-scroll'),menu=root.document?.querySelector?.('.music-correction-menu'),popover=menu?.querySelector?.('.music-correction-popover');
    const pageX=Number(root.scrollX??root.pageXOffset)||0,pageY=Number(root.scrollY??root.pageYOffset)||0;
    if(view){
      if(viewport)view.pitchScrollTop=viewport.scrollTop;if(pianoScroll)view.pitchScrollLeft=pianoScroll.scrollLeft;
      if(menu)view.correctionMenuOpen=menu.open;
      if(popover)view.correctionPopoverScrollTop=popover.scrollTop;
    }
    requestPaint();
    if(typeof root.scrollTo==='function'){try{root.scrollTo({left:pageX,top:pageY,behavior:'instant'})}catch(_){root.scrollTo(pageX,pageY)}}
    restoreEditorPitchScroll();
    if(viewport||menu)root.requestAnimationFrame?.(()=>{if(typeof root.scrollTo==='function'){try{root.scrollTo({left:pageX,top:pageY,behavior:'instant'})}catch(_){root.scrollTo(pageX,pageY)}}fitCorrectionPopover()});
  }
  function editorCloseCorrectionPanel(){
    const menu=root.document?.querySelector?.('.music-correction-menu'),view=state.midiEditor?.view;
    if(menu)menu.open=false;
    if(view){view.correctionMenuOpen=false;view.correctionPopoverScrollTop=0}
    return true;
  }
  function scheduleMidiEditorSave(delay=0){if(state.midiEditorSaveTimer)root.clearTimeout?.(state.midiEditorSaveTimer);state.midiEditorSaveTimer=null;if(!state.midiEditor?.dirty){state.midiEditorSaveRequested=false;return Promise.resolve({ok:true,skipped:true})}state.midiEditorSaveRequested=true;if(delay>0){state.midiEditorSaveTimer=root.setTimeout?.(()=>{state.midiEditorSaveTimer=null;return runMidiEditorAutosave()},delay)||null;return Promise.resolve({ok:true,scheduled:true})}return runMidiEditorAutosave()}
  function flushMidiEditorAutosave(){if(state.midiEditorSaveTimer)root.clearTimeout?.(state.midiEditorSaveTimer);state.midiEditorSaveTimer=null;return runMidiEditorAutosave()}
  function persistEditorView(delay=0){const session=state.midiEditor,view=session?.view;if(!session||!view)return Promise.resolve({ok:true,skipped:true});session.midiData.editor||(session.midiData.editor={});session.midiData.editor.view={zoom:Math.max(1,Math.min(30,Number(view.zoom)||1)),pitchMin:view.pitchMin==null?null:Math.max(0,Math.min(126,Math.round(Number(view.pitchMin)))),pitchMax:view.pitchMax==null?null:Math.max(1,Math.min(127,Math.round(Number(view.pitchMax)))),pitchScrollTop:view.pitchScrollTop==null?null:Math.max(0,Number(view.pitchScrollTop)||0),pitchScrollLeft:Math.max(0,Number(view.pitchScrollLeft)||0),snapEnabled:view.snapEnabled!==false,snap:SNAP_VALUES.includes(view.snap)?view.snap:'1/16',quantizeEnabled:view.quantizeEnabled===true,quantize:QUANTIZE_VALUES.includes(view.quantize)?view.quantize:'1/16'};session.dirty=JSON.stringify(session.midiData)!==JSON.stringify(session.savedMidiData);if(state.midiEditorViewSaveTimer)root.clearTimeout?.(state.midiEditorViewSaveTimer);state.midiEditorViewSaveTimer=null;if(delay>0){state.midiEditorViewSaveTimer=root.setTimeout?.(()=>{state.midiEditorViewSaveTimer=null;scheduleMidiEditorSave()},delay)||null;return Promise.resolve({ok:true,scheduled:true})}return scheduleMidiEditorSave()}
  async function runMidiEditorAutosave(){const session=state.midiEditor;if(!session?.dirty){state.midiEditorSaveRequested=false;return{ok:true,skipped:true}}if(state.midiEditorSaveInFlight)return{ok:true,deferred:true};state.midiEditorSaveInFlight=true;state.midiEditorSaveRequested=false;const saving=saveMidiEditor({silent:true});state.midiEditorSavePromise=saving;const result=await saving;if(state.midiEditorSavePromise===saving)state.midiEditorSavePromise=null;state.midiEditorSaveInFlight=false;if(state.midiEditor===session){if(session.dirty&&(state.midiEditorSaveRequested||result.stale))scheduleMidiEditorSave();repaintEditor()}return result}
  function resetAddNoteSequence(session=state.midiEditor){state.midiAddNoteSequence={session,part:session?.part||'',noteIds:[]}}
  function addNoteSequenceAnchor(session){const sequence=state.midiAddNoteSequence;if(sequence.session!==session||sequence.part!==session.part)return null;const notes=new Map((root.MusicStudioEditor.currentTrack(session)?.notes||[]).map(note=>[note.id,note]));for(let index=sequence.noteIds.length-1;index>=0;index--){const note=notes.get(sequence.noteIds[index]);if(note){sequence.noteIds=sequence.noteIds.slice(0,index+1);return note}}sequence.noteIds=[];return null}
  function editorSelectPart(part){if(state.midiEditor?.part!==part){const synth=state.melodyAudio.synth;for(const [pitch,timer] of pitchPreviewTimers){if(timer!=null)root.clearTimeout?.(timer);synth?.noteOff?.(pitch,'piano-roll-preview')}pitchPreviewTimers.clear();synth?.stopPreview?.()}root.MusicStudioEditor.selectPart(state.midiEditor,part);resetAddNoteSequence();state.midiMultiSelect=false;if(state.midiEditor?.view){state.midiEditor.view.pitchMin=null;state.midiEditor.view.pitchMax=null;state.midiEditor.view.pitchScrollTop=null}requestPaint()}
  function editorSetTrackMuted(trackId,muted){const session=state.midiEditor,editor=root.MusicStudioEditor;if(!session||!editor?.setTrackMuted)return false;editor.setTrackMuted(session,trackId,muted===true);scheduleMidiEditorSave();repaintEditor();return true}
  function editorSetTrackSolo(trackId,solo){const track=editorPlaybackTracks().find(item=>item.id===trackId||item.part===trackId);if(!track)return false;const current=state.melodyAudio.playbackState.soloByTrackId||{};state.melodyAudio.playbackState.soloByTrackId={...current,[track.id]:solo===true};repaintEditor();return true}
  function editorSelectNote(id,additive=false){root.MusicStudioEditor.selectNote(state.midiEditor,id,{toggle:additive||state.midiMultiSelect});repaintEditor()}
  function editorSyncDragSelection(deltaPitch=0){
    const session=state.midiEditor,core=root.MusicStudioEditor,document=root.document;if(!session||!core||!document)return[];const selectedIds=new Set(core.selectedIds(session)),shift=Math.round(Number(deltaPitch)||0),pitches=[...new Set((core.currentTrack(session)?.notes||[]).filter(note=>selectedIds.has(note.id)).map(note=>Math.max(0,Math.min(127,note.pitch+shift))))].sort((a,b)=>b-a),selectedPitches=new Set(pitches);
    document.querySelectorAll?.('.music-midi-note')?.forEach(note=>{const selected=selectedIds.has(note.dataset?.noteId);note.classList?.toggle?.('is-selected',selected);note.setAttribute?.('aria-pressed',String(selected))});
    document.querySelectorAll?.('.music-piano-key[data-pitch]')?.forEach(key=>key.classList?.toggle?.('is-selected-pitch',selectedPitches.has(Number(key.dataset?.pitch))));
    const layer=document.querySelector?.('.music-selected-pitch-layer');if(layer)layer.innerHTML=pitches.map(pitch=>`<span data-pitch="${pitch}" style="--pitch-y:${pianoPitchY(pitch)}px"></span>`).join('');return pitches
  }
  function editorToggleMultiSelect(button){state.midiMultiSelect=!state.midiMultiSelect;if(button){button.setAttribute('aria-pressed',String(state.midiMultiSelect));button.textContent=`複数選択：${state.midiMultiSelect?'ON':'OFF'}`}return state.midiMultiSelect}
  function editorSelectAllNotes(){root.MusicStudioEditor.selectAllNotes(state.midiEditor);repaintEditor()}
  function editorSetPlayheadPosition(event){if(event?.target?.closest?.('.music-midi-note'))return;const roll=event?.currentTarget,rect=roll?.getBoundingClientRect?.(),session=state.midiEditor;if(!roll||!rect||!session)return;const totalTicks=Number(roll.dataset.totalTicks)||1,step=editorSnapTicks(session),raw=(event.clientX-rect.left)/rect.width*totalTicks;if(state.melodyAudio.playing||state.melodyAudio.starting)editorStopMelody(false,false);resetAddNoteSequence(session);setEditorPlayhead(session,Math.max(0,Math.min(totalTicks,Math.round(raw/step)*step)));repaintEditor()}
  function editorStartPlayheadMove(event){if(event?.button!=null&&event.button!==0)return;event?.preventDefault?.();const ruler=event?.currentTarget,rect=ruler?.getBoundingClientRect?.(),session=state.midiEditor;if(!ruler||!rect||!session)return;const totalTicks=Number(ruler.dataset.totalTicks)||1,step=editorSnapTicks(session),move=pointer=>{const raw=(pointer.clientX-rect.left)/rect.width*totalTicks;setEditorPlayhead(session,Math.max(0,Math.min(totalTicks,Math.round(raw/step)*step)))};if(state.melodyAudio.playing||state.melodyAudio.starting)editorStopMelody(false,false);resetAddNoteSequence(session);move(event);ruler.setPointerCapture?.(event.pointerId);ruler.onpointermove=move;ruler.onpointerup=()=>{ruler.onpointermove=null;ruler.onpointerup=null;ruler.onpointercancel=null;repaintEditor()};ruler.onpointercancel=ruler.onpointerup}
  function editorStartLoopRange(event,requestedPart){
    if(state.midiInput.recording)return false;
    if(event?.button!=null&&event.button!==0)return;
    event?.preventDefault?.();event?.stopPropagation?.();
    const source=event?.currentTarget,ruler=source?.closest?.('.music-loop-ruler')||source,rect=ruler?.getBoundingClientRect?.(),session=state.midiEditor;
    if(!ruler||!rect||!session||!rect.width)return;
    const pointerId=event.pointerId,totalTicks=Number(ruler.dataset.totalTicks)||1,step=editorSnapTicks(session),snap=value=>Math.max(0,Math.min(totalTicks,Math.round(value/step)*step)),tick=pointer=>snap((pointer.clientX-rect.left)/rect.width*totalTicks),existing=editorLoop(session),part=requestedPart||event.target?.closest?.('[data-loop-part]')?.dataset?.loopPart||'create',anchor=tick(event),originalStart=existing.specified?existing.start:anchor,originalEnd=existing.specified?existing.end:anchor,originalLength=originalEnd-originalStart,originX=Number(event.clientX)||0,originY=Number(event.clientY)||0,clickThreshold=6;
    let start=part==='create'?anchor:originalStart,end=part==='create'?anchor:originalEnd,finished=false,dragged=false;
    const selection=()=>ruler.querySelector?.('.music-loop-selection'),draw=()=>{const element=selection();if(element){element.style.left=`${Math.min(start,end)/totalTicks*100}%`;element.style.width=`${Math.abs(end-start)/totalTicks*100}%`}};
    const move=pointer=>{if(pointer.pointerId!=null&&pointer.pointerId!==pointerId)return;const dx=(Number(pointer.clientX)||0)-originX,dy=(Number(pointer.clientY)||0)-originY;if(!dragged&&Math.hypot(dx,dy)<clickThreshold)return;dragged=true;const current=tick(pointer);if(part==='start')start=Math.min(current,originalEnd-step);else if(part==='end')end=Math.max(current,originalStart+step);else if(part==='move'){const nextStart=Math.max(0,Math.min(totalTicks-originalLength,originalStart+current-anchor));start=nextStart;end=nextStart+originalLength}else{start=Math.min(anchor,current);end=Math.max(anchor,current);if(end-start<step){if(current<anchor)start=Math.max(0,end-step);else end=Math.min(totalTicks,start+step)}}draw()};
    if(part==='create')ruler.insertAdjacentHTML?.('beforeend','<div class="music-loop-selection" data-loop-part="move" onpointerdown="MusicStudio.editorStartLoopRange(event,\'move\')"><button type="button" class="music-loop-handle is-start" data-loop-part="start" onpointerdown="MusicStudio.editorStartLoopRange(event,\'start\')" aria-label="ループ開始位置を調整"></button><span class="music-loop-selection-label"><span class="music-loop-label-full">Loop ON（ループ）</span><span class="music-loop-label-short">Loop</span></span><button type="button" class="music-loop-handle is-end" data-loop-part="end" onpointerdown="MusicStudio.editorStartLoopRange(event,\'end\')" aria-label="ループ終了位置を調整"></button></div>');
    ruler.setPointerCapture?.(pointerId);
    const finish=(pointer,cancelled=false)=>{if(finished)return;finished=true;ruler.onpointermove=null;ruler.onpointerup=null;ruler.onpointercancel=null;ruler.onlostpointercapture=null;if(ruler.hasPointerCapture?.(pointerId))ruler.releasePointerCapture?.(pointerId);if(cancelled){repaintEditor();return}if(part==='move'&&!dragged){root.MusicStudioEditor.setLoopEnabled(session,!existing.enabled)}else if(part!=='start'&&part!=='end'||dragged){const from=Math.min(start,end),to=Math.max(start,end);root.MusicStudioEditor.setLoopRange(session,from,Math.max(from+step,to),part==='create'?true:existing.enabled)}scheduleMidiEditorSave();repaintEditor()};
    ruler.onpointermove=move;ruler.onpointerup=pointer=>finish(pointer,false);ruler.onpointercancel=pointer=>finish(pointer,true);ruler.onlostpointercapture=pointer=>finish(pointer,true)
  }
  function editorToggleLoop(){const session=state.midiEditor;if(!session)return false;const loop=editorLoop(session),timeline=editorTimelineTicks(session);if(!loop.specified){const step=editorSnapTicks(session),measure=timeline.measureTicks,start=Math.max(0,Math.min(timeline.totalTicks-measure,Math.round((Number(session.playheadTick)||0)/step)*step));root.MusicStudioEditor.setLoopRange(session,start,Math.min(timeline.totalTicks,start+measure),true)}else root.MusicStudioEditor.setLoopEnabled(session,!loop.enabled);scheduleMidiEditorSave();repaintEditor();return editorLoop(session).enabled}
  function editorClearLoop(){const session=state.midiEditor;if(!session)return false;if(state.melodyAudio.playing||state.melodyAudio.starting)editorStopMelody(false,false);root.MusicStudioEditor.setLoopRange(session,null,null,false);scheduleMidiEditorSave();repaintEditor();return true}
  function editorAddNote(){const session=state.midiEditor;if(!session)return false;const anchor=addNoteSequenceAnchor(session),gridTicks=editorGridTicks(session),playheadTick=Number(session.playheadTick),startTick=anchor?anchor.startTick+gridTicks:Number.isFinite(playheadTick)?Math.max(0,playheadTick):0;if(startTick>=editorConfiguredTimelineTicks(session)){notice('小節を追加してください。','info');return false}const input={startTick};if(anchor)input.pitch=anchor.pitch;if(editorSnapEnabled(session))input.durationTicks=gridTicks;root.MusicStudioEditor.addNote(session,input);const noteId=session.selectedNoteId,sequence=state.midiAddNoteSequence;if(sequence.session!==session||sequence.part!==session.part)state.midiAddNoteSequence={session,part:session.part,noteIds:noteId?[noteId]:[]};else if(noteId)sequence.noteIds.push(noteId);scheduleMidiEditorSave();repaintEditor();return true}
  function editorDeleteNote(){const selected=editorSelectedNotes(state.midiEditor),count=root.MusicStudioEditor.editableNotes(selected).length;if(!count)return notice('Locked（固定）ノートは削除されません。Unlockしてから削除してください。','info');root.MusicStudioEditor.deleteSelected(state.midiEditor);scheduleMidiEditorSave();notice(`${count}ノートを削除しました。${selected.length>count?'Lockedノートは保持しました。':''} Undo（元に戻す）で復元できます。`,'success')}
  function editorLockSelectedNotes(){const notes=editorSelectedNotes(state.midiEditor),count=notes.filter(note=>!root.MusicStudioEditor.isNoteLocked(note)).length;if(!count)return false;root.MusicStudioEditor.lockSelectedNotes(state.midiEditor);scheduleMidiEditorSave();notice(`${count}ノートをLock（固定）しました。`,'success');repaintEditor();return true}
  function editorUnlockSelectedNotes(){const notes=editorSelectedNotes(state.midiEditor),count=notes.filter(note=>root.MusicStudioEditor.isNoteLocked(note)).length;if(!count)return false;root.MusicStudioEditor.unlockSelectedNotes(state.midiEditor);scheduleMidiEditorSave();notice(`${count}ノートのLockを解除しました。`,'success');repaintEditor();return true}
  function editorUndo(){root.MusicStudioEditor.undo(state.midiEditor);scheduleMidiEditorSave();repaintEditor()}
  function editorRedo(){root.MusicStudioEditor.redo(state.midiEditor);scheduleMidiEditorSave();repaintEditor()}
  function editorCopy(){root.MusicStudioEditor.copy(state.midiEditor);const count=state.midiEditor.clipboard.length;notice(`${count}ノートをコピーしました。Paste（貼り付け）を押してください。`,'success')}
  function editorPaste(){const count=state.midiEditor.clipboard.length;root.MusicStudioEditor.paste(state.midiEditor,state.midiEditor.playheadTick);scheduleMidiEditorSave();notice(`${count}ノートを再生ライン位置へ貼り付けました。Undo（元に戻す）が使えます。`,'success')}
  function editorDuplicate(){const count=editorSelectedNotes(state.midiEditor).length;root.MusicStudioEditor.duplicateSelected(state.midiEditor);scheduleMidiEditorSave();notice(`${count}ノートを直後へ複製しました。Undo（元に戻す）が使えます。`,'success')}
  function editorMoveSelected(deltaTick,deltaPitch){root.MusicStudioEditor.moveSelected(state.midiEditor,deltaTick,deltaPitch);scheduleMidiEditorSave();repaintEditor()}
  function editorResizeSelected(deltaDuration){root.MusicStudioEditor.resizeSelected(state.midiEditor,deltaDuration);scheduleMidiEditorSave();repaintEditor()}
  function editorSetSnap(value){if(!SNAP_VALUES.includes(value)||!state.midiEditor)return false;(state.midiEditor.view||(state.midiEditor.view={})).snap=value;persistEditorView();repaintEditor();return true}
  function editorSetEditRange(startMeasure,endMeasure){const session=state.midiEditor;if(!session)return false;const current=session.editRange||session.midiData.editor.editRange||{startMeasure:1,endMeasure:1},start=startMeasure==null?current.startMeasure:startMeasure,end=endMeasure==null?current.endMeasure:endMeasure,range=root.MusicStudioEditor.setEditRange(session,{startMeasure:startMeasure!=null&&Number(start)>Number(end)?start:startMeasure==null&&Number(end)<Number(start)?end:start,endMeasure:endMeasure!=null&&Number(end)<Number(start)?end:endMeasure==null&&Number(start)>Number(end)?start:end});scheduleMidiEditorSave();repaintEditor();return range}
  function editorToggleSnap(){if(!state.midiEditor)return false;const view=state.midiEditor.view||(state.midiEditor.view={});view.snapEnabled=view.snapEnabled===false;persistEditorView();repaintEditor();return view.snapEnabled}
  function editorSetQuantize(value){if(!QUANTIZE_VALUES.includes(value)||!state.midiEditor)return false;(state.midiEditor.view||(state.midiEditor.view={})).quantize=value;persistEditorView();repaintEditor();return true}
  function editorToggleQuantize(){if(!state.midiEditor)return false;const view=state.midiEditor.view||(state.midiEditor.view={});view.quantizeEnabled=view.quantizeEnabled!==true;persistEditorView();repaintEditor();return view.quantizeEnabled}
  function editorApplyQuantize(){const session=state.midiEditor;if(!session)return false;if(session.view?.quantizeEnabled!==true)return notice('QuantizeをONにしてください。','info');const result=root.MusicStudioEditor.quantizeSelectedStarts(session,QUANTIZE_VALUES.includes(session.view.quantize)?session.view.quantize:'1/16');if(!result.ok)return notice(result.reason,'info');if(!result.changed)return notice(`${result.count}ノートはすでに${result.resolution}グリッド上です。`,'info');scheduleMidiEditorSave();notice(`${result.count}ノートの開始位置を${result.resolution}へQuantizeしました。Undo（元に戻す）が使えます。`,'success');return result}
  function editorResetZoom(){if(state.midiEditor?.view){state.midiEditor.view.zoom=1;persistEditorView();repaintEditor()}}
  function editorMatchDuration(){root.MusicStudioEditor.matchSelectedDuration(state.midiEditor);scheduleMidiEditorSave();repaintEditor()}
  function editorSetSelectedNoteLength(value){const result=root.MusicStudioEditor.setSelectedNoteLength(state.midiEditor,value);if(result.ok&&result.changed)scheduleMidiEditorSave();repaintEditor();return result}
  function editorMatchVelocity(value){root.MusicStudioEditor.matchSelectedVelocity(state.midiEditor,value);scheduleMidiEditorSave();repaintEditor()}
  function editorClearSelection(){root.MusicStudioEditor.clearNoteSelection(state.midiEditor);repaintEditor()}
  function editorZoom(direction){const view=state.midiEditor.view||(state.midiEditor.view={zoom:1,pitchMin:48,pitchMax:72});view.zoom=Math.max(1,Math.min(30,(Number(view.zoom)||1)+Number(direction)));persistEditorView();repaintEditor()}
  function editorAddMeasures(){root.MusicStudioEditor.extendTimelineMeasures(state.midiEditor,4);scheduleMidiEditorSave();repaintEditor()}
  function editorRemoveMeasures(){const result=root.MusicStudioEditor.removeTimelineMeasures(state.midiEditor,1);if(!result.ok)return notice(result.reason==='notes'?'削除する小節にノートがあります。':'これ以上小節を減らせません。','info');scheduleMidiEditorSave();notice('1小節削除しました。','success');return result}
  function editorFitPitchRange(){const notes=root.MusicStudioEditor.currentTrack(state.midiEditor)?.notes||[],view=state.midiEditor.view||(state.midiEditor.view={zoom:1});view.pitchMin=notes.length?Math.max(0,Math.min(...notes.map(note=>note.pitch))-3):48;view.pitchMax=notes.length?Math.min(127,Math.max(...notes.map(note=>note.pitch))+3):72;view.pitchScrollTop=null;persistEditorView();requestPaint()}
  function editorRememberPitchScroll(element,axis='both'){if(state.midiEditor?.view&&element){const horizontal=axis==='horizontal'||element.classList?.contains?.('music-piano-scroll'),vertical=axis==='vertical'||element.classList?.contains?.('music-piano-viewport');if(horizontal||!vertical&&axis==='both')state.midiEditor.view.pitchScrollLeft=element.scrollLeft;if(vertical||!horizontal&&axis==='both')state.midiEditor.view.pitchScrollTop=element.scrollTop;persistEditorView(300)}}
  function editorHandleShortcut(event){
    const session=state.midiEditor,rawTarget=event?.target,target=rawTarget?.nodeType===3?rawTarget.parentElement:rawTarget,inputFocused=target?.closest?.('input,textarea,select,[contenteditable]:not([contenteditable="false"])');if(!session||!isMidiEditorRoute(currentRoute())||event?.isComposing||event?.repeat||inputFocused)return false;
    const key=String(event.key||''),code=String(event.code||''),lower=key.toLowerCase(),meta=Boolean(event.metaKey),control=Boolean(event.ctrlKey),shift=Boolean(event.shiftKey),option=Boolean(event.altKey),selected=root.MusicStudioEditor.selectedIds(session).length,step=editorSnapTicks(session),space=code==='Space'||key===' '||key==='Spacebar',recordKey=code==='KeyR'||lower==='r',enterKey=code==='Enter'||code==='NumpadEnter'||key==='Enter'||key==='Return';let action=null;
    if(meta&&lower==='z')action=()=>shift?editorRedo():editorUndo();
    else if(meta&&lower==='c'&&selected)action=editorCopy;
    else if(meta&&lower==='v'&&session.clipboard.length)action=editorPaste;
    else if(meta&&lower==='d'&&selected)action=editorDuplicate;
    else if(meta&&lower==='a')action=editorSelectAllNotes;
    else if(meta&&(key==='+'||key==='='))action=()=>editorZoom(1);
    else if(meta&&key==='-')action=()=>editorZoom(-1);
    else if(meta&&key==='0')action=editorResetZoom;
    else if(!meta&&!option&&(key==='Delete'||key==='Backspace')&&selected)action=editorDeleteNote;
    else if(!meta&&!control&&!option&&!shift&&space)action=editorToggleMelodyPlayback;
    else if(!meta&&!option&&enterKey)action=()=>{setEditorPlayhead(session,0);repaintEditor()};
    else if(!meta&&option&&key==='ArrowLeft'&&selected)action=()=>editorResizeSelected(-step);
    else if(!meta&&option&&key==='ArrowRight'&&selected)action=()=>editorResizeSelected(step);
    else if(!meta&&!option&&key==='ArrowLeft'&&selected)action=()=>editorMoveSelected(-step,0);
    else if(!meta&&!option&&key==='ArrowRight'&&selected)action=()=>editorMoveSelected(step,0);
    else if(!meta&&!option&&key==='ArrowUp'&&selected)action=()=>editorMoveSelected(0,shift?12:1);
    else if(!meta&&!option&&key==='ArrowDown'&&selected)action=()=>editorMoveSelected(0,shift?-12:-1);
    else if(!meta&&!control&&!option&&!shift&&recordKey)action=editorToggleMidiRecording;
    else if(!meta&&!option&&!shift&&lower==='n')action=editorAddNote;
    else if(!meta&&!option&&key==='Escape'&&selected)action=editorClearSelection;
    else if(!meta&&!option&&!shift&&['1','2','3','4'].includes(key))action=()=>editorSetSnap({'1':'1/4','2':'1/8','3':'1/16','4':'1/32'}[key]);
    if(!action)return false;if(space)event.preventDefault?.();action();return true
  }
  function editorCloseMidiMenuOnOutsidePointer(event){const menu=root.document?.querySelector?.('.music-midi-input-menu[open]');if(menu&&!event?.target?.closest?.('.music-midi-input-menu'))menu.open=false}
  let shortcutListening=false;
  function syncShortcutListener(route=currentRoute()){const shouldListen=isMidiEditorRoute(route);if(shouldListen&&!shortcutListening){root.addEventListener?.('keydown',editorHandleShortcut,true);root.addEventListener?.('pointerdown',editorCloseMidiMenuOnOutsidePointer,true);shortcutListening=true}else if(!shouldListen&&shortcutListening){root.removeEventListener?.('keydown',editorHandleShortcut,true);root.removeEventListener?.('pointerdown',editorCloseMidiMenuOnOutsidePointer,true);shortcutListening=false}return shortcutListening}
  function editorStartNoteDrag(event,id){
    if(event?.button!=null&&event.button!==0)return;event?.preventDefault?.();const session=state.midiEditor,core=root.MusicStudioEditor,additive=Boolean(event?.shiftKey||event?.metaKey||event?.ctrlKey||state.midiMultiSelect),originalSelectedIds=[...core.selectedIds(session)],already=originalSelectedIds.includes(id);if(!already)core.selectNote(session,id,{toggle:additive});editorSyncDragSelection();
    const target=event.currentTarget,roll=target?.closest?.('.music-piano-roll'),rect=roll?.getBoundingClientRect?.();if(!target||!rect)return repaintEditor();const totalTicks=Number(roll.dataset.totalTicks)||1,pitchSpan=(Number(roll.dataset.pitchMax)||127)-(Number(roll.dataset.pitchMin)||0)+1,startX=event.clientX,startY=event.clientY,step=editorSnapTicks(session),dragElements=[...(roll.querySelectorAll?.('.music-midi-note.is-selected')||[])];if(!dragElements.includes(target))dragElements.push(target);let moved=false,lastTick=0,lastPitch=0,previewPitchDelta=null;
    target.setPointerCapture?.(event.pointerId);target.onpointermove=move=>{const dx=move.clientX-startX,dy=move.clientY-startY;lastTick=Math.round((dx/rect.width*totalTicks)/step)*step;lastPitch=Math.round(-dy/rect.height*pitchSpan);moved=moved||Math.abs(dx)>4||Math.abs(dy)>4;dragElements.forEach(element=>{element.style.translate=`${dx}px ${dy}px`});if(moved&&lastPitch!==previewPitchDelta){editorSyncDragSelection(lastPitch);previewPitchDelta=lastPitch}};
    const finish=(preview,cancelled=false)=>{target.onpointermove=null;target.onpointerup=null;target.onpointercancel=null;dragElements.forEach(element=>{element.style.translate=''});if(cancelled){core.clearNoteSelection(session);originalSelectedIds.forEach(noteId=>core.selectNote(session,noteId,{additive:true}));editorSyncDragSelection();return repaintEditor()}if(moved&&(lastTick||lastPitch)){core.moveSelected(session,lastTick,lastPitch);scheduleMidiEditorSave()}else if(!moved){if(additive&&already)core.selectNote(session,id,{toggle:true});else if(!additive&&core.selectedIds(session).length>1)core.selectNote(session,id);if(preview){const note=core.currentTrack(session)?.notes.find(item=>item.id===id);if(note)editorPreviewPitch(note.pitch,note.velocity)}}repaintEditor()};
    target.onpointerup=()=>finish(true);
    target.onpointercancel=()=>finish(false,true);
  }
  function editorStartMarqueeSelection(event){
    if(event?.target?.closest?.('.music-midi-note')||event?.button!=null&&event.button!==0)return;
    if(event?.pointerType==='touch'||root.matchMedia?.('(pointer: coarse)')?.matches)return editorSetPlayheadPosition(event);
    const roll=event?.currentTarget,rect=roll?.getBoundingClientRect?.(),session=state.midiEditor,core=root.MusicStudioEditor;if(!roll||!rect||!session||!core)return;
    const startX=event.clientX,startY=event.clientY,original=new Set(core.selectedIds(session)),shift=Boolean(event.shiftKey),toggle=Boolean(event.metaKey||event.ctrlKey),threshold=4;let active=false,marquee=null,lastEvent=event;
    const bounds=pointer=>{const x=Math.max(rect.left,Math.min(rect.right,pointer.clientX)),y=Math.max(rect.top,Math.min(rect.bottom,pointer.clientY));return{left:Math.min(startX,x),right:Math.max(startX,x),top:Math.min(startY,y),bottom:Math.max(startY,y)}};
    const move=pointer=>{lastEvent=pointer;const dx=pointer.clientX-startX,dy=pointer.clientY-startY;if(!active&&Math.max(Math.abs(dx),Math.abs(dy))<=threshold)return;if(!active){active=true;marquee=roll.ownerDocument?.createElement?.('div');if(marquee){marquee.className='music-selection-marquee';marquee.setAttribute?.('aria-hidden','true');roll.appendChild?.(marquee)}}pointer.preventDefault?.();const area=bounds(pointer);if(marquee){marquee.style.left=`${area.left-rect.left}px`;marquee.style.top=`${area.top-rect.top}px`;marquee.style.width=`${area.right-area.left}px`;marquee.style.height=`${area.bottom-area.top}px`}};
    const finish=(cancelled=false)=>{roll.onpointermove=null;roll.onpointerup=null;roll.onpointercancel=null;marquee?.remove?.();if(cancelled)return;if(!active){core.clearNoteSelection(session);editorSetPlayheadPosition({target:roll,currentTarget:roll,clientX:lastEvent.clientX});return}const area=bounds(lastEvent),hits=[...(roll.querySelectorAll?.('.music-midi-note')||[])].filter(note=>{const noteRect=note.getBoundingClientRect?.();return noteRect&&noteRect.right>=area.left&&noteRect.left<=area.right&&noteRect.bottom>=area.top&&noteRect.top<=area.bottom}).map(note=>note.dataset?.noteId).filter(Boolean),next=toggle?new Set(original):shift?new Set(original):new Set();for(const id of hits){if(toggle&&next.has(id))next.delete(id);else next.add(id)}core.clearNoteSelection(session);for(const id of next)core.selectNote(session,id,{additive:true});repaintEditor()};
    roll.setPointerCapture?.(event.pointerId);roll.onpointermove=move;roll.onpointerup=pointer=>{lastEvent=pointer;finish(false)};roll.onpointercancel=()=>finish(true);
  }
  function editorStartNoteResize(event,id){
    event?.preventDefault?.();event?.stopPropagation?.();const session=state.midiEditor,core=root.MusicStudioEditor;if(!core.selectedIds(session).includes(id))core.selectNote(session,id);const target=event.currentTarget,noteElement=target?.closest?.('.music-midi-note'),roll=target?.closest?.('.music-piano-roll'),rect=roll?.getBoundingClientRect?.();if(!target||!rect)return;const totalTicks=Number(roll.dataset.totalTicks)||1,startX=event.clientX,step=editorSnapTicks(session),originalWidth=noteElement?.style.width||'0px';let delta=0;
    target.setPointerCapture?.(event.pointerId);target.onpointermove=move=>{delta=Math.round(((move.clientX-startX)/rect.width*totalTicks)/step)*step;if(noteElement)noteElement.style.width=`max(18px, calc(${originalWidth} + ${move.clientX-startX}px))`};
    target.onpointerup=()=>{target.onpointermove=null;target.onpointerup=null;if(delta){core.resizeSelected(session,delta);scheduleMidiEditorSave()}repaintEditor()};target.onpointercancel=target.onpointerup;
  }
  function editorToggleMeasure(measure){root.MusicStudioEditor.toggleMeasure(state.midiEditor,measure);scheduleMidiEditorSave();repaintEditor()}
  function editorSelectMeasureRange(event){event?.preventDefault?.();const form=event?.target,from=Math.max(1,Number(form?.elements.from?.value)||1),to=Math.max(from,Number(form?.elements.to?.value)||from),measures=Array.from({length:to-from+1},(_,index)=>from+index);root.MusicStudioEditor.setSelectedMeasures(state.midiEditor,measures);scheduleMidiEditorSave();repaintEditor()}
  function editorToggleLock(){root.MusicStudioEditor.toggleLockSelected(state.midiEditor);scheduleMidiEditorSave();repaintEditor()}
  function editorPrepareRegeneration(){const result=root.MusicStudioEditor.prepareRegeneration(state.midiEditor,now());if(!result.ok)return notice(result.reason,'info');scheduleMidiEditorSave();notice(`再生成対象（小節 ${result.request.targetMeasures.join(', ')}）を準備しました。AI生成は未接続です。`,'info')}
  function melodySynth(){const audio=state.melodyAudio;if(!audio.synth&&root.MusicStudioAudio)audio.synth=root.MusicStudioAudio.createSynth({volume:.12});return audio.synth}
  async function unlockMelodyAudio(){const synth=melodySynth();if(!synth?.supported?.())return false;return synth.unlock()}
  function editorPianoInput(pitch){const duration=Number(root.document?.querySelector('#melodyInputDuration')?.value)||state.midiEditor.midiData.ppq,velocity=Number(root.document?.querySelector('#melodyInputVelocity')?.value)||100;synthPreview(pitch,velocity);root.MusicStudioEditor.addNote(state.midiEditor,{pitch,durationTicks:duration,velocity});scheduleMidiEditorSave();repaintEditor()}
  async function synthPreview(pitch,velocity){const synth=melodySynth();if(!synth?.supported?.())return false;const scheduled=synth.previewNote(pitch,velocity,.35),unlocked=await synth.unlock();return Boolean(scheduled&&unlocked)}
  const pitchPreviewTimers=new Map();
  async function editorPreviewPitch(pitch,velocity=100){
    const synth=melodySynth(),note=Math.max(0,Math.min(127,Math.round(Number(pitch)||0))),level=Math.max(1,Math.min(127,Math.round(Number(velocity)||100))),channel='piano-roll-preview',part=state.midiEditor?.part||'melody';if(!synth?.supported?.())return false;
    const unlocked=await synth.unlock();if(!unlocked)return false;
    if(part!=='melody'&&synth.previewTrackNote)return synth.previewTrackNote(part,note,level,.35);
    const previous=pitchPreviewTimers.get(note);if(previous!=null)root.clearTimeout?.(previous);pitchPreviewTimers.delete(note);synth.noteOff(note,channel);
    const started=synth.noteOn(note,level,channel),stop=()=>{synth.noteOff(note,channel);pitchPreviewTimers.delete(note)},timer=root.setTimeout?.(stop,350);if(timer!=null)pitchPreviewTimers.set(note,timer);
    return Boolean(started)
  }
  function editorPreviewPitchFromKey(event){const pitch=Number(event?.currentTarget?.dataset?.pitch);if(!Number.isInteger(pitch)||pitch<0||pitch>127)return false;return editorPreviewPitch(pitch)}
  function recordingNoteElement(id){return root.document?.querySelector?.(`.music-midi-editor-page .music-live-note-layer [data-recording-note-id="${id}"]`)}
  function updateLiveNoteWidths(time=midiClock()){const midi=state.midiInput,session=state.midiEditor;if(!midi.recording||!session)return;const nowTick=recordingTickAt(time),totalTicks=editorTimelineTicks(session).totalTicks;for(const note of midi.liveNotes){if(note.active)note.durationTicks=Math.max(1,nowTick-note.startTick);const element=recordingNoteElement(note.id);if(element){element.style.width=`${Math.max(note.durationTicks/totalTicks*100,.45)}%`;updateNoteLabelElement(element)}}}
  function liveRecordingNoteHtml(note,totalTicks=editorTimelineTicks(state.midiEditor).totalTicks){const left=note.startTick/totalTicks*100,width=Math.max(note.durationTicks/totalTicks*100,.45),part=state.midiInput.recordingPart||state.midiEditor?.part||'melody',label=part==='drums'?drumNoteName(note.pitch):noteName(note.pitch);return`<span class="music-midi-note ${velocityClass(note.velocity)} is-recording${note.active?' is-held':' is-released'}" data-recording-note-id="${escapeHtml(note.id)}" data-pitch="${note.pitch}" ${note.active?`data-live-note-id="${escapeHtml(note.id)}"`:`data-recording-preview-id="${escapeHtml(note.id)}"`} style="--pitch-y:${pianoPitchY(note.pitch)}px;top:var(--pitch-y);left:${left}%;width:${width}%" aria-label="録音中 ${escapeHtml(label)}">${noteLabelMarkup(note,part)}</span>`}
  function appendLiveRecordingNote(note){const layer=root.document?.querySelector?.('.music-midi-editor-page .music-live-note-layer');if(!layer?.insertAdjacentHTML)return false;layer.insertAdjacentHTML('beforeend',liveRecordingNoteHtml(note));const element=recordingNoteElement(note.id);if(element)updateNoteLabelElement(element);return Boolean(element)}
  function markLiveRecordingNoteReleased(note){const element=recordingNoteElement(note.id);if(!element)return false;element.classList?.remove?.('is-held');element.classList?.add?.('is-released');element.removeAttribute?.('data-live-note-id');element.setAttribute?.('data-recording-preview-id',note.id);return true}
  function clearLiveRecordingNotes(){state.midiInput.liveNotes=[];const layer=root.document?.querySelector?.('.music-midi-editor-page .music-live-note-layer');if(layer)layer.innerHTML=''}
  function discardLiveRecordingOnUnload(){const midi=state.midiInput;if(midi.countingIn)cancelCountIn();if(!midi.recording){stopEditorMetronome();return}if(midi.recordingFrame!==null)root.cancelAnimationFrame?.(midi.recordingFrame);midi.recorder?.stop?.(midiClock());midi.recording=false;midi.recordingFrame=null;midi.recorder=null;clearLiveRecordingNotes();stopEditorMetronome();melodySynth()?.allNotesOff?.()}
  function handleRecordedMidiMessage(event){const midi=state.midiInput,bytes=event.data||[],eventTime=midiEventClock(event);if(midi.stopping)return;midi.messageGate=midi.messageGate||root.MusicStudioMidiInput?.createMessageGate?.(3);if(midi.messageGate&&!midi.messageGate.accept(bytes,eventTime))return;const status=Number(bytes[0])||0,command=status&0xf0,channel=(status&15)+1,pitch=Number(bytes[1])||0,velocity=Number(bytes[2])||0,synth=melodySynth(),noteOn=command===0x90&&velocity>0,noteOff=command===0x80||command===0x90&&velocity===0,monitorPart=midi.recordingPart||state.midiEditor?.part||'melody';if(monitorPart==='melody'){if(noteOn)synth?.noteOn(pitch,velocity,channel);else if(noteOff)synth?.noteOff(pitch,channel)}else if(noteOn)synth?.previewTrackNote?.(monitorPart,pitch,velocity,.35);if(midi.countingIn){if(eventTime<midi.recordingScheduledAt)return null;activateScheduledRecording()}const recorded=midi.recorder?.handleMessage(bytes,eventTime);if(!midi.recording)return recorded;if(noteOn){const live={id:`live-midi-note-${++midi.liveNoteSequence}`,pitch,velocity,inputChannel:channel,startTick:recordingTickAt(eventTime),durationTicks:1,active:true,isRecording:true};midi.liveNotes.push(live);if(!appendLiveRecordingNote(live))repaintEditor()}else if(noteOff){const live=midi.liveNotes.find(note=>note.active&&note.pitch===pitch&&note.inputChannel===channel);if(live){live.active=false;if(recorded){live.startTick=midi.recordingStartTick+(Number(recorded.startTick)||0);live.durationTicks=Math.max(1,Number(recorded.durationTicks)||1)}updateLiveNoteWidths(eventTime);markLiveRecordingNoteReleased(live)}}return recorded}
  function setMidiInputHandlers(){const midi=state.midiInput;if(!midi.access)return;for(const input of midi.boundInputs)input.onmidimessage=null;for(const input of midi.inputs)input.onmidimessage=null;const selected=midi.inputs.find(input=>input.id===midi.selectedId);if(selected)selected.onmidimessage=handleRecordedMidiMessage;midi.boundInputs=[...midi.inputs]}
  function refreshMidiInputDevices(access=state.midiInput.access){const midi=state.midiInput,inputs=[...(access?.inputs?.values?.()||[])],signature=inputs.map(input=>`${input.id}\u0000${input.name||''}\u0000${input.manufacturer||''}`).join('\u0001'),changed=signature!==midi.inputSignature,selectedConnected=inputs.some(input=>input.id===midi.selectedId);midi.inputs=inputs;midi.inputSignature=signature;if(!selectedConnected){if(midi.recording)editorStopMidiRecording();midi.selectedId=inputs[0]?.id||''}midi.status=inputs.length?`${inputs.length}台のMIDI入力を検出しました。`:'MIDI入力が見つかりません。接続後にもう一度確認してください。';setMidiInputHandlers();return changed}
  async function editorInitializeMidi(options={}){const adapter=root.MusicStudioMidiInput;if(!adapter?.isSupported?.(root.navigator)){const safariLike=adapter?.supportInfo?.(root.navigator)?.safariLike===true;state.midiInput.initialized=true;state.midiInput.supported=false;state.midiInput.status=safariLike?'SafariではMIDI入力を利用できません。Mac Chromeで開いてください。':'この環境ではWeb MIDI APIを利用できません。';return repaintEditor()}try{await unlockMelodyAudio();const midi=state.midiInput,forceRefresh=options?.forceRefresh===true;if(!midi.access||forceRefresh){const previousAccess=midi.access,result=await adapter.requestAccess(root.navigator);if(previousAccess&&previousAccess!==result.access)previousAccess.onstatechange=null;midi.access=result.access;midi.inputSignature='';if(result.access)result.access.onstatechange=()=>{if(refreshMidiInputDevices(result.access))repaintEditor()}}midi.initialized=true;midi.supported=true;refreshMidiInputDevices(midi.access);repaintEditor()}catch(error){state.midiInput.initialized=true;state.midiInput.supported=true;state.midiInput.status=`MIDI入力を利用できません：${error.message}`;repaintEditor()}}
  async function editorSelectMidiInput(id){const midi=state.midiInput,value=String(id||'');if(value==='__rescan__')return editorInitializeMidi({forceRefresh:true});if(!midi.inputs.some(input=>input.id===value))return;midi.selectedId=value;await unlockMelodyAudio();setMidiInputHandlers()}
  function editorSelectMidiTarget(part){const midi=state.midiInput,value=String(part||'');if(midi.starting||midi.countingIn||midi.recording||midi.stopping)return false;if(!['melody','drums','bass'].includes(value))return false;midi.targetPart=value;repaintEditor();return true}
  function midiClock(){return root.performance?.now?.()??Date.now()}
  function midiEventClock(event){const stamp=Number(event?.timeStamp),now=midiClock();if(!Number.isFinite(stamp))return now;const origin=Number(root.performance?.timeOrigin),candidates=Number.isFinite(origin)?[stamp,stamp-origin,stamp+origin]:[stamp];return candidates.reduce((nearest,value)=>Math.abs(value-now)<Math.abs(nearest-now)?value:nearest,stamp)}
  function editorBeatTiming(session=state.midiEditor){const signature=session?.midiData?.timeSignature||{},tempo=Math.max(1,Number(session?.midiData?.tempo)||Number(editorProject(session?.projectId)?.musicalSettings?.bpm)||1),numerator=Math.max(1,Math.round(Number(signature.numerator)||4)),denominator=[1,2,4,8,16,32].includes(Number(signature.denominator))?Number(signature.denominator):4,beatMs=60000/tempo*4/denominator,beatTicks=(Number(session?.midiData?.ppq)||480)*4/denominator;return{tempo,numerator,denominator,beatMs,beatTicks}}
  function editorPlaybackTracks(session=state.midiEditor,targets=['melody','drums','bass']){const tracks=session?.midiData?.tracks||[],requested=Array.isArray(targets)?targets:[targets],selected=[];for(const target of requested){const track=typeof target==='object'&&target?tracks.find(item=>item===target||item.id===target.id):tracks.find(item=>item.id===target||item.part===target);if(track&&!selected.includes(track))selected.push(track)}return selected}
  function editorPlaybackTransport(session=state.midiEditor,runtime={}){const audio=state.melodyAudio,playbackState=audio.playbackState||{};return root.MusicStudioPlayback?.createTransportState?.(session?.midiData||{},{playing:audio.playing,currentTick:session?.playheadTick,playbackState,...runtime})||null}
  function editorActivePlaybackTracks(session=state.midiEditor){return root.MusicStudioPlayback?.activePlaybackTracks?.(editorPlaybackTracks(session),state.melodyAudio.playbackState||{})||[]}
  function scheduleTrackPlayback(synth,tracks,transportState){if(root.MusicStudioPlayback?.schedulePlaybackTracks)return root.MusicStudioPlayback.schedulePlaybackTracks(synth,tracks,transportState);return{ok:false,noteCount:0,durationMs:0,reason:'track-playback-contract-unavailable'}}
  function stopEditorMetronome(){const audio=state.melodyAudio;audio.metronomeToken++;if(audio.metronomeTimer!=null)root.clearTimeout?.(audio.metronomeTimer);audio.metronomeTimer=null;audio.synth?.stopMetronome?.()}
  function startEditorMetronome({startAt=midiClock(),beatIndex=0}={}){const session=state.midiEditor,audio=state.melodyAudio;if(!session||!editorTransportSettings(session).metronomeEnabled)return false;stopEditorMetronome();const timing=editorBeatTiming(session),token=audio.metronomeToken,origin=Number(startAt),firstBeat=Math.max(0,Math.floor(Number(beatIndex)||0));let index=0;const schedule=()=>{if(token!==audio.metronomeToken)return;const target=origin+index*timing.beatMs,delay=Math.max(0,target-midiClock());audio.metronomeTimer=root.setTimeout?.(()=>{if(token!==audio.metronomeToken)return;melodySynth()?.metronomeClick?.((firstBeat+index)%timing.numerator===0);index++;schedule()},delay)??null};schedule();return true}
  function editorToggleTransportSetting(key){const session=state.midiEditor;if(!session||state.midiInput.countingIn||state.midiInput.recording||state.melodyAudio.playing||state.melodyAudio.starting)return false;const editor=session.midiData.editor||(session.midiData.editor={}),transport=editor.transport||(editor.transport={countInEnabled:true,metronomeEnabled:true});transport[key]=transport[key]===false;root.MusicStudioEditor.updateDirty(session);scheduleMidiEditorSave();repaintEditor();return transport[key]}
  function editorToggleCountIn(){return editorToggleTransportSetting('countInEnabled')}
  function editorToggleMetronome(){return editorToggleTransportSetting('metronomeEnabled')}
  function recordingTickAt(time=midiClock()){const midi=state.midiInput,session=state.midiEditor,ppq=Number(session?.midiData?.ppq)||480,tempo=editorBeatTiming(session).tempo,elapsed=Math.max(0,Number(time)-midi.recordingStartedAt);return midi.recordingStartTick+elapsed*ppq*tempo/60000}
  function scheduleRecordingPlayback(startAt){const midi=state.midiInput,session=state.midiEditor,tracks=editorPlaybackTracks(session),active=editorActivePlaybackTracks(session),audio=state.melodyAudio,synth=melodySynth();if(!active.some(track=>track.notes.length)||!synth?.playNotes&&!synth?.playTracks)return null;const loop=editorLoop(session),timeline=editorTimelineTicks(session),endTick=loop.enabled?loop.end:timeline.totalTicks,performanceNow=midiClock(),contextNow=Number(synth.context?.currentTime),startTime=Number.isFinite(contextNow)?contextNow+Math.max(0,(Number(startAt)-performanceNow)/1000):undefined,transport=editorPlaybackTransport(session,{playing:true,currentTick:midi.recordingStartTick,playbackStartOrigin:startTime});synth.stopPlayback?.();const result=scheduleTrackPlayback(synth,tracks,{...transport,startTick:midi.recordingStartTick,endTick,startTime,leadSeconds:startTime==null?0:undefined,playbackState:audio.playbackState});midi.recordingPlayback=result?.ok?result:null;audio.transport=result?.ok?{...transport,playing:true,currentTick:midi.recordingStartTick,playbackStartOrigin:result.playbackStart,activePlaybackTrackIds:transport.activePlaybackTrackIds}:transport;audio.playing=Boolean(result?.ok&&result.noteCount>0);audio.status=audio.playing?'既存Trackを再生しながら録音中':'';return midi.recordingPlayback}
  function commitRecordedNotes(recorder,time,offset,part=state.midiInput.recordingPart||'melody'){const notes=recorder?.stop?.(time)?.map(note=>({...note,id:uuid(),startTick:Math.max(0,(Number(note.startTick)||0)+offset)}))||[],editor=root.MusicStudioEditor;if(notes.length){if(editor?.addNotesToPart)editor.addNotesToPart(state.midiEditor,part,notes);else editor?.addNotes?.(state.midiEditor,notes)}return notes}
  function startRecordingPlayhead(){const midi=state.midiInput,session=state.midiEditor;if(!root.requestAnimationFrame)return;const advance=()=>{if(!midi.recording||midi.stopping)return;const time=midiClock(),loop=editorLoop(session),tick=recordingTickAt(time);if(loop.enabled&&tick>=loop.end){const ppq=Number(session.midiData.ppq)||480,tempo=editorBeatTiming(session).tempo,boundaryTime=midi.recordingStartedAt+(loop.end-midi.recordingStartTick)*60000/(ppq*tempo);editorStopMidiRecording({stoppedAt:boundaryTime,stopTick:loop.end})}else{setEditorPlayhead(session,Math.min(editorTimelineTicks(session).totalTicks,tick));updateLiveNoteWidths(time);midi.recordingFrame=root.requestAnimationFrame(advance)}};midi.recordingFrame=root.requestAnimationFrame(advance)}
  function beginMidiRecording(adapter,startedAt=midiClock()){const midi=state.midiInput,session=state.midiEditor,timing=editorBeatTiming(session);midi.countingIn=false;midi.countInBeat=0;if(midi.countInTimer!=null)root.clearTimeout?.(midi.countInTimer);midi.countInTimer=null;midi.countInAdapter=null;midi.recordingStartedAt=Number(startedAt);midi.recordingActivatedAt=midiClock();if(!midi.recordingPlayback)scheduleRecordingPlayback(midi.recordingStartedAt);midi.recorder=adapter.createRecorder({ppq:session.midiData.ppq,tempo:timing.tempo});midi.recorder.start(midi.recordingStartedAt);midi.recording=true;midi.status=midi.recordingPlayback?'既存Melodyを再生しながら録音中です。':'録音中です。MIDIキーボードを演奏してください。';setMidiInputHandlers();if(editorTransportSettings(session).metronomeEnabled&&state.melodyAudio.metronomeTimer==null)startEditorMetronome({startAt:midi.recordingStartedAt,beatIndex:Math.floor(midi.recordingStartTick/timing.beatTicks)});repaintEditor();startRecordingPlayhead();return{ok:true,recording:true,playback:midi.recordingPlayback}}
  function activateScheduledRecording(){const midi=state.midiInput;if(!midi.countingIn||!midi.countInAdapter)return false;midi.countInToken++;const resolve=midi.countInResolve,adapter=midi.countInAdapter,startedAt=midi.recordingScheduledAt;midi.countInResolve=null;const result=beginMidiRecording(adapter,startedAt);resolve?.(result);return result}
  function cancelCountIn(){const midi=state.midiInput;if(!midi.countingIn)return false;midi.countInToken++;if(midi.countInTimer!=null)root.clearTimeout?.(midi.countInTimer);midi.countInTimer=null;midi.countingIn=false;midi.countInBeat=0;midi.countInAdapter=null;midi.starting=false;midi.recordingPart=null;midi.recordingTrackId=null;midi.status='カウントインを停止しました。録音は開始していません。';editorStopMelody(false,false);midi.recordingPlayback=null;melodySynth()?.allNotesOff?.();melodySynth()?.stopPreview?.();const resolve=midi.countInResolve;midi.countInResolve=null;resolve?.({ok:true,countInCancelled:true,recording:false});repaintEditor();return true}
  function startCountIn(adapter){const midi=state.midiInput,session=state.midiEditor,timing=editorBeatTiming(session),token=++midi.countInToken,startAt=midiClock();midi.countingIn=true;midi.countInBeat=0;midi.countInAdapter=adapter;midi.countInStartedAt=startAt;midi.recordingScheduledAt=startAt+timing.numerator*timing.beatMs;scheduleRecordingPlayback(midi.recordingScheduledAt);midi.status=`カウントイン：${timing.numerator}拍`;setMidiInputHandlers();startEditorMetronome({startAt,beatIndex:0});return new Promise(resolve=>{midi.countInResolve=resolve;let beat=0;const advance=()=>{if(token!==midi.countInToken||!midi.countingIn)return;beat++;if(beat>timing.numerator){activateScheduledRecording();return}midi.countInBeat=beat;midi.status=`Count-in ${beat} / ${timing.numerator}`;repaintEditor();const nextAt=startAt+beat*timing.beatMs;midi.countInTimer=root.setTimeout?.(advance,Math.max(0,nextAt-midiClock()))??null};advance()})}
  async function editorStartMidiRecording(options={}){if(!state.midiEditor)return;const midi=state.midiInput;if(midi.recording&&midi.recorder?.recording)return{ok:false,reason:'already-recording'};if(midi.countingIn||midi.starting||midi.stopping)return{ok:false,reason:'transport-busy'};const targetPart=['melody','drums','bass'].includes(midi.targetPart)?midi.targetPart:'melody',targetTrack=state.midiEditor.midiData.tracks.find(track=>track.part===targetPart);midi.recordingPart=targetPart;midi.recordingTrackId=targetTrack?.id||targetPart;midi.recordRequestedAt=midiClock();midi.starting=true;try{if(midi.recordingFrame!==null)root.cancelAnimationFrame?.(midi.recordingFrame);midi.recording=false;midi.recordingFrame=null;midi.recorder=null;midi.recordingPlayback=null;clearLiveRecordingNotes();stopEditorMetronome();if(state.melodyAudio.playing||state.melodyAudio.starting)editorStopMelody(false,false);await unlockMelodyAudio();if(!midi.initialized)await editorInitializeMidi();const adapter=root.MusicStudioMidiInput;if(!midi.supported||!midi.inputs.length)return notice(midi.status||'MIDI入力が見つかりません。','info');midi.messageGate?.reset();const loop=editorLoop(state.midiEditor),playhead=Math.max(0,Number(state.midiEditor.playheadTick)||0);midi.recordingStartTick=loop.enabled&&(playhead<loop.start||playhead>=loop.end)?loop.start:playhead;setEditorPlayhead(state.midiEditor,midi.recordingStartTick);if(editorTransportSettings(state.midiEditor).countInEnabled&&!options.skipCountIn)return await startCountIn(adapter);midi.countInStartedAt=0;midi.recordingScheduledAt=midiClock();return beginMidiRecording(adapter,midi.recordingScheduledAt)}finally{midi.starting=false;if(!midi.recording&&!midi.countingIn){midi.recordingPart=null;midi.recordingTrackId=null}}}
  async function editorStopMidiRecording(options={}){const midi=state.midiInput;if(!midi.recording||!midi.recorder||midi.stopping)return;const session=state.midiEditor,targetPart=midi.recordingPart||'melody',targetName=root.MusicStudioEditor?.PARTS?.[targetPart]?.name||targetPart,stoppedAt=Number.isFinite(options.stoppedAt)?options.stoppedAt:midiClock(),loop=editorLoop(session),rawStopTick=recordingTickAt(stoppedAt),stopTick=Number.isFinite(options.stopTick)?options.stopTick:loop.enabled?Math.min(loop.end,Math.max(loop.start,rawStopTick)):Math.min(editorTimelineTicks(session).totalTicks,rawStopTick),offset=midi.recordingStartTick;midi.stopping=true;if(midi.recordingFrame!==null)root.cancelAnimationFrame?.(midi.recordingFrame);midi.recordingFrame=null;editorStopMelody(false,false);const notes=commitRecordedNotes(midi.recorder,stoppedAt,offset,targetPart);midi.recorder=null;midi.recordingPlayback=null;melodySynth()?.allNotesOff();melodySynth()?.stopPreview?.();clearLiveRecordingNotes();midi.recording=false;midi.status=notes.length?`${notes.length}ノートを${targetName}へ追加しました。保存しています…`:'録音を停止しました。ノートはありません。';repaintEditor();const saved=notes.length?await saveMidiEditor({silent:true}):{ok:true,skipped:true};midi.stopping=false;midi.recordingPart=null;midi.recordingTrackId=null;setEditorPlayhead(session,stopTick);midi.status=notes.length?(saved.ok?`${notes.length}ノートを${targetName}へ追加して保存しました。`:`${notes.length}ノートを追加しましたが、保存できませんでした。「MIDI編集を保存」を押してください。`):'録音を停止しました。ノートはありません。';setMidiInputHandlers();repaintEditor();return{...saved,notes,targetPart}}
  let midiRecordingStartPromise=null;
  function editorToggleMidiRecording(){const midi=state.midiInput;if(midi.stopping)return Promise.resolve({ok:false,reason:'transport-busy'});if(midi.countingIn)return editorStopTransport();if(midiRecordingStartPromise)return midiRecordingStartPromise.then(()=>midi.recording?editorStopTransport():{ok:false,reason:'start-failed'});if(midi.recording)return editorStopTransport();const starting=editorStartMidiRecording();midiRecordingStartPromise=Promise.resolve(starting).finally(()=>{midiRecordingStartPromise=null});return midiRecordingStartPromise}
  async function editorStopTransport(){editorStopMelody(false,false);if(cancelCountIn())return{ok:true,countInCancelled:true,recording:false};const recording=await editorStopMidiRecording();if(!recording){stopEditorMetronome();repaintEditor()}return recording||{ok:true,playbackStopped:true}}
  function setEditorPlayhead(session,tick){if(!session)return;session.playheadTick=Math.max(0,Number(tick)||0);const line=root.document?.querySelector?.('.music-playhead');if(line)line.style.left=`${Math.min(100,session.playheadTick/editorTimelineTicks(session).totalTicks*100)}%`}
  function startEditorPlayhead(session,result,request){const audio=state.melodyAudio,resultStart=Number.isFinite(result.startTick)?result.startTick:0;if(!root.requestAnimationFrame||!Number.isFinite(result.playbackStart)||!Number.isFinite(result.secondsPerTick)||result.secondsPerTick<=0)return;const advance=()=>{if(!audio.playing||request!==audio.playRequest)return;const currentTime=audio.synth?.context?.currentTime;if(!Number.isFinite(currentTime))return;const currentTick=Math.min(result.endTick,resultStart+Math.max(0,(currentTime-result.playbackStart)/result.secondsPerTick));if(audio.transport)audio.transport.currentTick=currentTick;setEditorPlayhead(session,currentTick);audio.playbackFrame=root.requestAnimationFrame(advance)};audio.playbackFrame=root.requestAnimationFrame(advance)}
  function playMelodySegment(session,tracks,startTick,endTick,request,looping){const audio=state.melodyAudio,synth=melodySynth();if(request!==audio.playRequest)return{ok:false,reason:'cancelled'};synth.stopPlayback();const transport=editorPlaybackTransport(session,{playing:true,currentTick:startTick}),result=scheduleTrackPlayback(synth,tracks,{...transport,startTick,endTick,leadSeconds:looping?0:undefined,playbackState:audio.playbackState});audio.playing=result.ok&&result.noteCount>0;audio.transport={...transport,playing:audio.playing,currentTick:startTick,playbackStartOrigin:result.playbackStart,activePlaybackTrackIds:transport?.activePlaybackTrackIds||[]};audio.status=audio.playing?(looping?'3Trackループ再生中':'3Track再生中'):'この範囲に再生できるノートがありません';if(audio.playing){const duration=looping?Math.max(1,endTick-startTick)*result.secondsPerTick*1000:result.durationMs;audio.playbackTimer=root.setTimeout?.(()=>{if(request===audio.playRequest)editorStopMelody()},duration);startEditorPlayhead(session,result,request)}return result}
  async function editorPlayMelody(){const session=state.midiEditor,tracks=editorPlaybackTracks(session),active=editorActivePlaybackTracks(session),audio=state.melodyAudio;if(!active.some(track=>track.notes.length))return notice('再生するTrackノートがありません。','info');if(audio.starting||audio.playing)return{ok:false,reason:'already-playing'};const loop=editorLoop(session),timeline=editorTimelineTicks(session),looping=loop.enabled,startTick=looping&&(session.playheadTick<loop.start||session.playheadTick>=loop.end)?loop.start:Math.max(0,Number(session.playheadTick)||0),endTick=looping?loop.end:undefined;editorStopMelody(false,false);setEditorPlayhead(session,startTick);const request=++audio.playRequest;audio.starting=true;audio.status='3Track再生を準備中';repaintEditor();if(!await unlockMelodyAudio()){if(request===audio.playRequest){audio.starting=false;audio.status=''}return notice('このブラウザではPlayback音源を利用できません。','error')}if(request!==audio.playRequest)return{ok:false,reason:'cancelled'};audio.starting=false;const result=playMelodySegment(session,tracks,startTick,Math.min(endTick??timeline.totalTicks,timeline.totalTicks),request,looping);if(result.ok&&result.noteCount>0&&editorTransportSettings(session).metronomeEnabled){const timing=editorBeatTiming(session),contextNow=melodySynth()?.context?.currentTime,leadMs=Number.isFinite(result.playbackStart)&&Number.isFinite(contextNow)?Math.max(0,(result.playbackStart-contextNow)*1000):0,startBeat=Math.ceil(startTick/timing.beatTicks),beatDelay=(startBeat*timing.beatTicks-startTick)*60000/(timing.tempo*(Number(session.midiData.ppq)||480));startEditorMetronome({startAt:midiClock()+leadMs+beatDelay,beatIndex:startBeat})}repaintEditor();return result}
  function editorToggleMelodyPlayback(){const audio=state.melodyAudio;return audio.playing||audio.starting||state.midiInput.recording?editorStopTransport():editorPlayMelody()}
  function editorStopMelody(repaint=true,resetPlayhead=true){const audio=state.melodyAudio;audio.playRequest++;if(audio.playbackTimer)root.clearTimeout?.(audio.playbackTimer);audio.playbackTimer=null;if(audio.playbackFrame!==null)root.cancelAnimationFrame?.(audio.playbackFrame);audio.playbackFrame=null;audio.synth?.stopPlayback();stopEditorMetronome();audio.starting=false;audio.playing=false;audio.status='';if(audio.transport)audio.transport={...audio.transport,playing:false,currentTick:resetPlayhead?0:Number(state.midiEditor?.playheadTick)||0};if(resetPlayhead)setEditorPlayhead(state.midiEditor,0);if(repaint)repaintEditor()}
  function transposeInput(panel=root.document?.querySelector?.('#melodyTransposePanel')){const field=name=>panel?.querySelector?.(`[name="${name}"]`),checked=name=>panel?.querySelector?.(`input[name="${name}"]:checked`);return{fromKey:field('transposeFromKey')?.value||state.midiEditor?.transposeSettings?.fromKey||'C',toKey:field('transposeToKey')?.value||state.midiEditor?.transposeSettings?.toKey||'C',target:checked('transposeTarget')?.value||state.midiEditor?.transposeSettings?.target||'all',measureFrom:Number(field('transposeMeasureFrom')?.value),measureTo:Number(field('transposeMeasureTo')?.value),direction:checked('transposeDirection')?.value||state.midiEditor?.transposeSettings?.direction||'shortest'}}
  function editorUpdateTransposeSettings(panel){const session=state.midiEditor;if(!session)return;session.transposeSettings=transposeInput(panel);session.transposePreview=null;repaintEditor();const menu=root.document?.querySelector?.('.music-correction-menu');if(menu)menu.open=true;return session.transposeSettings}
  function editorPreviewTranspose(){const session=state.midiEditor,input=transposeInput();session.transposeSettings=input;const result=root.MusicStudioEditor.previewTranspose(session,input);if(!result.ok)return notice(result.reason,'info');repaintEditor();return result}
  function editorCancelTranspose(){root.MusicStudioEditor.cancelTranspose(state.midiEditor);repaintEditor()}
  function editorApplyTranspose(){const preview=state.midiEditor?.transposePreview;if(!preview)return notice('先に移調プレビューを確認してください。','info');root.MusicStudioEditor.applyTranspose(state.midiEditor);scheduleMidiEditorSave();notice(`${preview.targetNoteIds.length}ノートを${preview.semitones>=0?'+':''}${preview.semitones}半音移調しました。Undoで元に戻せます。`,'success')}
  function noteLengthInput(panel=root.document?.querySelector?.('#melodyNoteLengthPanel')){const field=name=>panel?.querySelector?.(`[name="${name}"]`),checked=name=>panel?.querySelector?.(`input[name="${name}"]:checked`);return{target:checked('noteLengthTarget')?.value||state.midiEditor?.noteLengthSettings?.target||'all',measureFrom:Number(field('noteLengthMeasureFrom')?.value),measureTo:Number(field('noteLengthMeasureTo')?.value),length:field('noteLengthValue')?.value||state.midiEditor?.noteLengthSettings?.length||'current'}}
  function editorUpdateNoteLengthSettings(panel){const session=state.midiEditor;if(!session)return;session.noteLengthSettings=noteLengthInput(panel);session.noteLengthPreview=null;repaintEditor();const menu=root.document?.querySelector?.('.music-correction-menu');if(menu)menu.open=true;return session.noteLengthSettings}
  function editorPreviewNoteLength(){const session=state.midiEditor,input=noteLengthInput();session.noteLengthSettings=input;const result=root.MusicStudioEditor.previewNoteLength(session,input);if(!result.ok)return notice(result.reason,'info');repaintEditor();return result}
  function editorCancelNoteLength(){root.MusicStudioEditor.cancelNoteLength(state.midiEditor);repaintEditor()}
  function editorApplyNoteLength(){const preview=state.midiEditor?.noteLengthPreview;if(!preview)return notice('先にノート長Previewを確認してください。','info');root.MusicStudioEditor.applyNoteLength(state.midiEditor);scheduleMidiEditorSave();notice(`${preview.targetNoteIds.length}ノートの長さを変更しました。Undoで元に戻せます。`,'success')}
  function editorUpdateCorrectionGuide(form){const session=state.midiEditor,elements=form?.elements;if(!session||!elements)return;session.correctionSettings={key:elements.key?.value||'C',scale:elements.scale?.value||'Major',guideEnabled:elements.guideEnabled?.checked!==false};repaintEditor();const menu=root.document?.querySelector?.('.music-correction-menu');if(menu)menu.open=true;return session.correctionSettings}
  function editorCorrectionInput(){const form=root.document?.querySelector?.('#melodyCorrectionForm'),elements=form?.elements;if(!elements)return{key:'C',scale:'Major',quantize:'1/16',strength:100,swing:0,target:'all',measureFrom:1,measureTo:1,correctDuration:true,cleanShortOverlaps:true};if(state.midiEditor)state.midiEditor.correctionSettings={key:elements.key?.value||'C',scale:elements.scale?.value||'Major',guideEnabled:elements.guideEnabled?.checked!==false};return{key:elements.key?.value,scale:elements.scale?.value,quantize:elements.quantize?.value,strength:Number(elements.strength?.value),swing:Number(elements.swing?.value),target:elements.target?.value||form.querySelector?.('input[name="target"]:checked')?.value||'all',measureFrom:Number(elements.measureFrom?.value),measureTo:Number(elements.measureTo?.value),correctDuration:true,cleanShortOverlaps:true}}
  function editorPreviewCorrection(){const result=root.MusicStudioEditor.previewCorrection(state.midiEditor,editorCorrectionInput());if(!result.ok)return notice(result.reason,'info');repaintEditor();return result}
  function editorToggleCorrectionPreview(){if(!state.midiEditor?.correctionPreview)return editorPreviewCorrection();const preview=state.midiEditor.correctionPreview;preview.viewMode=preview.viewMode==='original'?'corrected':'original';repaintEditor();return preview.viewMode}
  function editorCancelCorrection(){root.MusicStudioEditor.cancelCorrection(state.midiEditor);repaintEditor()}
  function editorApplyCorrection(){root.MusicStudioEditor.applyCorrection(state.midiEditor);scheduleMidiEditorSave();notice('補正をMelodyへ適用しました。Undoで補正前へ戻せます。','success')}
  function editorDrumInput(pitch){editorPreviewPitch(pitch,100);root.MusicStudioEditor.addNote(state.midiEditor,{pitch,durationTicks:Math.max(1,Math.round(state.midiEditor.midiData.ppq/2)),velocity:100});scheduleMidiEditorSave();repaintEditor()}
  function editorGenerateCandidate(variant){const candidate=root.MusicStudioEditor.setCandidate(state.midiEditor,state.midiEditor.part,variant);if(!candidate)return notice('このパートには候補生成を接続していません。','info');notice(`${candidate.label}（${candidate.notes.length}ノート）を準備しました。採用するまで現在のトラックは変更されません。`,'info')}
  function editorApplyCandidate(){const part=state.midiEditor.part,candidate=state.midiEditor.candidates?.[part];if(!candidate)return notice('先に候補を生成してください。','info');root.MusicStudioEditor.applyCandidate(state.midiEditor,part);scheduleMidiEditorSave();notice(`${candidate.label}を${root.MusicStudioEditor.PARTS[part].name}トラックへ反映しました。`,'success')}
  function editorUpdateSelected(event){event?.preventDefault?.();const form=event?.target,note=editorNote(state.midiEditor),count=editorSelectedNotes(state.midiEditor).length;if(!form||!note)return;const velocity=Number(form.elements.velocity.value);if(count>1)root.MusicStudioEditor.setSelectedVelocity(state.midiEditor,velocity);else root.MusicStudioEditor.updateNote(state.midiEditor,note.id,{pitch:Number(form.elements.pitch.value),startTick:Number(form.elements.startTick.value),durationTicks:Number(form.elements.durationTicks.value),velocity});scheduleMidiEditorSave();repaintEditor()}
  async function saveMidiEditor(options={}){const pending=state.midiEditorSavePromise;if(pending)await pending;const session=state.midiEditor;if(!session?.dirty)return{ok:true,skipped:true};try{const project=await repository.get(session.projectId);if(!project)throw Error('保存対象のプロジェクトが見つかりません。');const updated={...project,midiData:root.MusicStudioEditor.clone(session.midiData),revision:(project.revision||1)+1,updatedAt:now()};await repository.put(updated);state.projects=state.projects.map(item=>item.projectId===updated.projectId?updated:item);root.MusicStudioEditor.markSaved(session,updated.midiData);if(!options.silent)notice(session.dirty?'保存中に新しい変更が加わりました。もう一度保存してください。':'MIDI編集をプロジェクトへ保存しました。',session.dirty?'info':'success');return{ok:true,project:updated,stale:session.dirty}}catch(error){console.error(error);session.dirty=true;notice('MIDI編集を保存できませんでした。ノートは画面に保持されています。','error');return{ok:false,error}}}
  function hasUnsavedChanges(){return Boolean(state.dirty||state.midiEditor?.dirty)}
  function openMidiEditor(id){if(!guardLeave())return false;const target=id||selectedMidiProject()?.projectId;if(!target){notice('先に音楽プロジェクトを作成してください。','info');routeTo(NEW_ROUTE);return true}state.midiEditor=null;routeTo(`${MIDI_EDITOR_ROUTE}/${encodeURIComponent(target)}`);return true}
  function openPart(id,part='melody'){if(!guardLeave())return false;const target=id||selectedMidiProject()?.projectId;if(!target){notice('先に音楽プロジェクトを作成してください。','info');routeTo(NEW_ROUTE);return true}state.midiEditor=null;ensureEditor(target);root.MusicStudioEditor.selectPart(state.midiEditor,part);routeTo(`${MIDI_EDITOR_ROUTE}/${encodeURIComponent(target)}`);return true}
  function openMelody(id){return openPart(id,'melody')}
  function openLogicPro(id){if(!guardLeave())return false;const target=id||getLastProject()||state.projects[0]?.projectId;if(target)setLastProject(target);routeTo(LOGIC_PRO_ROUTE);return true}
  function placeholderView(route,options={}){const id=route.replace(`${HOME_ROUTE}/`,'');const item=FEATURES.find(feature=>feature.id===id&&!feature.action);if(!item)return homeView(options);return `<main class="music-studio-shell music-placeholder" aria-labelledby="musicPlaceholderTitle">${backButton()}<section class="music-placeholder-panel"><div class="music-card-top"><span class="music-feature-icon" aria-hidden="true">${escapeHtml(item.icon)}</span>${statusBadge(item.status)}</div><p class="music-kicker">Music Studio</p><h1 id="musicPlaceholderTitle">${escapeHtml(item.title)}</h1><p class="music-lead">${escapeHtml(item.description)}</p><p class="music-empty">この機能は今後のMS作業で実装します。現在は既存データを変更しない安全な準備中画面です。</p><button class="music-primary" onclick="MusicStudio.goHome()">ホームへ戻る</button></section></main>`}
  function parseEditId(route){return route.startsWith(`${EDIT_ROUTE}/`)?decodeURIComponent(route.slice(EDIT_ROUTE.length+1)):''}
  function editorHelpDialog(part){const names={melody:'Melody制作',drums:'Drums制作',bass:'Bass制作'},guides={melody:['「MIDI入力」で機器を選ぶ（画面入力だけでも制作できます）','RecordまたはAdd NoteでMelodyを入力する','Piano Rollで位置・長さ・Velocityを調整する','Playで確認する','Savedを確認する','必要ならJSONバックアップを作る','Logic Proへ渡す場合はExport Melody MIDIを使う'],drums:['Drumsタブを開く','パッドまたは候補からリズムを入力する','Piano Rollで位置・長さ・Velocityを調整する','MelodyタブのPlayで全体を確認する','Savedを確認し、必要ならJSONバックアップを作る','Logic Proへ渡す場合はExport All MIDIを使う'],bass:['Bassタブを開く','Melody／コード参照を確認して候補を作る、またはAdd Noteで入力する','Piano Rollで位置・長さ・Velocityを調整する','MelodyタブのPlayで全体を確認する','Savedを確認し、必要ならJSONバックアップを作る','Logic Proへ渡す場合はExport All MIDIを使う']};return helpDialog('musicEditorHelp',`${names[part]||'MIDI制作'} ヘルプ`,`${steps(guides[part]||guides.melody)}${saveGuide()}${termsGuide()}`)}
  function projectHelpDialog(){return helpDialog('musicProjectHelp','Project画面 ヘルプ',`${steps(['曲名・BPM・拍子・Keyを確認して保存する','Melody制作を開いて入力・録音する','Drums、Bassを加える','保存済みを確認する','Project全体を残す時はJSON、DAWへ渡す時はMIDIを書き出す'])}${saveGuide()}${termsGuide()}`)}
  function logicHelpDialog(){return helpDialog('musicLogicHelp','Logic Pro Integration ヘルプ',`${steps(['Music Studioから渡す時はMelody MIDIまたはAll MIDIを書き出す','Logic ProでStandard MIDI Fileを読み込む','Logic Proで編集する','Logic ProからStandard MIDI Fileを書き出す','この画面のImport MIDIで解析する','既存Projectを残したまま、新規または複製として保存する'])}${saveGuide()}${termsGuide()}`)}
  function screenGuide(route,html){
    if(route===FIRST_SONG_GUIDE_ROUTE)return firstSongGuideView();
    if(route===HOME_ROUTE)return html.replace('</nav>',`${helpButton('musicHomeHelp','Music Studio Homeのヘルプ')}</nav><aside class="music-first-song-card" aria-labelledby="firstSongCardTitle"><div><p class="music-kicker">BEGINNER GUIDE</p><h2 id="firstSongCardTitle">最初の1曲を作る</h2><p>Project作成からLogic Proとの受け渡しまで、12の短い手順で確認できます。</p></div><div class="music-first-song-actions"><button type="button" class="music-primary" onclick="MusicStudio.openHelp('firstSongGuide')">ガイドを開く</button><button type="button" class="music-secondary music-guide-window-button" aria-label="最初の1曲ガイドを別ウィンドウで開く" onclick="MusicStudio.openFirstSongWindow()">↗ 別ウィンドウで開く</button></div></aside>${homeHelpDialog()}${firstSongDialog()}`);
    if(route===NEW_ROUTE)return html.replace('>BPM<input','><abbr title="曲の速さ。数値が大きいほど速くなります。">BPM</abbr><input').replace('>拍子<select','><abbr title="1小節に何拍あるかを表します。4/4は1小節に4拍です。">拍子</abbr><select').replace('>キー<select','><abbr title="曲の中心になる音と調です。">キー</abbr><select');
    if(parseEditId(route))return html.replace('<div class="music-section-heading">',`<div class="music-screen-help">${helpButton('musicProjectHelp','Project画面のヘルプ')}</div>${projectHelpDialog()}<div class="music-section-heading">`).replace('<button class="music-secondary" onclick="MusicStudio.exportProject',`<div class="music-action-explain"><button class="music-secondary" onclick="MusicStudio.exportProject`).replace('>JSON書き出し</button>',`>JSON書き出し</button><small>Project全体のバックアップ。後からMusic Studioへ読み込んで復元できます。</small></div>`).replace('>BPM<input','><abbr title="曲の速さ。数値が大きいほど速くなります。">BPM</abbr><input').replace('>拍子<select','><abbr title="1小節に何拍あるかを表します。4/4は1小節に4拍です。">拍子</abbr><select').replace('>キー<select','><abbr title="曲の中心になる音と調です。">キー</abbr><select');
    if(route===PROJECTS_ROUTE||route===PROJECTS_ALIAS_ROUTE)return html.replace('<div class="music-import-box">','<div class="music-import-box"><p><b>JSONを読み込む：</b>JSONバックアップからMusic Studioプロジェクトを追加復元します。既存Projectは削除しません。</p>');
    if(route===LOGIC_PRO_ROUTE)return html.replace('<header class="music-page-header">',`${logicHelpDialog()}<header class="music-page-header"><div class="music-screen-help">${helpButton('musicLogicHelp','Logic Pro Integrationのヘルプ')}</div>`).replace('<h2 id="midiImportTitle">Logic ProからMIDIを取り込む</h2>',`<h2 id="midiImportTitle">Logic ProからMIDIを取り込む</h2><p class="music-inline-guide"><b>Import MIDI：</b>Logic Proなどから書き出したStandard MIDI FileをMusic Studioへ取り込みます。</p>`);
    if(isMidiEditorRoute(route)){const part=state.midiEditor?.part||'melody',dialog=part==='melody'?firstSongDialog():editorHelpDialog(part),button=part==='melody'?firstSongHelpButton('Melody制作のヘルプ'):helpButton('musicEditorHelp',`${root.MusicStudioEditor?.PARTS?.[part]?.name||'MIDI'}制作のヘルプ`);return html.replace('<header class="music-editor-heading">',`${dialog}<header class="music-editor-heading">`).replace('</h1></header>',`</h1>${button}</header>`).replace('</button><details class="music-editor-menu music-midi-input-menu">',`</button><span class="music-saved-explain">この端末・このブラウザのMusic Studio保存領域に自動保存済み。クラウド同期ではありません。</span><details class="music-editor-menu music-midi-input-menu">`).replace('Import MIDI（MIDIを読み込む）<input','Import MIDI（MIDIを読み込む）<small>Logic ProなどのStandard MIDI Fileを取り込みます。</small><input').replace(`Export ${root.MusicStudioEditor?.PARTS?.[part]?.name||''} MIDI（書き出す）</button>`,`Export ${root.MusicStudioEditor?.PARTS?.[part]?.name||''} MIDI（書き出す）</button><small class="music-control-help">${part==='melody'?'MelodyパートをStandard MIDI Fileとして書き出し、Logic ProなどのDAWへ渡します。':'選択中のパートをStandard MIDI Fileとして書き出します。'}</small>`).replace('Export All MIDI（全て書き出す）</button>','Export All MIDI（全て書き出す）</button><small class="music-control-help">Melody / Drums / Bassを含むMIDIを書き出します。</small>')}
    return html
  }
  function ensureRouteNavigation(route,html){
    const routeEditorId=midiEditorId(route),isEditor=Boolean(routeEditorId||route===`${HOME_ROUTE}/midi-composer`);
    let output=html.replace(/<button class="music-secondary music-back"[^>]*>[^<]*<\/button>/,'');
    if(isEditor)output=output.replace(/(<main\b[^>]*>)/,'$1<div class="music-editor-chrome">');
    if(isEditor)output=output.replace('</header>','</header></div>');
    const guided=screenGuide(route,output).replace('曲の末尾に1小節追加します。','現在の動作では、曲の末尾に4小節追加します。');
    return isEditor?guided.replace(/<div class="music-editor-chrome">(<dialog[\s\S]*?<\/dialog>)/,'$1<div class="music-editor-chrome">'):guided;
  }
  function renderRoute(route,options={}){syncShortcutListener(route);let html;if(route===HOME_ROUTE)html=homeView(options);else if(route===NEW_ROUTE)html=createView();else if(route===PROJECTS_ROUTE||route===PROJECTS_ALIAS_ROUTE)html=listView();else if(route===SETTINGS_ROUTE)html=settingsView();else if(route===BACKUP_ROUTE)html=backupView();else if(route===LOGIC_PRO_ROUTE)html=logicProView();else if(route===`${HOME_ROUTE}/midi-composer`){const project=selectedMidiProject();html=project?midiEditorView(project.projectId):state.loaded?createView():`<main class="music-studio-shell music-project-page"><div class="music-empty"><h1>MIDI Composerを準備しています</h1><p>Music Studio専用保存領域を読み込んでいます。</p></div></main>`}else{const editorId=midiEditorId(route),editId=parseEditId(route);html=editorId?midiEditorView(editorId):editId?editView(editId):placeholderView(route,options)}return ensureRouteNavigation(route,html)}
  function formValue(){const form=root.document?.querySelector('#musicProjectForm');if(!form)throw Error('フォームが見つかりません。');const get=name=>form.elements[name]?.value??'';return{projectName:get('projectName'),songTitle:get('songTitle'),purpose:get('purpose'),bpm:get('bpm'),timeSignature:get('timeSignature'),key:get('key'),status:get('status'),productionNotes:get('productionNotes')}}
  async function submitCreate(event){event?.preventDefault?.();try{const project=makeProject(formValue());const checked=validateProject(project);if(!checked.valid){notice(checked.errors.join(' '),'error');return}await repository.put(project);setLastProject(project.projectId);state.dirty=false;await refresh();notice('新しい音楽プロジェクトを保存しました。','success');routeTo(`${EDIT_ROUTE}/${encodeURIComponent(project.projectId)}`)}catch(error){console.error(error);notice('保存できませんでした。既存データは変更されていません。','error')}}
  async function submitEdit(event,id){event?.preventDefault?.();cancelScheduledSave();const result=await saveCurrentProject(id,true);if(result.ok){await refresh();notice('変更を保存しました。','success')}else notice(result.message||'保存できませんでした。入力内容は維持しています。','error')}
  async function duplicateProject(id){try{const source=await repository.get(id);if(!source)return;const timestamp=now();const duplicate={...clone(source),projectId:uuid(),projectName:`${source.projectName}（コピー）`,revision:1,createdAt:timestamp,updatedAt:timestamp};await repository.put(duplicate);setLastProject(duplicate.projectId);await refresh();notice('元のプロジェクトを変更せず複製しました。','success');routeTo(`${EDIT_ROUTE}/${encodeURIComponent(duplicate.projectId)}`)}catch(error){console.error(error);notice('複製できませんでした。','error')}}
  async function askDelete(id){const project=await repository.get(id);if(!project)return;const confirmed=root.confirm?.(`「${project.projectName}」を削除しますか？\nこの操作はこのプロジェクトだけに影響します。`)??false;if(!confirmed){notice('削除をキャンセルしました。','info');return}try{cancelScheduledSave();await repository.delete(id);if(getLastProject()===id)setLastProject('');state.dirty=false;await refresh();notice('プロジェクトを削除しました。ほかのプロジェクトは変更していません。','success');routeTo(PROJECTS_ROUTE)}catch(error){console.error(error);notice('削除できませんでした。','error')}}
  function safeFileName(name,maxLength=120,fallback='music-project'){const safe=String(name||'').normalize('NFKC').replace(/[\\/:*?"<>|\u0000-\u001f]/g,'-').replace(/[\s_-]+/g,'-').replace(/^[.\s-]+|[.\s-]+$/g,'').slice(0,maxLength);return safe||String(fallback||'music-project').normalize('NFKC').replace(/[\\/:*?"<>|\u0000-\u001f]/g,'-').slice(0,maxLength)||'music-project'}
  function fileNameTemplateErrors(template){const tokens=String(template||'').match(/\{[^}]+\}/g)||[];const allowed=new Set(['{projectName}','{songTitle}','{date}','{time}','{version}','{type}']);return [...new Set(tokens.filter(token=>!allowed.has(token)))].map(token=>`未対応トークン ${token} は使用できません。`)}
  function renderFileName(template,data={},options={}){const errors=fileNameTemplateErrors(template);if(errors.length)return{ok:false,errors,filename:''};const stamp=options.date instanceof Date?options.date:new Date(options.date||Date.now());const date=Number.isNaN(stamp.getTime())?'date':stamp.toISOString().slice(0,10);const time=Number.isNaN(stamp.getTime())?'time':stamp.toISOString().slice(11,19).replace(/:/g,'-');const values={projectName:data.projectName||'',songTitle:data.songTitle||'',type:data.type||'',date,time,version:data.version||APP_VERSION};const raw=String(template||'').replace(/\{(projectName|songTitle|type|date|time|version)\}/g,(_,key)=>values[key]);return{ok:true,errors:[],filename:safeFileName(raw,clampNumber(options.maxLength,20,200,120,true),options.fallback||'music-studio')}}
  async function exportProject(id){const project=await repository.get(id);if(!project)return;const exported={...clone(project),midiAssets:(project.midiAssets||[]).map(markExternal),audioAssets:(project.audioAssets||[]).map(markExternal),fileReferences:(project.fileReferences||[]).map(markExternal)};const text=JSON.stringify(exported,null,2);const date=now().slice(0,10);const filename=`${safeFileName(project.projectName)}-${date}.json`;if(!root.document||!root.Blob||!root.URL?.createObjectURL)return{filename,text};const url=root.URL.createObjectURL(new Blob([text],{type:'application/json'}));const link=root.document.createElement('a');link.href=url;link.download=filename;root.document.body.appendChild(link);link.click();link.remove();setTimeout(()=>root.URL.revokeObjectURL(url),1000);notice('JSONを書き出しました。MIDI・音声本体は含まれません。','success');return{filename,text}}
  function markExternal(asset){return{...clone(asset),storage:{...(asset.storage||{}),requiresReselection:true}}}
  async function importText(text){let parsed;try{parsed=JSON.parse(text)}catch(_){return{ok:false,message:'JSONが壊れているため読み込めません。既存データは変更していません。'}}const checked=validateProject(parsed);if(!checked.valid)return{ok:false,message:`読み込めません：${checked.errors.join(' ')}`};const incoming=clone(parsed);let duplicated=false;if(await repository.has(incoming.projectId)){incoming.projectId=uuid();incoming.projectName=`${incoming.projectName}（読み込み）`;incoming.createdAt=now();duplicated=true}incoming.updatedAt=now();incoming.revision=Math.max(1,Number(incoming.revision)||1);await repository.put(incoming);setLastProject(incoming.projectId);await refresh();return{ok:true,project:incoming,duplicated,message:duplicated?'ID重複を検出し、新しいIDで安全に読み込みました。':'プロジェクトを読み込みました。'}}
  async function importFile(file){if(!file)return;try{const result=await importText(await file.text());notice(result.message,result.ok?'success':'error');if(result.ok)routeTo(`${EDIT_ROUTE}/${encodeURIComponent(result.project.projectId)}`)}catch(error){console.error(error);notice('ファイルを読み込めません。既存データは変更していません。','error')}}
  function setPath(target,path,value){const parts=path.split('.');let cursor=target;for(const part of parts.slice(0,-1))cursor=cursor[part];cursor[parts.at(-1)]=value}
  function readSettingsForm(){const form=root.document?.querySelector('#musicSettingsForm');if(!form)throw Error('設定フォームが見つかりません。');const next=clone(state.settings||defaultSettings());for(const element of [...form.elements]){if(!element.name)continue;let value=element.type==='checkbox'?element.checked:element.value;if(element.type==='number')value=Number(value);if(element.name==='projectDefaults.key'&&!value)value=null;setPath(next,element.name,value)}next.logicPro.autoLaunch=false;next.logicPro.directControl=false;next.privacy.externalTransfer=false;next.privacy.usageData=false;next.privacy.analytics=false;next.privacy.cloudSync=false;next.privacy.localOnly=true;next.experimental.enabled=false;next.experimental.features={};return next}
  function settingsTemplateErrors(value){return fileNameTemplateErrors(value.fileNaming?.template)}
  async function persistSettings(input){const errors=settingsTemplateErrors(input);if(errors.length)return{ok:false,message:errors.join(' ')};const normalized=normalizeSettings({...input,updatedAt:now()});try{await repository.putSettings?.(normalized);state.settings=normalized;state.settingsDraft=clone(normalized);state.dirty=false;state.saveStatus='saved';configureAutosave();return{ok:true,settings:normalized,message:'設定を保存しました。'}}catch(error){console.error('Music Studio settings save failed',error);state.saveStatus='error';return{ok:false,message:'設定を保存できませんでした。現在の入力内容は維持しています。'}}}
  async function saveSettingsForm(event){event?.preventDefault?.();state.saveStatus='saving';const indicator=root.document?.querySelector('.music-save-state');if(indicator){indicator.dataset.status='saving';indicator.textContent='保存中'}try{const result=await persistSettings(readSettingsForm());notice(result.message,result.ok?'success':'error')}catch(error){console.error(error);state.saveStatus='error';notice('設定値を確認してください。現在の設定は変更していません。','error')}}
  function markSettingsDirty(){state.dirty=true;state.saveStatus='dirty';const indicator=root.document?.querySelector('.music-save-state');if(indicator){indicator.dataset.status='dirty';indicator.textContent='未保存の変更があります'}updateFileNamePreview()}
  function updateFileNamePreview(){const form=root.document?.querySelector('#musicSettingsForm'),output=root.document?.querySelector('#musicFileNamePreview');if(!form||!output)return;const result=renderFileName(form.elements['fileNaming.template']?.value,{projectName:'夜空のプロジェクト',songTitle:'星明かり',type:'midi'},{maxLength:form.elements['fileNaming.maxLength']?.value,fallback:form.elements['fileNaming.fallback']?.value,date:'2026-07-20T21:30:00.000Z'});output.textContent=result.ok?`プレビュー：${result.filename}.json`:`警告：${result.errors.join(' ')}`}
  function discardSettings(){if(state.dirty&&(root.confirm?.('未保存の設定変更を破棄しますか？')??false)===false)return;state.settingsDraft=clone(state.settings||defaultSettings());state.dirty=false;state.saveStatus='saved';requestPaint()}
  async function resetSettings(){if((root.confirm?.('Music Studio設定だけを初期値に戻しますか？\nプロジェクトデータは削除されません。')??false)===false)return;const createdAt=state.settings?.createdAt||now();const result=await persistSettings({...defaultSettings(),createdAt});notice(result.ok?'設定を初期値に戻しました。プロジェクトは変更していません。':result.message,result.ok?'success':'error')}
  function downloadJson(filename,value){const text=typeof value==='string'?value:JSON.stringify(value,null,2);if(!root.document||!root.Blob||!root.URL?.createObjectURL)return{filename,text};try{const url=root.URL.createObjectURL(new Blob([text],{type:'application/json'}));const link=root.document.createElement('a');link.href=url;link.download=filename;root.document.body.appendChild(link);link.click();link.remove();root.setTimeout?.(()=>root.URL.revokeObjectURL(url),1000);return{filename,text}}catch(error){console.error('Music Studio download failed',error);throw Error('JSONファイルを書き出せませんでした。')}
  }
  function exportSettings(){const value=normalizeSettings(state.settings||defaultSettings(),state.settings?.updatedAt||now());delete value.id;const result=downloadJson(`music-studio-settings_${now().slice(0,10)}.json`,value);notice('設定JSONを書き出しました。プロジェクトは含まれません。','success');return result}
  async function importSettingsText(text,confirmImport=false){let parsed;try{parsed=JSON.parse(text)}catch(_){return{ok:false,message:'設定JSONが壊れているため読み込めません。現在設定は維持しています。'}}const validation=validateSettings(parsed);if(!validation.valid)return{ok:false,message:`設定を読み込めません：${validation.errors.join(' ')}`};if(confirmImport&&(root.confirm?.('現在のMusic Studio設定を読み込んだ内容へ変更しますか？\nプロジェクトデータには影響しません。')??false)===false)return{ok:false,cancelled:true,message:'設定の読み込みをキャンセルしました。'};return persistSettings({...parsed,id:SETTINGS_ID,createdAt:state.settings?.createdAt||parsed.createdAt})}
  async function importSettingsFile(file){if(!file)return;try{const result=await importSettingsText(await file.text(),true);notice(result.message,result.ok?'success':result.cancelled?'info':'error')}catch(error){console.error(error);notice('設定ファイルを読み込めません。現在設定は維持しています。','error')}}
  function backupObject(){const projects=state.projects.map(project=>({...clone(project),midiAssets:(project.midiAssets||[]).map(markExternal),audioAssets:(project.audioAssets||[]).map(markExternal),fileReferences:(project.fileReferences||[]).map(markExternal)}));const references=projects.flatMap(project=>(project.fileReferences||[]).map(reference=>({projectId:project.projectId,...clone(reference)})));return{format:BACKUP_FORMAT,version:BACKUP_VERSION,metadata:{createdAt:now(),appVersion:APP_VERSION,projectCount:projects.length,settingsIncluded:true,binariesIncluded:false},settings:normalizeSettings(state.settings||defaultSettings()),projects,references}}
  function validateBackup(value){const errors=[],warnings=[];if(!value||typeof value!=='object'||Array.isArray(value))return{valid:false,errors:['JSONのルートがオブジェクトではありません。'],warnings};if(value.format!==BACKUP_FORMAT)errors.push(`形式は ${BACKUP_FORMAT} である必要があります。`);if(value.version!==BACKUP_VERSION)errors.push(`バックアップVersion ${value.version??'未設定'} には対応していません。`);if(!Array.isArray(value.projects))errors.push('projects配列がありません。');if(value.settings&&!validateSettings(value.settings).valid)warnings.push('設定形式が正しくないため、設定は復元対象から除外します。');if(Array.isArray(value.projects))value.projects.forEach((project,index)=>{if(!validateProject(project).valid)warnings.push(`プロジェクト${index+1}は不正なためスキップします。`)});return{valid:errors.length===0,errors,warnings}}
  function exportBackup(safety=false){const backup=backupObject();const prefix=safety?'music-studio-before-restore':'music-studio-backup';const result=downloadJson(`${prefix}_${now().slice(0,10)}.json`,backup);if(!safety)notice(`設定とプロジェクト${backup.projects.length}件のバックアップを書き出しました。実ファイル本体は含まれません。`,'success');return result}
  async function previewBackupText(text){let parsed;try{parsed=JSON.parse(text)}catch(_){return{ok:false,message:'バックアップJSONが壊れているため読み込めません。'}}const validation=validateBackup(parsed);if(!validation.valid)return{ok:false,message:`バックアップを読み込めません：${validation.errors.join(' ')}`};state.backupPreview=clone(parsed);requestPaint();return{ok:true,backup:parsed,warnings:validation.warnings,message:`バックアップを検証しました。内容を確認して復元してください。${validation.warnings.length?` 警告：${validation.warnings.join(' ')}`:''}`}}
  async function previewBackupFile(file){if(!file)return;try{const result=await previewBackupText(await file.text());notice(result.message,result.ok?'success':'error')}catch(error){console.error(error);notice('バックアップファイルを読み込めません。','error')}}
  async function restoreBackup(value,{settings=true,projects=true,safetyCopy=false}={}){const validation=validateBackup(value);if(!validation.valid)return{ok:false,added:0,skipped:0,failed:value?.projects?.length||0,message:validation.errors.join(' ')};if(safetyCopy)exportBackup(true);let added=0,skipped=0,failed=0,settingsRestored=false;for(const source of projects?value.projects:[]){const checked=validateProject(source);if(!checked.valid){skipped++;continue}try{const incoming=clone(source);if(await repository.has(incoming.projectId)){incoming.projectId=uuid();incoming.projectName=`${incoming.projectName}（復元）`;incoming.createdAt=now()}incoming.updatedAt=now();await repository.put(incoming);added++}catch(error){failed++;console.warn('Music Studio project restore failed',source?.projectId,error)}}if(settings&&value.settings&&validateSettings(value.settings).valid){const result=await persistSettings({...value.settings,id:SETTINGS_ID,createdAt:state.settings?.createdAt||value.settings.createdAt});settingsRestored=result.ok}else if(settings&&value.settings)skipped++;await refresh();return{ok:failed===0,added,skipped,failed,settingsRestored,message:`復元結果：プロジェクト${added}件を追加、スキップ${skipped}件、失敗${failed}件、設定${settingsRestored?'復元':'維持'}。既存データは削除していません。`}}
  async function restorePreviewedBackup(){const value=state.backupPreview;if(!value)return;if((root.confirm?.('表示中のバックアップを安全に追加・統合しますか？\n既存データは削除しません。')??false)===false)return;const settings=root.document?.querySelector('#restoreSettings')?.checked!==false,projects=root.document?.querySelector('#restoreProjects')?.checked!==false;const result=await restoreBackup(value,{settings,projects,safetyCopy:true});state.backupPreview=null;notice(result.message,result.failed?'error':'success')}
  function clearBackupPreview(){state.backupPreview=null;requestPaint()}
  function backupSignature(){return JSON.stringify({settings:state.settings?.updatedAt||'',projects:state.projects.map(project=>[project.projectId,project.revision,project.updatedAt])})}
  function byteLabel(bytes){if(bytes<1024)return`${bytes} B`;if(bytes<1024*1024)return`${(bytes/1024).toFixed(1)} KB`;return`${(bytes/1024/1024).toFixed(1)} MB`}
  async function loadAutoBackups(){try{state.autoBackups=((await repository.listAutoBackups?.())||[]).sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));return state.autoBackups}catch(error){console.warn('Music Studio automatic backups could not be read',error);state.autoBackups=[];return[]}}
  async function runAutoBackupCheck(timestamp=now()){
    if(!state.settings?.backup.automaticEnabled)return{ok:true,created:false,reason:'disabled'};
    const backups=await loadAutoBackups(),latest=backups[0],nowMs=Date.parse(timestamp),intervalMs=state.settings.backup.intervalHours*3600000;
    if(latest&&Number.isFinite(nowMs)&&nowMs-Date.parse(latest.createdAt)<intervalMs)return{ok:true,created:false,reason:'not-due'};
    const signature=backupSignature();if(latest?.signature===signature)return{ok:true,created:false,reason:'unchanged'};
    const snapshot=backupObject(),text=JSON.stringify(snapshot),bytes=typeof root.TextEncoder==='function'?new root.TextEncoder().encode(text).length:text.length*2;
    if(bytes>4*1024*1024)return{ok:false,created:false,reason:'too-large',message:'自動バックアップが4MBを超えるため作成しませんでした。手動JSON書き出しをご利用ください。'};
    const record={backupId:uuid(),createdAt:timestamp,signature,sizeBytes:bytes,sizeLabel:byteLabel(bytes),snapshot};
    try{await repository.putAutoBackup?.(record);const all=[record,...backups].sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));const keep=state.settings.backup.maxCopies;for(const old of all.slice(keep))await repository.deleteAutoBackup?.(old.backupId);await loadAutoBackups();return{ok:true,created:true,backup:record,message:'期限を迎えたため、端末内に自動バックアップを作成しました。'}}catch(error){console.warn('Music Studio automatic backup failed',error);return{ok:false,created:false,reason:'storage-error',message:'自動バックアップを作成できませんでした。既存データは変更していません。'}}
  }
  async function checkAutoBackupNow(){const result=await runAutoBackupCheck();notice(result.message||(result.reason==='disabled'?'自動バックアップは無効です。':result.reason==='not-due'?'次の作成期限にはまだ達していません。':'内容に変更がないため重複作成しませんでした。'),result.ok?'info':'error')}
  async function restoreAutoBackup(id){const record=state.autoBackups.find(item=>item.backupId===id);if(!record)return notice('自動バックアップが見つかりません。','error');if((root.confirm?.(`${formatDate(record.createdAt)} の自動バックアップを追加・統合しますか？\n既存データは削除しません。`)??false)===false)return;const result=await restoreBackup(record.snapshot,{settings:true,projects:true,safetyCopy:true});notice(result.message,result.failed?'error':'success')}
  function configureAutosave(){if(state.intervalTimer)root.clearInterval?.(state.intervalTimer);state.intervalTimer=null;if(state.settings?.autosave.enabled&&root.setInterval)state.intervalTimer=root.setInterval(()=>{const id=parseEditId(currentRoute());if(id&&state.dirty)saveCurrentProject(id)},state.settings.autosave.intervalSeconds*1000)}
  async function saveCurrentProject(id,force=false){if(!id||(!state.dirty&&!force))return{ok:true,skipped:true};let input;try{input=formValue()}catch(error){return{ok:false,message:error.message}}const sequence=++state.saveSequence,generation=state.editGeneration;state.saveStatus='saving';updateSaveIndicator('保存中');try{const current=await repository.get(id);if(!current){state.saveStatus='error';updateSaveIndicator('保存に失敗しました');return{ok:false,message:'保存対象が見つかりません。入力内容は維持しています。'}}const updated={...current,...input,revision:(current.revision||1)+1,musicalSettings:{...current.musicalSettings,bpm:Number(input.bpm),timeSignature:(()=>{const [numerator,denominator]=input.timeSignature.split('/').map(Number);return{numerator,denominator}})(),key:input.key||null},updatedAt:now()};delete updated.bpm;delete updated.timeSignature;delete updated.key;const validation=validateProject(updated);if(!validation.valid){state.saveStatus='error';updateSaveIndicator('保存に失敗しました');return{ok:false,message:validation.errors.join(' ')}}await repository.put(updated);if(sequence<state.saveSequence||generation!==state.editGeneration){state.dirty=true;state.saveStatus='dirty';updateSaveIndicator('未保存の変更があります');return{ok:true,stale:true,project:updated}}state.savedSequence=sequence;state.dirty=false;state.saveStatus='saved';setLastProject(id);state.projects=state.projects.map(project=>project.projectId===id?updated:project);updateSaveIndicator('保存しました');return{ok:true,project:updated}}catch(error){console.error('Music Studio autosave failed',error);state.saveStatus='error';state.dirty=true;updateSaveIndicator('保存に失敗しました');notice('自動保存に失敗しました。入力内容は画面に維持されています。手動保存をお試しください。','error');return{ok:false,message:'保存に失敗しました。'}}}
  function updateSaveIndicator(text){const indicator=root.document?.querySelector('.music-save-state');if(indicator){indicator.dataset.status=state.saveStatus;indicator.dataset.dirty=String(state.dirty);indicator.textContent=text}}
  function formatDate(value){try{return new Intl.DateTimeFormat('ja-JP',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))}catch(_){return String(value||'')}}
  function cancelScheduledSave(){if(state.autosaveTimer)root.clearTimeout?.(state.autosaveTimer);state.autosaveTimer=null}
  function markDirty(){state.editGeneration++;state.dirty=true;state.saveStatus='dirty';updateSaveIndicator('未保存の変更があります');cancelScheduledSave();const id=parseEditId(currentRoute());if(id&&state.settings?.autosave.enabled&&root.setTimeout)state.autosaveTimer=root.setTimeout(()=>{state.autosaveTimer=null;if(parseEditId(currentRoute())===id)saveCurrentProject(id)},state.settings.autosave.idleDelaySeconds*1000)}
  function cancelEdit(id=''){if(state.dirty&&(root.confirm?.('未保存の変更を破棄しますか？')??false)===false)return;state.dirty=false;routeTo(id?`${EDIT_ROUTE}/${encodeURIComponent(id)}`:HOME_ROUTE);requestPaint()}
  function guardLeave(){if(state.midiInput.recording){notice('録音を停止してから移動してください。','error');return false}if(state.midiEditor?.dirty){notice('MIDI編集に未保存の変更があります。保存してから移動してください。','error');return false}return !state.dirty||(root.confirm?.('未保存の変更を破棄して移動しますか？')??false)}
  function go(route){if(guardLeave()){cancelScheduledSave();state.dirty=false;state.notice='';routeTo(route)}}
  function goDreamArchitect(standalone=false){if(!guardLeave())return false;cancelScheduledSave();state.dirty=false;if(standalone)root.location.href='./index.html#dream-architect';else if(typeof root.setView==='function')root.setView('dream-architect');else root.location.hash='dream-architect';return true}
  function isMusicRoute(route){return route===HOME_ROUTE||route.startsWith(`${HOME_ROUTE}/`)}
  function syncHostChrome(route){syncShortcutListener(route);root.document?.body?.classList?.toggle('is-music-studio-route',isMusicRoute(route))}
  let lastRoute=currentRoute(),restoringRoute=false;
  function handleHistoryRouteChange(paint){const route=currentRoute();if(restoringRoute){restoringRoute=false;return}if(route===lastRoute)return;if(state.dirty){const leave=root.confirm?.('未保存の変更があります。破棄して移動しますか？')??false;if(!leave){restoringRoute=true;root.location.hash=lastRoute;return}state.dirty=false}cancelScheduledSave();lastRoute=route;paint(route)}
  function installHostRoutes(){if(typeof root.managementViewForRoute!=='function')return false;const base=root.managementViewForRoute;root.managementViewForRoute=function(route){syncHostChrome(route);return isMusicRoute(route)?renderRoute(route):base(route)};const baseSetView=typeof root.setView==='function'?root.setView:null;if(baseSetView)root.setView=function(route){if(parseEditId(currentRoute())!==parseEditId(route))cancelScheduledSave();lastRoute=route;syncHostChrome(route);return baseSetView(route)};const baseOpenApp=typeof root.openApp==='function'?root.openApp:null;if(baseOpenApp)root.openApp=function(appId,urlOverride){if(appId==='musicStudio'&&!urlOverride)return root.setView(HOME_ROUTE);return baseOpenApp(appId,urlOverride)};const historyHandler=()=>handleHistoryRouteChange(route=>{syncHostChrome(route);if(isMusicRoute(route))root.render?.()});root.addEventListener?.('popstate',()=>root.setTimeout?.(historyHandler,0));root.addEventListener?.('hashchange',historyHandler);syncHostChrome(currentRoute());root.render?.();return true}
  let standaloneTarget=null;
  function paintStandalone(){standaloneTarget=standaloneTarget||root.document?.querySelector('#music-studio-app');if(!standaloneTarget)return;const route=currentRoute();standaloneTarget.innerHTML=renderRoute(route,{standalone:true});restoreEditorPitchScroll();root.document.title=route===HOME_ROUTE?'Music Studio':'Music Studio — プロジェクト管理'}
  function mountStandalone(){const historyHandler=()=>handleHistoryRouteChange(()=>paintStandalone());root.addEventListener?.('popstate',()=>root.setTimeout?.(historyHandler,0));root.addEventListener?.('hashchange',historyHandler);paintStandalone()}
  async function initialize(){try{await loadSettings();configureAutosave();await refresh();await loadAutoBackups();await loadMidiHistory();await loadMidiImportHistory();requestPaint();const automatic=await runAutoBackupCheck();if(automatic.created){state.notice=automatic.message;state.noticeKind='success';requestPaint()}}catch(error){console.error('Music Studio storage could not be opened',error);repository=memoryRepository();state.settings=normalizeSettings({});state.settingsDraft=clone(state.settings);state.loaded=true;notice('専用保存領域を開けないため、このセッションでは一時保存になります。','error')}}

  const api={HOME_ROUTE,PROJECTS_ROUTE,NEW_ROUTE,EDIT_ROUTE,MIDI_EDITOR_ROUTE,SETTINGS_ROUTE,BACKUP_ROUTE,LOGIC_PRO_ROUTE,FORMAT,SCHEMA_VERSION,APP_VERSION,DB_NAME,STORE_NAME,SETTINGS_STORE_NAME,AUTO_BACKUP_STORE_NAME,MIDI_HISTORY_STORE_NAME,MIDI_IMPORT_HISTORY_STORE_NAME,SETTINGS_FORMAT,SETTINGS_VERSION,BACKUP_FORMAT,BACKUP_VERSION,LAST_PROJECT_KEY,FEATURES:FEATURES.map(item=>({...item})),STATUS_LABELS:{...STATUS_LABELS},PROJECT_STATUS_LABELS:{...PROJECT_STATUS_LABELS},state,defaultSettings,normalizeSettings,validateSettings,makeProject,validateProject,safeFileName,fileNameTemplateErrors,renderFileName,memoryRepository,indexedDbRepository,setRepository,loadSettings,refresh,homeView,createView,listView,editView,midiEditorView,settingsView,backupView,logicProView,placeholderView,renderRoute,isMusicRoute,installHostRoutes,mountStandalone,initialize,submitCreate,submitEdit,duplicateProject,askDelete,exportProject,importText,importFile,selectedMidiProject,midiExportInput,midiExportSummary,requestMidiExport,cancelMidiExport,createMidiTestProject,performMidiExport,loadMidiHistory,inspectMidiFile,cancelMidiImport,loadMidiImportHistory,saveMidiImport,importMidiAsNew,importMidiAsDuplicate,inspectAudioReference,openMidiEditor,openPart,openMelody,openLogicPro,drumNoteName,noteLabelMode,updateNoteLabelElement,updateNoteLabelLayout,editorVisibleBarRange,syncEditorPianoPosition,editorPlaybackTracks,editorSelectPart,editorSelectNote,editorToggleMultiSelect,editorSelectAllNotes,editorStartNoteDrag,editorStartMarqueeSelection,editorStartNoteResize,editorMoveSelected,editorResizeSelected,editorSetSnap,editorToggleSnap,editorResetZoom,editorMatchDuration,editorMatchVelocity,editorClearSelection,editorHandleShortcut,editorZoom,editorAddMeasures,editorRemoveMeasures,editorFitPitchRange,editorRememberPitchScroll,editorPreviewPitch,editorAddNote,editorDeleteNote,editorLockSelectedNotes,editorUnlockSelectedNotes,editorUndo,editorRedo,editorCopy,editorPaste,editorDuplicate,editorToggleMeasure,editorSelectMeasureRange,editorToggleLock,editorPrepareRegeneration,editorPianoInput,editorInitializeMidi,editorSelectMidiInput,editorSelectMidiTarget,editorStartMidiRecording,editorStopMidiRecording,editorStopTransport,editorPlayMelody,editorStopMelody,editorUpdateTransposeSettings,editorPreviewTranspose,editorCancelTranspose,editorApplyTranspose,editorUpdateCorrectionGuide,editorPreviewCorrection,editorToggleCorrectionPreview,editorCancelCorrection,editorApplyCorrection,editorDrumInput,editorGenerateCandidate,editorApplyCandidate,editorUpdateSelected,saveMidiEditor,hasUnsavedChanges,markDirty,markSettingsDirty,updateFileNamePreview,saveSettingsForm,persistSettings,discardSettings,resetSettings,exportSettings,importSettingsText,importFile,backupObject,validateBackup,exportBackup,previewBackupText,previewBackupFile,restoreBackup,restorePreviewedBackup,clearBackupPreview,loadAutoBackups,runAutoBackupCheck,checkAutoBackupNow,restoreAutoBackup,backupSignature,saveCurrentProject,configureAutosave,cancelScheduledSave,cancelEdit,clearNotice,getSettings(){return clone(state.settings||defaultSettings())},getMidiSettings(){return clone((state.settings||defaultSettings()).midi)},getLogicProSettings(){return clone((state.settings||defaultSettings()).logicPro)},searchProjects(value){state.search=value;const query=value.trim().toLocaleLowerCase('ja');const items=state.projects.filter(project=>!query||[project.projectName,project.songTitle,project.purpose].some(field=>String(field||'').toLocaleLowerCase('ja'))).sort((a,b)=>(b.updatedAt||'').localeCompare(a.updatedAt||''));const target=root.document?.querySelector('.music-project-list');if(target)target.innerHTML=items.map(projectRow).join('')||`<div class="music-empty"><p>${query?'検索条件に一致するプロジェクトはありません。':'プロジェクトはまだありません。'}</p><button class="music-primary" onclick="MusicStudio.goNew()">最初のプロジェクトを作る</button></div>`},openProject(id){cancelScheduledSave();state.dirty=false;setLastProject(id);routeTo(`${EDIT_ROUTE}/${encodeURIComponent(id)}`)},goHome(){go(HOME_ROUTE)},goProjects(){go(PROJECTS_ROUTE)},goNew(){go(NEW_ROUTE)},goBackup(){go(BACKUP_ROUTE)},goSettings(){go(SETTINGS_ROUTE)}};
  api.editorToggleMidiRecording=editorToggleMidiRecording;
  api.editorSetTrackMuted=editorSetTrackMuted;
  api.editorSetTrackSolo=editorSetTrackSolo;
  api.editorToggleCountIn=editorToggleCountIn;
  api.editorToggleMetronome=editorToggleMetronome;
  api.editorSetQuantize=editorSetQuantize;
  api.editorToggleQuantize=editorToggleQuantize;
  api.editorApplyQuantize=editorApplyQuantize;
  api.editorSetSelectedNoteLength=editorSetSelectedNoteLength;
  api.editorStartPartialEdit=editorStartPartialEdit;
  api.editorPreviewPartialEditPitchUp=editorPreviewPartialEditPitchUp;
  api.editorRunPartialEditProvider=editorRunPartialEditProvider;
  api.editorApplyPartialEdit=editorApplyPartialEdit;
  api.editorCancelPartialEdit=editorCancelPartialEdit;
  api.editorPreviewPitchFromKey=editorPreviewPitchFromKey;
  api.pianoPitchY=pianoPitchY;api.pianoYToPitch=pianoYToPitch;
  api.editorToggleMelodyPlayback=editorToggleMelodyPlayback;
  api.editorSetPlayheadPosition=editorSetPlayheadPosition;
  api.editorStartPlayheadMove=editorStartPlayheadMove;
  api.editorStartLoopRange=editorStartLoopRange;
  api.editorToggleLoop=editorToggleLoop;
  api.editorClearLoop=editorClearLoop;
  api.editorUpdateNoteLengthSettings=editorUpdateNoteLengthSettings;
  api.editorPreviewNoteLength=editorPreviewNoteLength;
  api.editorCancelNoteLength=editorCancelNoteLength;
  api.editorApplyNoteLength=editorApplyNoteLength;
  api.editorSetEditRange=editorSetEditRange;
  api.editorCloseCorrectionPanel=editorCloseCorrectionPanel;
  api.importSettingsFile=importSettingsFile;
  api.openHelp=openHelp;
  api.closeHelp=closeHelp;
  api.openFirstSongWindow=openFirstSongWindow;
  api.focusMusicStudio=focusMusicStudio;
  api.goDreamArchitect=goDreamArchitect;
  api.openProject=function(id){if(!guardLeave())return false;cancelScheduledSave();state.dirty=false;setLastProject(id);routeTo(`${EDIT_ROUTE}/${encodeURIComponent(id)}`);return true};
  root.MusicStudio=api;
  if(root.document){root.addEventListener?.('resize',()=>{scheduleNoteLabelLayout();scheduleEditorPianoPosition()});root.addEventListener?.('pagehide',()=>{discardLiveRecordingOnUnload();if(state.midiEditor?.dirty)flushMidiEditorAutosave()});root.document.addEventListener?.('visibilitychange',()=>{if(root.document.visibilityState==='hidden'&&state.midiEditor?.dirty)flushMidiEditorAutosave()});root.addEventListener?.('beforeunload',event=>{if(state.midiEditor?.dirty)flushMidiEditorAutosave();if(hasUnsavedChanges()){event.preventDefault?.();event.returnValue=''}});if(root.document.body?.dataset.musicStudioStandalone==='true')mountStandalone();else installHostRoutes();initialize()}
})(typeof window!=='undefined'?window:globalThis);
