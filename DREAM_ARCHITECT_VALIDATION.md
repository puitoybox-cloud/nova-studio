# LINK-04〜LINK-06 検証記録

検証日：2026-07-20

## 自動・静的確認

- 全JavaScriptファイル：`node --check` 成功（同梱Node.jsを使用）
- 連携コア：10テスト成功
- `git diff --check`：成功
- package.jsonがない静的HTML/JavaScript構成のため、プロジェクト定義のlint・buildコマンドはなし

テストはキャラクター0/1/複数、正式・Vidu画像、永続参照なし、話数なし、旧・新共有形式、不正共有形式、結果1/複数、不正結果、同一・変更済み重複を対象とする。

## ブラウザ確認

- 1440×900：ホーム、選択画面、送信前確認、外部未接続案内、結果候補を確認。横スクロールなし、ボタン重なりなし、コンソールエラーなし。
- 1024×768：ホーム、Story Archive、検索、設定、バックアップ、連携画面を確認。各画面 `scrollWidth === clientWidth`、結果候補は縦スクロール内に収まり、操作ボタンの重なりなし。
- Story Archive：ホームへ戻る導線、作品・話数表示、検索・フィルター、カード空状態を確認。
- JSON：バックアップ画面と書出し・読込入口を確認。ブラウザ内のdownloadイベント取得はできなかったため、実ファイル生成の最終確認はティア確認待ち。
- 既存localStorage：削除・初期化・置換操作を実行していない。既存データの読込後にホームと各管理画面を表示できることを確認。

## 帰宅後の受入手順

1. Draft PRのブランチを開き、Nova Studioホームが従来どおり表示されることを確認する。
2. 作品と話数を切り替え、再読込後も選択が維持されることを確認する。
3. 「連携内容を選ぶ」を開き、作品名・話数・未接続表示を確認する。
4. キャラクターと素材を複数選び、「送信前確認へ」を開く。
5. 作品ID、話数、キャラクター、素材、共有されない情報、作成日時を確認する。
6. 「選択内容を変更する」「共有データを作り直す」「キャンセル」を順に確認する。
7. 「Dream Architect Studioを開く」で未接続案内とv2キーが表示されることを確認する。
8. 制作結果JSONを読み込み、登録前確認、保留、却下を確認する。
9. 同じresultIdを再読込し、同内容は増えず、変更内容は更新候補になることを確認する。
10. Story Archiveへ戻り、検索、画像資料、正式採用・Vidu参照表示を確認する。
11. 設定の背景を変更し、文字が読めることを確認する。
12. バックアップJSONを書き出し、別の安全なブラウザプロファイルで読み込めることを確認する。
13. Mac実機の標準幅とiPad横向きで横スクロール、重なり、見切れがないことを確認する。

実機確認が終わるまで、ロードマップ状態は「ティア確認待ち」とする。
